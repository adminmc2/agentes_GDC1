# REVIEW — Plan de trabajo paso a paso (con gates de validación)

> **Qué es este documento:** plan operativo de las tareas pendientes. Cada paso tiene: objetivo, archivos involucrados, **condiciones de cierre (gate)**, y validación del revisor. **No se pasa al siguiente paso hasta que TODAS las condiciones se cumplen.** El revisor lee este documento en cada iteración y certifica el cumplimiento de cada gate.
>
> **Audiencia:** revisor (validar gates) + ejecutor (Claude Code) + autor (decidir).
>
> **Relación con `PROCESO-MAESTRO.md`:** maestro = decisiones cerradas + bitácora; REVIEW = plan ejecutable con gates pendientes.
>
> **Rol de este archivo:** estado vivo + plan + bitácora. **NO es autoridad operativa** — la autoridad de cómo actuar es `CLAUDE.md`.
>
> **Regla editorial:** la bitácora se centra en **estado y decisiones vivas**. Entradas nuevas cortas. Cuando una entrada deja de ser operativa, se compacta y, si procede, se referencia el histórico (`docs/historico/`) en lugar de replicar el detalle.
>
> **Última actualización:** 2026-05-24 (v12.4 — Capa 2 sobre U1 cierra **explicaciones de cuadros**: 12 explicaciones nuevas (4 vocabulario + 5 gramática + 3 verbales) inyectadas en `nc1-reciclaje.json` con rigor pedagógico — flujo didáctico, prerrequisitos, arco del curso, convivencia con plano verbal. **Paleta pastel unificada** en el dashboard reciclaje: section-headers, dots y chips del modal matriz alineados con el sistema `cat-tag.p-*` del inventario. Cierra Fase 1 + Fase 2 del plan editorial — abre paso a Fase 3 (Capa 2 U2-U9 + cierre cross-hilo por unidad)).
>
> **Hito previo (v12.3):** 2026-05-24 (Modal del reciclaje rediseñado: strip de dots → **mapa matriz hilos × unidades** + tiempos verbales en timeline. Cierra la Fase 1 del plan editorial — infra visual lista).
>
> **Hito previo (v12.2):** 2026-05-24 (Dashboard sub-agrupa por `_grupo` en el bloque gramatical + deuda B.D1 abierta).
>
> **Hito previo (v12.1):** 2026-05-23 (Reconciliación fase 1 ↔ registry v1.7 — grupo "Tiempos y modos verbales": 5 entradas nuevas + regeneración canónico).
>
> **Hito previo (v11.85):** 2026-05-23 (Dashboard: drawer lateral → modal centrado 760px/88vh — REDISEÑO §4.4).
>
> **Hito previo (v11.82):** 2026-05-23 (Sincronización **automática** de `_meta.version` y `_meta.fecha` del canónico vía pre-commit hook (`scripts/hooks/pre-commit` + `scripts/sync_meta_reciclaje.py`); cualquier commit que toque `nc1-reciclaje.json` actualiza `_meta` antes de cerrar. Documentado en `fases/2-reciclaje/CLAUDE.md`. El dashboard siempre reflejará la versión real sin intervención manual).
>
> **Hito previo (v11.0):** 2026-05-19 (Milestone post-fase 1. Bump major + fix scanner dashboard).
>
> **Hito previo:** 2026-05-18 (v10.164 — **U9 cerrada (fin de fase 1)**: 'Ropa', pp. 92-101. 41 actividades, 6 cuadros, 8 _decisiones_ia. Migración Opción C legacy→canónico post-v10.153. Verbos: ir/estar en Pretérito indefinido (registry verbal ya lo soportaba). Rectificación revisor (4 puntos): (1) /-a en Colores con fem atestada (rojo/-a, blanco/-a, negro/-a); (2) 'oscuro' reubicado de Colores → Adjetivos descriptivos; (3) bucket 'Adjetivos descriptivos' recurrente creado con 8 items perdidos (alto/-a, ancho/-a, corto/-a, deportivo/-a, estrecho/-a, grande, largo/-a, oscuro/-a) — mismo bug sistémico que U5/U8; (4) refs de actividad actualizadas. Nueva deuda matcher: 'morado/-a' no codificado por image.descripcion ausente en INPUT_FIELDS_LIST. Validador U9 → 0/0/0. **Estado global: U0-U9 saneadas. Fase 1 cerrada en su parte mecanizable.**

---

## Principio arquitectónico (no negociable)

**Datos centralizados + instrucciones modulares.**

- Los datos de cada unidad viven una sola vez: `unidades/UX/...`. Nunca se duplican por fase.
- Las instrucciones de cada fase viven en su carpeta: `fases/N-X/CLAUDE.md` + `fases/N-X/prompt.md`. Estas SÍ son modulares por fase.
- Razón: viola la Regla de Oro #5 (una fuente única) duplicar datos. CLAUDE.md modular ya carga solo el contexto relevante; los datos solo se cargan cuando Claude Code los lee con `Read`.
- Optimización de tokens cuando aplique: leer secciones específicas con offset/limit en lugar del archivo entero; sesiones limpias por fase (`Ctrl+L`).

---

## Reglas globales del proceso (aplican a TODOS los pasos)

Antes de declarar un paso como ✅ completado, se verifica que TODAS estas actualizaciones meta se hayan ejecutado:

| Archivo meta | Cuándo se actualiza |
|---|---|
| **`CLAUDE.md`** (raíz) | Si el paso introduce: nueva fase operativa, nueva regla de oro, nuevo comando útil, cambio en estructura de carpetas, cambio en convención de naming, nuevo archivo en lista de "documentos relacionados". |
| **`PROCESO-MAESTRO.md`** | Si el paso cierra una decisión nueva (entra en Parte 4) o resuelve una pendiente (sale de Parte 5). En cualquier caso, se añade entrada a la bitácora. |
| **`REVIEW.md`** (este archivo) | Siempre. Tras completar un paso: marcar ✅, registrar gate-met, añadir entrada a bitácora del propio REVIEW, insertar pasos nuevos si aparecieron. |
| **`CHANGELOG.md`** | Si el paso implica un commit relevante (cambio físico de archivos, código nuevo, documento nuevo). Una entrada por commit con motivo, movimientos, actualizaciones de referencias. |
| **`README.md`** | Si cambia estructura raíz, comandos, o instalación. |
| **`.gitignore`** | Si se añaden/eliminan paths trackeables o se cambia política. |

**Si alguna actualización meta no se hizo, el gate del paso NO se considera cumplido**, aunque el trabajo principal esté hecho.

---

## Estado global

| Bloque | Estado |
|---|---|
| Fase 1 — Extracción de inventario | ✅ **U0-U9 saneadas** con el contrato canónico v10.145 + §5.10 + §5.11. **Fase 1 cerrada en su parte mecanizable** (v10.164). Trayecto: Lote 3D-cleanup (U0-U5) + U6 piloto v10.158-v10.160 + U7 v10.161 + U8 v10.163 + U9 v10.164. Las 10 unidades validan 0/0/0. Deudas residuales: deuda matcher **cerrada** (v11.3-v11.5); abiertas — canónicos huérfanos (`Abreviaturas de los diccionarios` U8, `vegetariano` U4, escalados §0.1) y auditoría retroactiva U0-U5 parcialmente cubierta. **Canon semántico activado** en rollout R1 (v10.108). Rediseño operativo cerrado en v10.115-117 (reglas IA-first + 4 registries poblados). **6 archivos operativos** + 4 registries + 4 PCIC A1. `validar_inventario.py` alineado con la parte mecanizable del contrato (shape canónico + §5.10 A + §5.11) desde v10.151. Deuda residual: parte editorial no automatizable (§5.10 Categoría B + excepciones léxicas de §5.11) delimitada en `schema-inventario.md` §A.3. |
| Infraestructura (dashboard, validador) | ✅ Activa **local** (`python3 diagrama.py`). Sin despliegue en la nube — stack Docker/Railway retirado en v11.21 (el dashboard es herramienta local; B5 queda superado). |
| Documentación raíz (CLAUDE.md, README, PROCESO-MAESTRO, REVIEW) | ✅ Sincronizada (v10.108f) |
| Bloque B (cerrar infraestructura fase 1) | 🔄 Parcial — B1.5 ✅ · Fase 2 reciclaje **REACTIVADA** (v11.69): Nivel 4 cerrado en herramienta — Capa 1 (`generar_reciclaje_capa1.py`) + validadores del gate (`validar_reciclaje.py`, `validar_cross_unidad.py`) + `nc1-reciclaje.json` regenerado al shape del rediseño (118 hilos). **Pendiente:** la Capa 2 IA no se ha estrenado sobre ninguna unidad — su primera corrida es su shakedown. `integrar_unidad.py` ya no toca el reciclaje (lo gestiona el pipeline de fase 2) · tarjetas dependen de B1+fase 2 · píldoras dependen de fase 5 · B5 superado (despliegue retirado v11.21) |
| Bloque C (fases 2-8) | 📋 Pendiente |
| Bloque D (lecciones Claude Code) | 📋 Pendiente |
| Bloque E (limpieza final) | 📋 Pendiente |

---

## Bloque A — Estabilizar la fase 1 ✅ CERRADO 2026-05-07

**Sub-pasos:**
- **A1.** Validar U3 con el autor — ✅ cerrado 2026-05-05.
- **A2.** Probar el sistema con una unidad nueva — ✅ cerrado con reserva 2026-05-05 (probado con U0).
- **A3.** Resolver bugs B1-B4 — ✅ cerrado 2026-05-05.
- **A4.** Refactor documental de fase 1 — ✅ cerrado 2026-05-07 (merge `110e722`, v10.69; cierres post-merge v10.70-v10.76b).

**Resultado histórico (al cierre del Bloque A, 2026-05-07):** U0/U1/U3 validaban 0/0 con el contrato post-refactor (taxonomía 20 + destreza/enfoque); **6 archivos operativos** en `fases/1-extraccion-inventario/` (CLAUDE, prompt, schema, reglas, glosario, convenciones-y-casos — el glosario se añadió en v10.111); validador y schema alineados hasta v10.62 (cross-check A4.5.5); en ese momento la alineación post-rediseño v10.115-117 quedó declarada como deuda transitoria con validación manual sustitutiva en el Apéndice §A.1 del schema; refs históricas archivadas en `docs/historico/refactor-prompt-fase1/`. **Actualización posterior (v10.151):** el validador absorbió la parte mecanizable del contrato (shape canónico + §5.10 A + §5.11), retirando la sustitución por validación manual para esa parte; la deuda residual (parte editorial) quedó delimitada en §A.3 del schema.

**Detalle íntegro archivado** (sub-pasos A4.0-A4.6, gates, riesgos, plan ejecutable completo) en `docs/historico/REVIEW-bloque-A-cerrado.md`. La bitácora cronológica general permanece viva más abajo en este mismo archivo.

## Bloque B — Cerrar la infraestructura de fase 1 (parcial)

> **Decisiones del autor (2026-05-05):** el bloque B se ejecuta en partes:
> - **`nc1-tarjetas.json`**: requiere fase 2 primero. Sin tarjetas generadas, el global está vacío. Pendiente hasta cerrar fase 2.
> - **`nc1-pildoras.json`**: pendiente hasta trabajar U3 vocabulario (fase 5).
> - **`nc1-reciclaje.json`**: se puede discutir y diseñar ahora. El autor quiere definir cómo construirlo antes de implementar — formalizado como **B1.5** (gate de diseño previo a B2).

> **🟠 Deuda viva — reconciliación fase 1 ↔ registry gramatical v1.7 (2026-05-23):** Las 5 categorías del grupo «Tiempos y modos verbales» se canonizaron en `gramatica-canonica.json` (v11.56, commit 0ff6c8c) pero **no se retroinyectaron** en `gramatica_consolidada` de U3/U4/U6/U7. Por eso `generar_reciclaje_capa1.py` (que lee inventario + índice, no `_apariciones` del registry) no proyecta esas categorías a `nc1-reciclaje.json`. **No es bug de render**: es divergencia entre tres capas de verdad (registry / inventarios / Capa 1). **Resolución diferida al ejecutor 2** — exige decidir frontera de modelo (¿dato verbal en `tiempos_y_verbos_consolidado` y `rasgo_por_tiempo` vs. categoría en `gramatica_consolidada`?) antes de cualquier reescritura. **No tocar el generador para leer `_apariciones`**: rompería el contrato determinista de Capa 1.

### B1. Escribir scripts Python para los JSONs globales del curso

**Objetivo:** poder regenerar `nc1-tarjetas.json`, `nc1-pildoras.json` con scripts deterministas (cero LLM).

**Pre-condición:** A1 + A2 cerrados (sin un sistema de extracción estable, los globales son prematuros). B3 (decidir migración del contenido editorial) idealmente cerrado.

**Archivos a crear:**
- `scripts/regenerar_tarjetas_globales.py` — lee `unidades/U*/tarjetas/csv/*.csv`, escribe `unidades/nc1-tarjetas.json` siguiendo el esquema de PROCESO-MAESTRO Parte 4 decisión 18.
- `scripts/regenerar_pildoras_globales.py` — lista `unidades/U*/pildoras/*.pdf`, parsea TEX para títulos, escribe `unidades/nc1-pildoras.json` siguiendo decisión 19.

**Archivos a modificar:**
- `CLAUDE.md` — sección "Comandos útiles": añadir los dos comandos nuevos.
- `fases/1-extraccion-inventario/prompt.md` — referenciar los scripts en la sección de pipeline.

**Actualizaciones meta requeridas:**
- `CLAUDE.md` ✓ (comandos).
- `PROCESO-MAESTRO.md` — bitácora.
- `REVIEW.md` — gate marcado.
- `CHANGELOG.md` — entrada por commit.
- `README.md` — si los scripts cambian la estructura visible.

**Gate de cierre:**
1. ✅ Los dos scripts creados y ejecutables.
2. ✅ Cada uno tiene docstring con uso.
3. ✅ Idempotencia verificada: ejecutar dos veces produce el mismo output.
4. ✅ Manejan el caso "carpeta vacía" sin fallar.
5. ✅ Documentados en CLAUDE.md.
6. ✅ Commit + CHANGELOG.

**Bloquea a:** B2 (parte tarjetas + píldoras).

---

### B1.4. Crear `nc1-curso.json` — índice editorial global del curso ✅ CERRADO 2026-05-08 (v10.82)

**Objetivo:** disponer del índice editorial canónico del curso como artefacto JSON consultable, antes de diseñar `nc1-reciclaje.json` (B1.5). Sin un mapa global de "qué enseña cada unidad", el reciclaje no se puede mapear de forma trazable.

**Fuente:** índice oficial del libro impreso (Scope and Sequence, páginas 6-7). NO se usa `viejo/00-curso-general.md`, que contenía datos imprecisos en páginas y mezclaba dato editorial con metadocumentación pedagógica.

**Decisiones de schema** (registradas como decisión 35 en PROCESO-MAESTRO Parte 4, refinada en v10.82b):
- Path: `unidades/nc1-curso.json`.
- Top-level: `curso`, `titulo`, `editorial`, `nivel`, `fuente`, `estructura_libro`, `unidades`, `apendice`, `_nota` (opcional).
- Por unidad regular (U1-U9): campos top-level `vocabulario`, `gramatica`, `para_aprender` (string o `null`), `pronunciacion_ortografia`, `comunicacion`, `destrezas`, `cultura`, `pagina_inicio`, `paginas_libro`.
- Por U0 (atípica): solo `contenido_general`.
- Apéndice: array de objetos con `seccion` y `pagina_inicio`, sin contenido detallado.
- Contenido de las celdas: **literal del índice del libro**, sin interpretación MCER añadida.
- **Source of truth refinada (v10.82b):** `nc1-curso.json` es canónico para el índice editorial; los inventarios per-unidad pueden divergir legítimamente y la divergencia se anota como deuda técnica en `_nota`, **no bloquea cierre**.

**Hallazgos durante la extracción del índice del libro:**
- Las páginas en `viejo/00-curso-general.md` estaban mal: U1=10-23 y U2=14-33 se solapaban. Las correctas según Scope and Sequence son U1=12-21, U2=22-31, ..., U9=92-101.
- **U3 paginación divergente:** Scope dice 32-41; el inventario actual de U3 dice 34-43. **Causa identificada por el autor:** el PDF actual de U3 tiene errores; la unidad se re-extraerá por el ejecutor 2 en un worktree paralelo cuando el autor proporcione un PDF correcto. Anotado en `_nota` de `nc1-curso.json`.
- Los `contenidos_indice` ya extraídos de U1 y U3 mezclan "PARA APRENDER" (estrategia metacognitiva) dentro del campo `gramatica` (ej. U3 "Hacer un cuaderno de vocabulario"). Anotado como fix futuro de los inventarios.

**Gate cerrado:**
1. ✅ `unidades/nc1-curso.json` creado con datos literales de las 10 unidades + apéndice.
2. ✅ JSON parseable.
3. ✅ Decisión 35 registrada en PROCESO-MAESTRO Parte 4.
4. ✅ Commit + CHANGELOG.

**Bloquea a:** B1.5.

---

### B1.5. Diseño de `nc1-reciclaje.json` — definir contrato antes de generar ✅ CERRADO (v10.89, 2026-05-08)

**Objetivo:** cerrar formalmente cómo se construye `nc1-reciclaje.json`.

**Gate de cierre:**
1. ✅ Disparadores, formato per-entrada, política de mantenimiento y esqueleto top-level cerrados con el autor.
2. ✅ Decisión registrada en PROCESO-MAESTRO.md (bloque `nc1-reciclaje.json`).
3. ✅ Esqueleto top-level confirmado — igual al que asume B2. Sin ajustes en B2.
4. ✅ `unidades/nc1-reciclaje.json` creado vacío.
5. ✅ Commit + CHANGELOG.

**Bloquea a:** B2 (parte reciclaje).

---

### B2. Completar los 3 JSONs globales del curso

**Objetivo:** tener `nc1-tarjetas.json`, `nc1-pildoras.json` y `nc1-reciclaje.json` en `unidades/` con contenido real. **U3 no ancla este paso.**

**Estado actual:**
- `nc1-reciclaje.json` — ✅ poblado pase 1 (v10.94): 23 hilos / 70 eventos cubriendo U0-U9 desde `nc1-curso.json`. Schema modelo de hilos, taxonomía 4 tipos (vocabulario, forma_verbal, contenido_gramatical, estrategia).
- `nc1-tarjetas.json` — 📋 no existe. Bloqueado por B1 (script) + fase 2 (vocabulario analizado).
- `nc1-pildoras.json` — 📋 no existe. Bloqueado por fase 5 (píldoras por unidad).

**Sub-pasos desacoplados:**

**B2a — Primera población de `nc1-reciclaje.json`** ⚠ **ANULADO (replanteamiento 2026-05-11)**

El modelo de hilos jerárquicos (mapa/auto/detalle, pase 1/pase 2) queda anulado por la propuesta E-final, que mueve la autoridad de naming de fase 2 a fase 1 mediante un canon semántico. Los hilos del `nc1-reciclaje.json` actual quedan **congelados** hasta que el canon esté limpio. La reformulación de fase 2 entra como trabajo posterior al cierre del canon.

- ~~B2a.1 — Pase 1 (mapa)~~: estado histórico (v10.94 cubría 23 hilos / 70 eventos). Se reformula tras canon.
- ~~B2a.2 — Pase 2 (detalle)~~: no se ejecutará en su forma original.

**B2b — Crear `nc1-tarjetas.json`** (bloqueado)
- Pre-condición: B1 (script `regenerar_tarjetas_globales.py`) + al menos una unidad con vocabulario analizado (fase 2).

**B2c — Crear `nc1-pildoras.json`** (bloqueado)
- Pre-condición: fase 5 (primera unidad con píldoras).

**Actualizaciones meta requeridas (por sub-paso):**
- `PROCESO-MAESTRO.md` — bitácora.
- `REVIEW.md` — gate.
- `CHANGELOG.md` — commit.

**Gate de cierre de B2 completo:**
1. ✅ Los 3 archivos existen y tienen contenido real.
2. ✅ Cada uno cumple su esquema (validable visualmente).
3. ✅ Commit + CHANGELOG.

**Bloquea a:** B4.

---

### B3. Decidir migración de tarjetas/pildoras/MDs de U3 a `unidades/U3/`

**Objetivo:** cerrar la cuestión de si solo el inventario de U3 está en el sistema activo, o todo el contenido editorial.

**Pre-condición:** ninguna (decisión del autor).

**Archivos involucrados (si se decide migrar):**
- Mover `viejo/unidades/U03/tarjetas/` → `unidades/U3/tarjetas/`.
- Mover `viejo/unidades/U03/pildoras/` → `unidades/U3/pildoras/`.
- Mover los markdowns de sección → `unidades/U3/<seccion>.md` (decidir naming).

**Actualizaciones meta requeridas:**
- `PROCESO-MAESTRO.md` — entrada en Parte 4 con la decisión.
- `CLAUDE.md` — actualizar estructura del repo si cambia.
- `REVIEW.md` — gate.
- `CHANGELOG.md` — entrada del movimiento.
- `.gitignore` — actualizar paths si aplica.

**Gate de cierre:**
1. ✅ Decisión cerrada y registrada en PROCESO-MAESTRO Parte 4.
2. ✅ Si se decide migrar: contenido movido y rutas internas actualizadas.
3. ✅ Si se decide NO migrar: anotado claramente que U3 activa = solo inventario, el resto vive en viejo y se aprovechará al final.
4. ✅ Commit + CHANGELOG.

---

### B4. Integrar visualización de los 3 JSONs globales en el dashboard

**Objetivo:** poder revisar los globales desde el dashboard.

**Pre-condición:** B2 cerrado.

**Archivos a modificar:**
- `diagrama.py` — endpoint `/api/reciclaje` ya existe (v10.92); pendientes `/api/tarjetas` y `/api/pildoras` cuando se generen los JSONs respectivos.
- `web/index.html` — vista RECICLAJE ya operativa (v10.92); pendientes vistas para tarjetas y píldoras.

**Actualizaciones meta requeridas:**
- `CLAUDE.md` — solo si introduce cambio en cómo invocar dashboard.
- `PROCESO-MAESTRO.md` — bitácora.
- `REVIEW.md` — gate.
- `CHANGELOG.md` — commit.

**Gate de cierre:**
1. ✅ Los 3 endpoints funcionan (verificar con `curl`).
2. ✅ La vista en el dashboard muestra los 3 globales legibles.
3. ✅ Schema-aware: si un global está vacío, se ve "vacío" sin errores.
4. ✅ Commit + CHANGELOG.

---

### B5. Despliegue público del dashboard — ⊘ SUPERADO (revertido en v11.21)

> **Estado v11.21:** el despliegue en Railway se retiró. Repo A ya no se publica en la nube — el dashboard es herramienta local (`python3 diagrama.py`). El stack `Dockerfile` / `.dockerignore` / `railway.toml` se eliminó. Razón: el despliegue tenía sentido cuando se compartía con el equipo y los módulos de agentes estaban en este repo; tras la migración a dos repos (los agentes viven en repo B) y la decisión de no desplegar repo A, el stack quedó sin uso. Lo de abajo es registro histórico de cuando B5 estuvo activo.
>
> **Contexto (histórico):** este paso se ejecutó **fuera del orden B1-B4**, como carril paralelo, a petición del autor para compartir el dashboard con el equipo editorial.

**Objetivo:** dashboard accesible públicamente para el equipo editorial (consultar Inventarios + Proyecto), sin BD ni Langfuse.

**Decisión de scope (autor):** los módulos con BD (tarjetas, correcciones, reglas, agentes, trazas) se quedan inactivos en el despliegue porque corresponden a flujos no operativos hoy.

**Archivos involucrados:**
- `requirements.txt` — quitadas deps no usadas (`crewai`, `langfuse`, `deepeval`).
- `Dockerfile` — quitado nodejs y promptfoo CLI no usados.
- `diagrama.py` — fixes ortográficos en etiquetas Mermaid (Compañeros, Validación, Píldoras, etc.).
- `railway.toml` — sin cambios, ya existente.

**Gate cumplido:**
1. ✅ URL pública responde HTTP 200: `https://entornoeditorial.up.railway.app`.
2. ✅ `/api/version` devuelve la versión del CHANGELOG (auto-leída).
3. ✅ Build slim funcional (deps mínimas: `psycopg2-binary` + `python-dotenv`).
4. ✅ Diagramas Mermaid con ortografía corregida (tildes y eñes).
5. ✅ Commits referenciados: `5024914` (fix ortográfico v10.33), `3611bd7` (build slim v10.34), `b3b07e2` (CHANGELOG retroactivo). Pusheados a `origin/main`.

**Bloquea a:** ya nada operativo. Pendiente de decisión externa: si en el futuro se quiere control de acceso (basic auth o Cloudflare Access), se abre paso aparte.

---

### Deuda abierta — B.D1 · Política general de recurrentes post-principal de paradigmas abstractos

**Estado:** 📋 **Abierta** (2026-05-23, derivada de v12.1 scope B).

**Pregunta a cerrar:** ¿Todo paradigma abstracto que entra como **principal** en `gramatica_consolidada` debe seguir teniendo cola **recurrente explícita** en las unidades posteriores donde reaparece materialmente (formas atestadas que aplican el paradigma sin nuevo cuadro)? Hoy la convención editorial existe solo para categorías léxicas y para algunas categorías gramaticales clásicas (Interrogativos, Pronombre sujeto, Hay…); no está cerrada como **política general** para paradigmas verbales abstractos.

**Por qué surge ahora:** la pasada de coherencia previa al lote v12.1 detectó 3 grupos de aplicaciones post-principal sin cobertura en `gramatica_consolidada`:

1. **Paradigma regular del presente (-ar/-er/-ir)** — principal U3. Aplicaciones atestadas en U4 (comer, llamarse), U5 (ahorrar, contaminar, vivir), U6 (abrir, beber, estudiar, llamarse, pasear, trabajar, vivir), U7 (12 lemas, sobre todo reflexivos -ar: afeitarse, bañarse, ducharse, lavarse, levantarse, llamar, llegar, quedarse, recorrer, comer, hablar, dar).
2. **Uso del imperativo — instrucciones y peticiones** — principal U6. Aplicación atestada en U7 con 7 lemas en imperativo (dar, decir, hablar, hacer, ir, salir, venir) en instrucciones de actividades.
3. **Irregularidad vocálica o→ue (presente)** — principal U7. Caso límite en U8: `doler` con rasgo mixto "tipo gustar + o→ue".

**Lo que ya se hizo en v12.1:** se aplicó solo el caso "limpio" (Irreg. e→ie U6 recurrente con `cerrar` — testigo nuevo del paradigma post-U4). Los 3 grupos de arriba quedaron fuera para no convertir el lote de reconciliación en una segunda campaña.

**Lo que hay que decidir:**

- **(a) Criterio de inclusión** — ¿cuándo una aplicación post-principal entra como `recurrente`? Tres opciones extremas: (i) **siempre** que haya forma atestada que aplique el paradigma; (ii) **solo** cuando hay sistematización o cuadro nuevo que invoca el paradigma; (iii) **selectiva** según peso pedagógico (criterio editorial). La convención actual para categorías léxicas tiende a (iii) — `recurrente` exige material no incidental + valor pedagógico, no toda aparición.
- **(b) Tratamiento de casos mixtos** — `doler` (tipo gustar + o→ue): ¿se considera recurrente de o→ue (igual que `cerrar` es testigo de e→ie en U6), o se excluye por rasgo no puro (igual que `venir` quedó fuera de e→ie por su rasgo mixto "1ª persona + e→ie")? Coherencia con la decisión tomada para `venir`.
- **(c) Granularidad de items** — si entran, ¿items = todas las formas atestadas del lema en presente?, ¿solo las paradigmáticas?, ¿se replica la convención de cada principal?
- **(d) Trazabilidad cross-unidad** — ¿basta con que el dashboard de fase 2 muestre la trayectoria vía hilo (un evento por unidad atestada), o también hay que duplicarla como `recurrente` en cada inventario? El plano fase 2 ya conecta los puntos; añadir recurrentes en fase 1 es duplicación con coste editorial alto.

**Alcance estimado si se decide (a)/(c) inclusivas:** 4 entradas recurrentes nuevas en U4, U5, U6 y U7 para el paradigma regular del presente + 1 entrada recurrente en U7 para Uso del imperativo + 1 entrada (posiblemente) en U8 para o→ue. **6 inventarios afectados.** Trabajo editorial considerable.

**Bloquea a:** nada urgente — la deuda no impide ningún cierre actual. Es asunto de **completitud editorial** del modelo y **coherencia visual** del dashboard.

**Para resolver:** sesión de criterio editorial con el autor; cerrar (a)-(d) como entradas en `reglas-operativas.md` (fase 1) o `reglas-reciclaje.md` (si se decide que es asunto de fase 2). Tras cierre, ejecución mecánica al patrón v12.1.

---

## Bloque C — Diseñar y construir las fases 2-8

### Plantilla de pasos por cada fase

Para cada fase nueva (2 a 8), repetir la secuencia C.X.1 → C.X.6:

#### C.X.1 Cerrar criterios de la fase con el autor (chat)
**Pre-condición:** fase anterior cerrada (o decisión explícita de paralelismo).
**Archivos:** ninguno físico todavía.
**Meta:** `PROCESO-MAESTRO.md` — añadir decisiones a Parte 4.
**Gate:** autor confirma criterios cerrados.

#### C.X.2 Crear prompt versionado de la fase
**Archivos a crear:**
- `fases/<N>-<nombre>/prompt.md`
- *Opcional:* `fases/<N>-<nombre-fase>/CLAUDE.md` (CLAUDE.md específico que se carga al trabajar dentro de esa carpeta).

**Meta:**
- `CLAUDE.md` (raíz) — actualizar tabla de las 8 fases (Estado: 🛠 En construcción → ✅ Operativa cuando se cierre).
- `PROCESO-MAESTRO.md` — bitácora.
- `REVIEW.md` — gate.
- `CHANGELOG.md` — commit del prompt nuevo.

**Gate:** prompt completo, autor lo aprueba.

#### C.X.3 Implementar scripts auxiliares (si aplica)
**Archivos a crear:** según necesidad (`scripts/<accion>.py`).
**Meta:** `CLAUDE.md` (comandos), `CHANGELOG.md`, `REVIEW.md`.
**Gate:** scripts ejecutables, idempotentes, documentados.

#### C.X.4 Probar la fase con U3
**Archivos a crear:** outputs de la fase (CSVs, MDs en `unidades/U3/<seccion>/`).
**Meta:** `CHANGELOG.md`, `REVIEW.md`.
**Gate:** output validado por autor.

#### C.X.5 Iterar prompt según errores
**Archivos a modificar:** `fases/<N>-<nombre>/prompt.md`.
**Meta:** `CHANGELOG.md`, `REVIEW.md`.
**Gate:** sin errores nuevos en una segunda pasada.

#### C.X.6 Cierre de la fase
**Meta:**
- `CLAUDE.md` — fase marcada ✅ Operativa en la tabla, prompt en lista de "documentos relacionados".
- `PROCESO-MAESTRO.md` — bitácora final.
- `REVIEW.md` — bloque C.X marcado ✅, siguiente fase desbloqueada.
- `CHANGELOG.md` — commit "vX.X: fase <N> operativa".
- `README.md` — si cambia estructura visible.

**Gate:** TODOS los anteriores cumplidos.

### Orden recomendado de las fases

| Orden | Fase | Justificación |
|---|---|---|
| 1.º | Fase 2 — Análisis de vocabulario | Más simple, alimenta fase 3. |
| 2.º | Fase 3 — Tarjetas de vocabulario | Mucho material en `viejo/` aprovechable. |
| 3.º | Fase 5 — Píldoras formativas | Protocolo en `viejo/agentes/ag-vocabulario.md` y `ag-gramatica.md`. |
| 4.º | Fase 4 — Tarjetas de estrategia | Requiere ≥2 unidades para validar "no repetir". |
| 5.º | Fase 6 — Generación sección por sección | Cuando las anteriores estén estables. |
| 6.º | Fase 7 — Doble versión | Consume fase 6. |
| 7.º | Fase 8 — Principios + repertorios | Apoyo transversal, puede empezarse en paralelo. |

---

## Bloque D — Sistema de lecciones de Claude Code

### D1. Diseñar el formato y la activación de las lecciones
**Objetivo:** decidir cómo se registran y cargan los aprendizajes que evitan errores recurrentes.

**Pre-condición:** ninguna.

**Archivos a decidir (con autor):**
- ¿Un único archivo `lecciones-claude.md` en raíz, o uno por fase?
- ¿Cómo se activa? ¿Referenciado desde CLAUDE.md raíz, o auto-cargado?
- ¿Formato: cronológico, por sección, por tipo de error?

**Meta:** `PROCESO-MAESTRO.md` decisión cerrada en Parte 4.
**Gate:** decisión documentada.

### D2. Crear el contenedor y la primera lección
**Pre-condición:** D1 cerrado.

**Archivos a crear:** según D1 (ej. `lecciones-claude.md`).

**Primera lección a registrar:** "El contenido de cada actividad debe aparecer en `datos.items_libro` (o equivalente) **exactamente como en el libro**, no como referencia ni respuesta. Detectada el 2026-05-05 al revisar U3."

**Meta:**
- `CLAUDE.md` — añadir referencia a lecciones.
- `PROCESO-MAESTRO.md` — bitácora.
- `REVIEW.md` — gate.
- `CHANGELOG.md` — commit.

**Gate:**
1. ✅ Archivo creado con primera lección.
2. ✅ CLAUDE.md actualizado.
3. ✅ Commit + CHANGELOG.

---

## Bloque E — Limpieza final (al cerrar todo el curso)

**Pre-condición global:** todas las fases ✅ operativas, U1-U9 extraídas, JSONs globales completos, dashboard estable.

### E1. Decidir qué se conserva de `viejo/`
**Archivos:** revisar `viejo/` carpeta por carpeta.
**Meta:** `PROCESO-MAESTRO.md` con la decisión carpeta por carpeta.
**Gate:** lista cerrada de "qué se migra antes de borrar".

### E2. Migrar lo aprovechable
**Pre-condición:** E1 cerrado.
**Meta:** `CHANGELOG.md` con cada movimiento, `CLAUDE.md` actualizado.
**Gate:** todo lo de la lista E1 migrado y verificado.

### E3. Eliminar `viejo/`
**Pre-condición:** E2 cerrado + autor da OK explícito.
**Archivos a eliminar:** `viejo/` completo.
**Meta:** `CLAUDE.md`, `README.md`, `PROCESO-MAESTRO.md`, `.gitignore` — eliminar todas las referencias. (`.dockerignore` ya no aplica: eliminado con el stack de despliegue en v11.21.)
**Gate:** sin referencias residuales en código ni docs.

### E4. Eliminar `PROCESO-MAESTRO.md` y `REVIEW.md`
**Pre-condición:** todo el contenido valioso integrado en `CLAUDE.md`, `README.md`, prompts.
**Meta:** `CLAUDE.md` ya integrado lo esencial; `CHANGELOG.md` registra la eliminación.
**Gate:** autor confirma que no se pierde información operativa.

### E5. Reescribir `README.md` y `CHANGELOG.md` con el estado final
**Meta:** documentación final coherente.
**Gate:** README explica el sistema de un solo vistazo, CHANGELOG cierra la migración.

---

## Tabla maestra de archivos por categoría

### Archivos del sistema (raíz, en uso)
| Archivo | Estado | Próxima modificación esperada |
|---|---|---|
| `CLAUDE.md` | ✅ Activo, conciso (Anthropic best practices) | Cada cierre de fase, cada cambio en estructura/comandos/reglas |
| `PROCESO-MAESTRO.md` | ✅ Activo | Cada decisión nueva o cierre de pendiente |
| `REVIEW.md` | ✅ Activo | Continuamente: cada paso completado, nuevos pasos detectados |
| `README.md` | ✅ Actualizado | Cuando cambia estructura raíz, comandos, instalación |
| `CHANGELOG.md` | ✅ Activo | En cada commit relevante (siempre que haya cambio físico) |
| `.gitignore` | ✅ Actualizado | Cuando cambien rutas trackeables |

### Código activo (raíz)
| Archivo | Estado | Próxima modificación esperada |
|---|---|---|
| `diagrama.py` | ✅ Activo — dashboard local (`python3 diagrama.py`) | B4 (vista globales), nuevos endpoints si surgen |
| `web/index.html` | ✅ Vista Inventarios funcional | B4, mejoras estéticas |
| `eval/` | ⚠ Heredado | Bloque C cuando aplique |
| `fases/1-extraccion-inventario/prompt.md` | ✅ Operativo | A1 (con cada error nuevo en U3), A2 (con casos nuevos en otras unidades) |
| `scripts/validar_inventario.py` | ✅ Operativo | Cuando se añada un caso de validación |
| `scripts/regenerar_tarjetas_globales.py` | 📋 No existe | B1 |
| `scripts/regenerar_pildoras_globales.py` | 📋 No existe | B1 |

### Contenido editorial (raíz)
| Carpeta/Archivo | Estado | Próxima modificación |
|---|---|---|
| `unidades/U0/U0-nc1-inventario.json` | ✅ Validado · trackeado · unidad atípica (1 aviso intencional por `_nota_unidad_atipica`) | Ninguna prevista |
| `unidades/U1/U1-nc1-inventario.json` | ✅ Validado · trackeado | Ninguna prevista |
| `unidades/U2/U2-nc1-inventario.json` | ✅ Validado · trackeado (integrado en v10.87) | Ninguna prevista |
| `unidades/U3/U3-nc1-inventario.json` | ✅ Validado · trackeado (reintegrado en v10.91 con PDF correcto, 47 actividades, 32-41) | Ninguna prevista |
| `unidades/U3/fuente/U3-nc1.pdf` | ✅ PDF correcto (gitignored) | — |
| `unidades/U3/tarjetas/`, `pildoras/`, MDs | 📋 No migrados | B3 |
| `unidades/U4/U4-nc1-inventario.json` | ✅ Validado · trackeado (integrado v10.93, 49 actividades, 6 cuadros, 42-51) | Ninguna prevista |
| `unidades/U5/`...`U9/` | 📋 Carpetas vacías, sin inventario | Nuevas extracciones (U5 → ejecutor 2 en `extract/U5`) |
| `unidades/nc1-tarjetas.json` | 📋 No existe | B2 |
| `unidades/nc1-pildoras.json` | 📋 No existe | B2 |
| `unidades/nc1-reciclaje.json` | ✅ Poblado pase 1 (v10.94): 23 hilos / 70 eventos · U0-U9 · modelo de hilos · 4 tipos | B2a.2 — pase 2 contra inventarios |

### Archivo (`viejo/`, intocable hasta E)
| Carpeta | Estado |
|---|---|
| `viejo/CLAUDE-anterior.md` | Conservado |
| `viejo/unidades/U03/` | Contenido editorial original |
| `viejo/scripts/` | Scripts CrewAI v5 antiguos |
| `viejo/agentes/`, `viejo/repertorios/`, `viejo/referencias/`, `viejo/diseno/`, `viejo/_template/`, `viejo/materiales/`, `viejo/material-complementario/`, `viejo/marco-teorico-metodologico.md`, `viejo/00-curso-general.md` | Material editorial y de referencia |

---

## Cómo el revisor analiza este documento

En cada iteración:

1. Identifica el paso actual (el primero ⚠ o 📋 en el orden lógico).
2. Verifica que el ejecutor ha tocado los **archivos involucrados** que el paso lista.
3. Verifica que las **actualizaciones meta** (CLAUDE.md, PROCESO-MAESTRO, REVIEW, CHANGELOG, README, gitignore) se han hecho según corresponda.
4. Recorre el **gate de cierre** del paso, condición por condición.
5. Si TODAS las condiciones se cumplen: marca el paso como ✅ y libera el siguiente.
6. Si alguna falla: bloquea el paso, lista qué falta, devuelve al ejecutor.
7. Si en el proceso aparece un caso nuevo (bug, decisión inesperada): añade un paso nuevo en el bloque correspondiente.

---

## Bitácora de actualizaciones del REVIEW

- **2026-05-23** — Abierta deuda **B.D1 · Política general de recurrentes post-principal de paradigmas abstractos**. Derivada del scope B de v12.1: la pasada de coherencia detectó 3 grupos sin cobertura en `gramatica_consolidada` (paradigma regular del presente como recurrente en U4-U7, Uso del imperativo recurrente en U7, o→ue recurrente en U8 con caso mixto `doler`). v12.1 dejó solo el caso limpio (Irreg. e→ie U6 recurrente con `cerrar`). La política general sobre cuándo una aplicación post-principal entra como `recurrente` para paradigmas verbales abstractos no está cerrada — convención editorial tradicional aplica criterio (iii) selectivo, pero no está explicitada para este grupo de categorías. Ver sección `Deuda abierta — B.D1` arriba con preguntas (a)-(d) a cerrar. **Alcance estimado** si se decide inclusivo: 6 inventarios afectados (U4, U5, U6, U7, U8 + posibles U9). No bloquea cierres actuales — es completitud editorial.

- **2026-05-23** — Schema fase 1: campo `portada` opcional (v11.88). Captura la columna de apertura de cada unidad — número + título + fotos temáticas, siempre en la 1.ª página de Vocabulario, a la izquierda. Hoy el inventario no lo recogía: las claves de página son solo `pagina/seccion/actividades/cuadros`, e `imagen.descripcion` solo existe a nivel de actividad. La portada es maquetación de portada — ni actividad ni cuadro — por eso campo top-level, no parte de `paginas_detalle`. Shape: `{pagina: int, descripcion: str (verbatim del libro)}`. Campo opcional para no romper U0-U9 (validador 0/0/0 tras el cambio). `schema-inventario.md` §1 recuento actualizado (4 opcionales canónicas en lugar de 3; corregida inconsistencia preexistente del header que decía "13 obligatorias" cuando L54 ya decía 12 tras eliminación de `contenidos_indice` en v11.67) + nueva §1.1 con shape y semántica. `validar_inventario.py`: `portada` añadida a `CLAVES_TOP_OPCIONALES` + chequeo de shape (si presente: ambas subclaves obligatorias, `pagina` int, `descripcion` str no vacío). Próximo (paso a paso, no lote masivo, acordado con autor): rellenar `portada` unidad por unidad leyendo el PDF de cada una, empezando por U0.
- **2026-05-23** — Dashboard: drawer lateral → modal centrado (v11.85). Refactor pendiente desde el rediseño (§4.4: "se abre como modal a página completa, no una columna lateral"). El drawer lateral de 480px que implementamos en v11.70 era una solución rápida pero insuficiente para alojar explicaciones largas y para el modal a página completa que pedirá `nivel: detalle` cuando Capa 2 empiece a producirlo. Sustituido por modal centrado sobre overlay semi-transparente (rgba 0.55), ancho `min(760px, 94vw)`, altura `max 88vh`, scroll interno, bordes redondeados, sombra elevada. Click fuera (overlay) sigue cerrando. El id `rec-drawer` se conserva por compatibilidad con el JS — solo cambia el posicionamiento CSS y la sombra. Lógica de render y datos sin tocar. Sintaxis JS validada con `node --check`. Cuando Capa 2 empiece a generar `nivel: detalle`, el modal puede expandirse a página completa o duplicarse en una variante "fullscreen" — la infraestructura ya está preparada.
- **2026-05-23** — Capa 2 sobre U1 — etiquetas + propuestas; explicaciones declaradas pendientes (v11.84). Segunda sesión de Capa 2 IA. Aplicado a los 21 eventos de U1 en `unidades/nc1-reciclaje.json`: 9 `introduce` en los hilos canónicos de U1; 1 `amplia` (Números cardinales — U0 introdujo 0-10, U1 amplía 1-20); 1 `aplica` (Para la clase, recurrencia); 4 `anticipacion` (Adjetivos de nacionalidad → U2, Interrogativos → U3, Signos de puntuación → U7, Sonidos y correspondencias cross-unidad); 2 `[introduce, sistematiza]` (Artículos determinados — cuadro@p14#2 paradigma completo el/la/los/las; verbal · ser — cuadros p14#1, p15, p20#1, p20#3 sistematizan); 2 `[introduce]` verbal (llamarse, tener); 1 `[sistematiza]` (`voc · Saludos y despedidas` U1); 1 evento sin etiqueta (`voc · Gentilicios` U1 — limbo). Dos correcciones in-sesión a partir de feedback editorial del autor: (1) `voc · Saludos y despedidas` U1 pasa de `[introduce]` a `[sistematiza]` — si U0 tiene `[anticipacion]` el siguiente evento del mismo hilo no puede ser `introduce` (la "primera aparición canónica" no encaja si ya hubo contacto anticipado); lo coherente es `sistematiza` (recoge lo activado en U0 y añade despedidas + registro formal/informal). (2) `voc · Gentilicios` U1 pasa de `[aplica]` a `etiquetas: []` — `aplica` exige contenido previo introducido, y Gentilicios no tiene canónico; queda en limbo editorial hasta que se resuelva la propuesta de alias. **Procedencia verbal decidida por Capa 2** para los 3 lemas (ser/llamarse/tener) → `reconciliado · "indice:Verbos ser, llamarse y tener (formas singulares)"`. **2 propuestas previas de U0 resueltas (rechazadas con resolución)**: `prop-abecedario-cobertura-cross-unidad` (fase 1 separó correctamente Abecedario vs Nombres de las letras); `prop-saludos-y-despedidas-vocab-vs-cultura` (cultura fuera del scope; se queda como vocabulario). **1 propuesta nueva abierta**: `prop-gentilicios-alias-adjetivos-nacionalidad` (`reconciliacion`) — diferida a U2. **Pendiente declarado**: las explicaciones de los cuadros U1 (10 candidatas) NO se redactaron en este lote — el autor pidió más rigor en el criterio editorial (las explicaciones deben ser flujos didácticos detallados, no descripciones de actividades; han de aplicarse a cada etiqueta incluyendo verbos con su contexto de tiempos y formas) y el modal centrado del dashboard primero para verlas en contexto. Observación al margen: las `formas` de `verb · llamarse` U1 incluyen el typo `"Me lllamo"` heredado del inventario fase 1.
- **2026-05-23** — Rectificación cobertura U7 campo "Animales domésticos y salvajes" (v11.83). Detectado por el autor: el CSV de tarjetas (`unidades/U07-propuesta/tarjetas/csv/animales.csv`, repo B) tenía 14 animales, alineado con `items[]` del inventario. Pero la nota `D-Animales-amplios` del propio inventario (v10.161) declaraba 18 lemas como ampliación del campo desde los textos de Destrezas (p78-p79) y solo se habían materializado 3 (orangután, yegua, potro); los 15 restantes faltaban en `items[]`. Inconsistencia interna de la extracción de fase 1, no del CSV. Aplicado: (a) `items[]` de "Animales domésticos y salvajes" en `U7-nc1-inventario.json` pasa de 14 a 29 (+mamífero, anfibio, reptil, ave, ciervo, cebra, rana, tortuga, cocodrilo, víbora, pitón, boa, paloma, loro, cacatúa, en singular canónico — forma de diccionario aunque el libro los presente en plural); `fuentes` ampliadas con `p78-act1`, `p78-act2`; nota `D-Animales-amplios` reformulada como registro de la rectificación. Validador U7 → 0/0/0. (b) CSV de tarjetas regenerado a 29 filas, mismo formato (9 traducciones, 3 combos, sílaba tónica, gramapop). Decisión del autor: las 4 categorías taxonómicas (mamífero, anfibio, reptil, ave) sí entran como items del mismo campo (no campo aparte) y se tarjetizan. Caso atípico `ave`: artículo `el` por 'a-' tónica inicial pero noun femenino — gramapop lo refleja. **Reapertura puntual de fase 1 en alcance U7** (cerrada en v10.164); rectificación de cobertura por nota mal materializada, no cambio de contrato. Memoria `project_fase1_cerrada` sigue vigente.
- **2026-05-23** — Sincronización **automática** de `_meta` del canónico (v11.82). Tras v11.81 (sync manual del campo `_meta.version` a v11.81), el autor pidió que la actualización fuera automática para no depender de memoria humana. Implementado: (1) `scripts/sync_meta_reciclaje.py` — helper idempotente que lee `CHANGELOG.md` (máximo `vX.Y`) y la fecha de hoy, escribe `_meta.version` y `_meta.fecha` solo si cambian; (2) `scripts/hooks/pre-commit` — hook git que detecta si `unidades/nc1-reciclaje.json` está staged en el commit y, si lo está, ejecuta el sync y vuelve a hacer stage del JSON; si no, no hace nada. Instalación: `git config core.hooksPath scripts/hooks` (una sola vez). Documentado en `fases/2-reciclaje/CLAUDE.md`. Desde ahora cualquier commit que incluya cambios al canónico (regeneración de Capa 1, escritura directa de Capa 2, ediciones manuales) lleva `_meta` actualizado automáticamente — el dashboard refleja la versión real sin intervención.
- **2026-05-23** — Sync `_meta.version` del canónico a v11.81 (v11.81). Higiene tras v11.80: el campo `_meta.version` lo escribe el generador de Capa 1 cuando regenera, pero los enriquecimientos de Capa 2 (etiquetas, explicaciones, propuestas) se escriben directamente al JSON sin pasar por el generador. Consecuencia: el dashboard llevaba mostrando `v11.77` en la cabecera (último valor que escribió el generador en v11.76) cuando el contenido ya estaba en v11.80. Sincronizado manualmente a v11.81 — el dashboard ahora muestra la versión real del archivo. Convención implícita: bumpear `_meta.version` siempre que se toque el JSON aunque no se regenere (apunte para futuras sesiones de Capa 2).
- **2026-05-23** — **Capa 2 sobre U0 cerrada** — primera unidad enriquecida (v11.80). Primera sesión real del procedimiento §12 sobre U0 ("Punto de partida", atípica). Sirvió de shakedown del flujo end-to-end: `Capa 1 mecánica` → `sesión IA enriquecedora` → `merge no destructivo`. Cambios persistidos en `unidades/nc1-reciclaje.json`: (1) **etiquetas editoriales** en los 6 eventos U0 — `voc · Abecedario español`, `voc · Países que hablan español`, `voc · Números cardinales`, `voc · Para la clase` con `[introduce]`; `voc · Saludos y despedidas` y `pron · Sonidos y correspondencias ortográficas` con `[anticipacion]`. Corrección en esta sesión: la propuesta inicial de marcar `[introduce, sistematiza]` en Abecedario y Países se retiró — `sistematiza` exige contenido "ya activo" (reglas §3) y U0 es primera aparición canónica; ambas pasan a `[introduce]` puro. (2) **Explicación** escrita en `voc · Saludos y despedidas` U0 desde el cuadro@p11 (Buenos días / Buenas tardes / Buenas noches): `que_dice_el_libro` + `fuente: cuadro@p11` + `analisis_ia` que sitúa el evento en el arco U0→U1 (U0 abre las fórmulas pragmáticas mínimas sin verbos ni pronombres; U1 cultura ampliará con "Hola" informal y la distinción tú/usted). (3) **Dos propuestas editoriales abiertas** en `propuestas[]`, ambas con estado `pendiente` y diferidas explícitamente a U1: `prop-abecedario-cobertura-cross-unidad` (tipo `siempre_presente`) cuestiona si el hilo del abecedario debería recoger eventos en U1+ cuando el inventario alfabético reaparece transversalmente, o si esas reapariciones quedan adecuadamente cubiertas por otros hilos (sonidos específicos, letras homófonas); `prop-saludos-y-despedidas-vocab-vs-cultura` (tipo `reconciliacion`) detecta la tensión entre fase 1 (categorizó como `vocabulario`) y el curso (declara como `cultura` en U1) — el contenido es funcionalmente pragmático, decisión editorial sobre reclasificar o aceptar la simplificación léxica queda para revisar al procesar U1. Validador estructural sin errores. U0 cumple §13 (a) chequeo estructural + (c) revisión editorial; el cross-unidad (b) sigue con sus 14 alertas previas, ninguna de las cuales afecta a U0 directamente; las propuestas que afectan a U0 están explícitamente diferidas. **Primera unidad enriquecida del curso** — el procedimiento de Capa 2 funciona como contrato (§12).
- **2026-05-23** — Dashboard drawer: `reconciliado_con` visible como texto persistente (v11.78). Micro-UX detectado tras v11.77: el `reconciliado_con` completo solo era visible al hacer hover sobre el chip de procedencia (en el `title`), invisible en móvil/touch y poco escaneable. v11.78 lo muestra **persistente** en la fila del evento del drawer, en gris claro itálico precedido por flecha (`→ indice:Números 0-10`, `→ pcic:A1`). Se conserva el `title` del chip con `procedencia · reconciliado_con` como hover-detail adicional. Solo `web/index.html`; sintaxis JS validada con `node --check`.
- **2026-05-23** — Dashboard: leyenda de procedencia con 4 chips (v11.77). Follow-up no bloqueante de v11.76: cuando `reconciliado_con` pasó a llevar prefijo (`indice:` o `pcic:`), el rótulo único `[E] reconciliado según el PCIC` se volvió impreciso para 4 de los 48 reconciliados (los `indice:`). v11.77 divide la categoría en la vista (no en el JSON): la leyenda y el drawer muestran ahora 4 chips — `I` contenido del índice / `E` equivalente del índice / `P` reconciliado según el PCIC / `F` fuera del índice. El JSON conserva 3 valores técnicos; la sub-división vive solo en `REC_PROCEDENCIA_CATS` de `web/index.html`. Solo dashboard tocado; sintaxis JS validada con `node --check`. Reparto visible: I=78 · E=4 · P=44 · F=3 · sin asignar=154 (verbal).
- **2026-05-23** — Modelo: triage identitario **mecanizado vía registries** + **merge no destructivo** (v11.76). Iteración sobre v11.75 tras revisión del autor: la cautela "Capa 1 sin aliases para nada" era sobrecorrección — `campos-semanticos-canonicos.json` ya trae `origen` + `aliases_indice`, y `gramatica-canonica.json` / `pronunciacion-ortografia-canonica.json` / `perifrasis-canonicas.json` traen `_pcic_ref`. Toda esa información es respaldo estructurado curado por fase 1 y se puede usar mecánicamente para resolver `reconciliado` sin tocar al juicio editorial. **Cambios:** (a) **Modelo (§9 REDISEÑO + §4 reglas + glosario + prompt fase 2 + schema §3)**: el triage es íntegramente Capa 1 mecánico para vocabulario, gramática, pron/orto y perífrasis; el bloque **verbal** queda como excepción explícita porque `verbos-canonicos.json` no expone respaldo estructurado equivalente (decisión heredada de fase 1 — el análisis verbal es competencia de fase 2); Capa 2 deja de tener procedencia en su núcleo de tareas, solo la toca en verbal o cuando hay que corregir un caso mal clasificado mecánicamente. **`reconciliado_con` amplía semántica** con prefijos `indice:<entrada del curso>` (alias del índice) o `pcic:<ref>` (`pcic:A1` como fallback). (b) **Generador (`scripts/generar_reciclaje_capa1.py`)**: `IndiceCurso` preserva las entradas literales del índice; `Registries` expone los registries crudos; nuevo `resolver_procedencia()` mecaniza el triage. **Merge no destructivo**: la regeneración fusiona con el archivo previo — Capa 1 sobreescribe campos mecánicos (`procedencia_indice`, `reconciliado_con`, `evidencias`, `formas`); **preserva** los interpretativos (`etiquetas`, `explicacion`, `detalle`, `propuestas[]`). En el bloque `verbal`, donde `procedencia_indice` / `reconciliado_con` los escribe Capa 2 (no Capa 1), también se preservan; el detector de pérdidas los reconoce como enriquecimiento. **Abort por defecto ante pérdida de enriquecimiento editorial** (cautela explícita del autor); flag `--permitir-perdidas` opt-in vuelca el detalle a `docs/historico/`. Test verificado: inyectando `procedencia_indice` + `reconciliado_con` en un evento verbal y regenerando, los tres campos sobreviven al merge. `validar_capa1()` relajada para aceptar `reconciliado` + prefijo obligatorio en `reconciliado_con`; `validar_reciclaje.py` (estructural) también valida el prefijo. (c) **Reparto nuevo**: 78 `declarado` · 48 `reconciliado` (44 con prefijo `pcic:`, 4 con `indice:`) · 3 `nuevo` · 154 sin asignar (verbal, los 52 hilos × ~3 eventos). Sin regresión en validador cross-unidad (14 alertas R1/R4 idénticas). **Verificado**: las 6 etiquetas de Capa 2 que escribimos en U0 sobreviven al merge (0 pérdidas). Caso `Números cardinales` U0 pasa de "sin asignar" (v11.75) a `reconciliado` con `reconciliado_con: "indice:Números 0-10"` — exactamente lo que el autor pedía. Caso `Sonidos y correspondencias` U0 pasa a `reconciliado` con `pcic:<ref del registry>`. **Nota UX pendiente**: la leyenda del dashboard `[E] reconciliado según el PCIC` es ligeramente imprecisa ahora que algunos reconciliados llevan prefijo `indice:`. El drawer ya muestra el `reconciliado_con` completo. Se evalúa una iteración v11.77 si la imprecisión molesta.
- **2026-05-23** — Modelo: `procedencia_indice` pasa a eje identitario puro, curso-wide, sin aliases mecánicos (v11.75). Detectado en la sesión de Capa 2 sobre U0: "Saludos y despedidas" en U0 salía como `nuevo` (= "fuera del índice" en dashboard) **aunque el curso lo declara para U1 cultura**. El modelo viejo (§9.1 previo) **mezclaba dos ejes**: pertenencia al índice del curso y temporalidad respecto de la unidad canónica. Eso produce mentiras editoriales: algo del índice, anticipado en una unidad anterior, aparece como "fuera del índice". **Cambio de modelo (Nivel 1/2):** `procedencia_indice` se redefine como respuesta **solo al eje identitario** — ¿está el contenido del hilo en el índice del curso, en cualquier unidad? La temporalidad la lleva **enteramente la etiqueta** (`anticipacion`/`aplica`/sin etiqueta temporal). Tres salidas: **`declarado`** = título canónico coincide literal con una entrada del índice del curso (curso-wide, sin aliases — precomputable por Capa 1); **`reconciliado`** = alias/equivalencia PCIC (decisión de Capa 2, propuesta + cierre humano); **`nuevo`** = no aparece en el índice en ninguna unidad ni es reconciliable. **Cautela explícita del autor en la decisión:** la Capa 1 **no** usa aliases del registry para inflar `declarado` — si lo hiciera, re-mezclaría ejes por la puerta de atrás. Aliases siempre son territorio de Capa 2. Reformulados §9 del REDISEÑO (definiciones nuevas + ejemplo "Marcadores temporales del pasado U3" corregido a `declarado + anticipacion`), §4 y §14 R1 de reglas-reciclaje (R1 ahora alimenta sobre todo la etiqueta `anticipacion`; `procedencia_indice` es estable por hilo), glosario raíz. Refactor de `scripts/generar_reciclaje_capa1.py`: `IndiceCurso` simplificado a un único conjunto de slugs curso-wide cubriendo los 8 campos de contenido del curso (incluye `cultura`, `comunicacion`, `destrezas`, `para_aprender`, no solo los 3 lingüísticos del rediseño — porque el eje identitario abarca el índice completo); `declarado()` toma solo `titulo`, sin aliases; `add_evento_mapa` decide procedencia con la misma regla (no la fuerza por venir del paso mapa). Canónico regenerado: **100 eventos `declarado`** (antes 41), 183 sin asignar (Capa 2 los triará). Validadores estructural y cross-unidad sin regresión (14 alertas R1/R4 idénticas — son de datos, no del modelo). Caso verificable: `voc-saludos-y-despedidas` ahora `declarado` en todos sus eventos (incluyendo U0), como debe ser.
- **2026-05-23** — Revisión de las 14 alertas R1/R4 del validador cross-unidad sobre el canónico v11.68 (v11.74). Diagnóstico cerrado: ninguna de las 14 es bug de fase 1. **12 son insumo normal del pipeline** y no requieren intervención antes del arranque de Capa 2 — las 6 R1 (anticipación material) son la señal correcta del modelo y alimentarán la etiqueta `anticipacion` cuando Capa 2 procese U1-U6; 6 de las 8 R4 son léxico PCIC incidental coherente con la unidad (`recurrente` de fondo, comportamiento esperado §3.6 — `Centros e instituciones educativas` U2/U3, `Cine y teatro` U3, `Televisión y radio` U3, `Restaurante` U4). **Dos sospechosas anotadas como `propuestas` a abrir en la sesión de Capa 2** sobre la unidad correspondiente, para no depender de memoria de sesión:
    - **`voc · Gentilicios` en U1** — recurrente en U1 y U4 (3 items totales en todo el curso), nunca principal; registry `origen=excepcion`. Sospecha de **solape/alias con `Adjetivos de nacionalidad`** (principal en U2, 17 items). Gentilicios (sustantivo: *un español*) y adjetivos de nacionalidad (adjetivo: *es español*) coinciden formalmente en español — probable que sean el mismo contenido bajo dos nombres. Propuesta tipo `reconciliacion` a abrir cuando Capa 2 procese U1.
    - **`voc · Bebida` en U4** — recurrente en U4 (9 items), nunca principal. Tensión real entre el **índice del curso** (vocab principal U4 = "Alimentos, Comidas preparadas", sin "Bebida") y el **contenido efectivo** de la unidad (título "Comidas y bebidas", 9 items reales del léxico de bebidas). Propuesta editorial a abrir cuando Capa 2 procese U4: ¿elevar a principal, tratar como alias de "Alimentos", o resolver por otra vía? Afecta al índice del curso (`nc1-curso.json`), no solo al inventario.

  Sin tocar datos ni registries en este lote — solo registro de la revisión y de las dos propuestas pendientes.
- **2026-05-23** — Dashboard RECICLAJE: dos leyendas (etiqueta + procedencia del índice) con rótulos editoriales (v11.72). Itera sobre v11.71 a partir de feedback del autor sobre la vista real: el modelo del evento tiene **dos ejes ortogonales** (`etiquetas` y `procedencia_indice`, `REDISEÑO-EN-CURSO.md §9.5`) y el dashboard solo mostraba uno; además, los valores técnicos `declarado`/`reconciliado` eran opacos al editor. Cambios solo en `web/index.html`: (1) quitadas las dos notas verbosas de arriba (nivel + estado de etiquetas); (2) sustituidas por **dos leyendas compactas** una por eje, con **diferenciación no cromática** entre ejes: etiqueta sigue como círculo de color, procedencia pasa a **cuadrito gris oscuro con inicial mnemotécnica** (`I` contenido del índice · `E` reconciliado según el PCIC · `F` fuera del índice) — los dos ejes hablan visualmente lenguajes distintos para que ningún chip de un eje se pueda confundir con uno del otro; cada entrada de la leyenda muestra rótulo + `(n)` del reparto real, los valores sin presencia se atenúan; (3) **rótulos editoriales** para `procedencia_indice`: `declarado` → "contenido del índice", `reconciliado` → "reconciliado según el PCIC", `nuevo` → "fuera del índice" — el JSON conserva los valores técnicos del schema, el mapeo a rótulos vive solo en la vista (drawer incluido). Glosario raíz (`glosario.md` Bloque 2) gana entrada nueva `procedencia_indice` con tabla técnico↔editorial y significado de cada valor — para que el editor pueda consultar la equivalencia sin abrir `reglas-reciclaje.md §9`. Reparto que muestra hoy el dashboard: procedencia "contenido del índice" 41/283 (verde activo) · "reconciliado según el PCIC" 0 (atenuado) · "fuera del índice" 0 (atenuado) · sin asignar 242; etiquetas 0/283 (las 7 atenuadas) + 283 sin etiquetar.
- **2026-05-23** — Dashboard RECICLAJE: corrección UX (v11.71). Feedback del autor mirando la vista real tras v11.70: (1) el badge `AUTO` aparecía con fondo lila intenso en las 118 filas con el mismo valor — ruido visual sin información que discrimine; (2) la leyenda "Etiqueta del evento (Capa 2)" mostraba siete chips de color (introduce/amplía/aplica/sistematiza/contrasta/anticipación/discrimina) que **no aparecen** en los puntos del timeline (todos grises hoy porque la Capa 2 no ha corrido) — la leyenda prometía una semántica que no se cumplía en el render; (3) `auto` no se explicaba en el dashboard, requería conocer `REDISEÑO §4.2`. Tres correcciones, solo en `web/index.html`: badge por fila reemplazado por **marcador pequeño sin fondo** que aparece **solo si el hilo se desvía del nivel dominante** (hoy 0 marcadores, porque todos son `auto`); nueva **nota global** arriba de la vista — "118/118 hilos en `auto`. Esqueleto poblado desde inventarios; la Capa 2 IA aún no ha generado hilos en `detalle`."; leyenda de etiquetas sustituida por una **nota honesta** del estado-esqueleto, que enumera las 7 etiquetas por nombre y se compromete a mostrar los colores cuando existan eventos etiquetados (la nota se reconfigura automáticamente). Sintaxis JS verificada con `node --check`. Sin tocar datos, lógica, ni `diagrama.py`.
- **2026-05-22** — Dashboard: vista RECICLAJE adaptada al shape del rediseño (v11.70). La vista RECICLAJE de `web/index.html` se construyó para el shape pre-rediseño de `nc1-reciclaje.json` (`h.tipo`, `ev.accion`/`impacto`/`descripcion`/`seccion`, `h.usos`, `recData.actualizado`) y quedó **rota** tras la regeneración del canónico en v11.68 — un consumidor del archivo que la validación de v11.68 (validadores de JSON) no cubrió; lo detectó el autor al preguntar por el dashboard. Rewire al modelo nuevo, con criterio de visualización aprobado antes de tocar código: agrupación por `bloque` (5 secciones, orden vocabulario→perífrasis); cada hilo con badge de `nivel_analisis`; timeline de eventos por unidad; `etiquetas[]` como chips múltiples; `procedencia_indice` como badge secundario; `evidencias[]` como contenido del evento; drawer del evento con `evidencias`/`formas`/`tiempo` completas; cabecera de la vista con `_meta.fecha`/`estado`; `propuestas[]` en bloque separado al final con estado vacío sobrio. Criterio de diseño respetado: el estado-esqueleto de Capa 1 (todos los eventos con `etiquetas: []`, porque la Capa 2 no ha corrido) se representa como **estado válido** — dot gris, leyenda "sin etiquetar (Capa 1)" — sin inventar sustitutos para los campos retirados (`impacto`/`descripcion`/`seccion`/`usos`) ni fingir semántica de Capa 2. Solo `web/index.html` (−119 líneas netas; el modelo nuevo es más plano — desaparecen los `usos`/`tipos_verbo` anidados y su drawer de verbo). `diagrama.py` no se tocó: `/api/reciclaje` ya sirve el archivo. Sintaxis del bloque JS verificada con `node --check`; render simulado contra el canónico real (118 hilos / 283 eventos / 0 propuestas, 5 bloques) sin campos faltantes.
- **2026-05-22** — Fase 2 REACTIVADA — lote documental (v11.69). Se levanta la pausa de la decisión 36. Fundamento: el Nivel 4 del rediseño cerró su parte de herramienta en v11.62-v11.68 (Capa 1 `auto`+`mapa`, validador estructural §13a, validador cross-unidad R1-R5 §13b, `nc1-reciclaje.json` regenerado al shape del rediseño) — la pausa, que era "hasta cerrar el canon / rediseñar fase 2", deja de tener objeto. Lote **documental, sin lógica nueva**: banners de estado de `CLAUDE.md`/`prompt.md` de fase 2 pasan a REACTIVADA; `prompt.md` y `reglas-reciclaje.md` §13-§14 cablean los comandos reales (`generar_reciclaje_capa1.py`, `validar_reciclaje.py`, `validar_cross_unidad.py`) en lugar de "Nivel 4 pendiente"; `scripts/integrar_unidad.py` retira el flag `--regenerar-reciclaje` y la lógica de cuarentena de `regenerar_reciclaje_vocabulario.py` — el reciclaje lo gestiona el pipeline de fase 2, no la integración del inventario (`REDISEÑO-EN-CURSO.md` §13.4); sincronizados `CLAUDE.md` raíz (comando de `integrar_unidad.py`), `README.md` (tabla de fases + estado actual), `PROCESO-MAESTRO.md` (estructura, decisión 23/36, bitácora) y este REVIEW (estado global Bloque B). **Cautela registrada en los contratos:** la Capa 2 (sesión IA enriquecedora, §12) tiene contrato cerrado pero **nunca se ha ejecutado**; la primera unidad procesada es también su shakedown — no se da por rodada. **Pieza siguiente** (fuera de v11.69, decisión de prioridad del autor): la primera ejecución editorial de la Capa 2 sobre una unidad.
- **2026-05-22** — Fase 2 Nivel 4: regeneración del canónico `nc1-reciclaje.json` (v11.68). Se ejecuta el generador de Capa 1 (v11.62-v11.63) sin `--dry-run` sobre el canónico: `nc1-reciclaje.json` pasa del shape pre-rediseño v10.114 (181 hilos, claves `curso/_acciones_validas/_tipos_validos/...`) al shape del rediseño (`_meta/hilos/propuestas` — 118 hilos, 283 eventos, `propuestas[]` vacío). Validación inmediata: validador estructural (`validar_reciclaje.py`) 0 errores contra `schema-reciclaje.md`; validador cross-unidad R1-R5 — pre-condiciones R2/R5 OK, 14 alertas (R1×6, R4×8) **idénticas a las de v11.65**, lo que confirma que son de datos y no regresión de la generación. Con esto la **Capa 1 del pipeline de fase 2 queda materializada de extremo a extremo** (generador `auto`+`mapa` + ambos validadores del gate + canónico regenerado y validado). **Fase 2 sigue PAUSADA**: la regeneración produce el esqueleto mecánico, no reactiva la fase. Pendientes para cerrar fase 2: la **Capa 2** (sesión IA enriquecedora — etiquetas, triage `reconciliado`/`nuevo`, `explicacion`, `detalle`; diseño en `REDISEÑO-EN-CURSO.md` §12) y la **reactivación operativa** (levantar la pausa de decisión 36, actualizar el banner del `CLAUDE.md` de fase 2 y el flujo de `integrar_unidad.py`).
- **2026-05-22** — Eliminado `contenidos_indice` del inventario canónico (v11.67). Cierre de la pieza que abrió v11.66. `contenidos_indice` era un campo top-level de cada inventario que copiaba el índice editorial de la unidad — duplicación pura de lo que ya vive en `nc1-curso.json`. Investigado con git: la copia existía desde ~2026-05-05 (texto abreviado), `nc1-curso.json` se creó después (2026-05-08) más fiel al libro, y la copia nunca se re-sincronizó — deuda técnica conocida desde B1.4 (registrada en `PROCESO-MAESTRO.md` y en el `_nota` de `nc1-curso.json`). Decisión (autor + revisor): no regenerar la copia sino **eliminarla** — el índice del curso tiene fuente única en `nc1-curso.json`, conforme a la regla de oro 4; mantener la copia obligaba a re-sincronizarla para siempre. Alcance del lote (1 commit atómico, ~21 archivos): campo retirado de los 10 inventarios U0-U9; `validar_inventario.py` deja de exigirlo (fuera de `CLAVES_TOP` + chequeo de presencia retirado); `verificar_integridad.py` chequeo 4 — la comparación retirada temporalmente en v11.66 pasa a definitiva; contratos de fase 1 actualizados (`schema-inventario.md` 13→12 claves obligatorias, `glosario.md`, `reglas-operativas.md`, `PROCESO-MAESTRO.md`); el dashboard conserva el bloque "Índice de contenidos" pero leyéndolo de `nc1-curso.json` — `get_inventario` adjunta `_indice_curso` (`diagrama.py`) y `web/index.html` lo renderiza. Validación tras el lote: U0-U9 a 0/0/0; `verificar_integridad.py` 9/9 chequeos, 0 errores; `validar_cross_unidad.py` pre-condiciones OK, sin regresión. Resuelve además la contradicción interna de fase 1 entre el glosario ("deben coincidir exactamente") y `PROCESO-MAESTRO.md` ("pueden divergir legítimamente"). No queda ninguna referencia viva a `contenidos_indice` que exija el campo.
- **2026-05-22** — Fase 1: fix del chequeo 4 de `verificar_integridad.py` (v11.66). Comparador desactualizado detectado al implementar el validador cross-unidad (v11.65) — daba 55 falsos positivos. Dos causas: (a) `nivel` se comparaba por unidad, pero es un campo **global** de `nc1-curso.json` (corregido: compara contra el global; los valores coinciden, `'A1.1'`); (b) `contenidos_indice` se comparaba contra los campos de `nc1-curso.json` siendo artefactos divergentes — verificado con git: `contenidos_indice` de los inventarios existe desde ~2026-05-05 (texto concatenado, copia abreviada del índice del libro), `nc1-curso.json` se creó 3 días después, el 2026-05-08, como índice fiel al libro (listas, texto completo de comunicación, subsecciones pron/para-aprender separadas) y no se ha reeditado desde entonces. No hubo "reedición" de `nc1-curso.json`: son dos copias del índice hechas en momentos distintos, y la segunda es la buena. La comparación de `contenidos_indice` se **retira temporalmente** del chequeo 4 (con comentario explicativo en el código); tras el fix `verificar_integridad.py` pasa entero (0/0). **Pieza pendiente** (fase 1, aparte): regenerar `contenidos_indice` de los 10 inventarios desde `nc1-curso.json` (la fuente canónica que el glosario ya designa) y reactivar el check — incluye decidir si `contenidos_indice` pasa a listas (cambio de schema) o sigue texto comparado contra `join(lista)`. No se relaja el contrato del glosario: se alinea el dato viejo con la fuente canónica.
- **2026-05-22** — Fase 2 Nivel 4: validador cross-unidad R1-R5 (v11.65). Componente (b) del gate de cierre de fase 2 (`reglas-reciclaje.md` §13-§14). Nuevo `scripts/validar_cross_unidad.py`. **R2 y R5** (pre-condiciones que abortan; un fallo es bug de fase 1) se **delegan** a `verificar_integridad.py` — sus chequeos 1/2/3/5 cubren materialidad y coherencia bidireccional; no se re-implementa lo que fase 1 ya valida (regla de oro 4). **R1, R3, R4** (alertas que entran en el criterio de cierre §13) se calculan sobre los inventarios. **R1** en versión proxy determinista, decidida con el autor el 2026-05-22: en vez de análisis de frecuencia sobre el texto crudo de las actividades (ruidoso — stopwords, morfología, instrucciones), detecta solo lo trazable de fase 1 — categoría `recurrente` en U(n) cuyo `principal` aparece por primera vez en una unidad posterior (anticipación material). No cubre léxico que nunca llegó al consolidado; ese R1 con frecuencia queda como posible carril experimental futuro fuera del gate. **R3**: categoría consolidada ubicada en la dimensión equivocada respecto a los registries. **R4**: `recurrente` que nunca es `principal` en ninguna unidad + lemas verbales colocados en `vocabulario_consolidado`. Ejecución actual: pre-condiciones R2/R5 OK; 14 alertas (R1×6, R4×8, R3×0) — observaciones cross-unidad defendibles que el validador per-unidad de fase 1 no puede detectar; quedan para que el autor las resuelva cuando se cierre cada unidad. **Hallazgo informativo** (reportado, no abortante, sin tocar): `verificar_integridad.py` chequeo 4 (coincidencia cabecera↔`nc1-curso.json`) falla con 55 errores — **no es drift de datos de los inventarios** (`validar_inventario.py` da 0/0/0): el chequeo 4 quedó desactualizado respecto al shape actual de `nc1-curso.json` — compara `nivel` por unidad cuando hoy es un campo global, y `contenidos_indice` (texto concatenado en el inventario) contra los campos del curso (listas). Falsos positivos del comparador, ajenos a R2/R5. Arreglo trivial (chequeo 4 de `verificar_integridad.py`), pendiente como pieza aparte de fase 1 para no mezclarlo con este lote. Con esto el gate §13 está completo en código (validador estructural (a) en v11.64 + cross-unidad (b) ahora). Pendiente del Nivel 4: regeneración real del canónico `nc1-reciclaje.json` y reactivación de la fase.
- **2026-05-22** — Fase 2 Nivel 4: validador estructural como script (v11.64). Componente (a) del gate de cierre de fase 2 (`reglas-reciclaje.md` §13). Nuevo `scripts/validar_reciclaje.py`: chequeo estructural de `nc1-reciclaje.json` contra `schema-reciclaje.md` — claves, tipos, enumeraciones; valida el archivo en cualquier estadio del pipeline (salida de Capa 1 o enriquecida por Capa 2, con `etiquetas`, triage `reconciliado`/`nuevo`, `explicacion`, `detalle`). No comprueba invariantes propias de Capa 1 (eso lo hace el generador) ni la calidad cross-unidad R1-R5 (validador aparte, §14). Refactor de `generar_reciclaje_capa1.py` para evitar duplicar el chequeo de schema (regla de oro 4 — fuente única): importa `validar_schema` del nuevo script y conserva solo `validar_capa1` con las invariantes §11.4 específicas de su salida (no asigna etiquetas, no escribe `reconciliado`/`nuevo`, no fabrica `explicacion`/`detalle`). Pruebas: dry-run del generador sin regresión (118 hilos / 283 eventos / OK); validador en seco sobre salida fresca de Capa 1 → 0 errores; test sintético confirma que acepta contenido estilo Capa 2 y detecta errores deliberados (tiempo en bloque equivocado, etiqueta inválida, `reconciliado_con` huérfano). El canónico `nc1-reciclaje.json` sigue intacto — en shape pre-rediseño, por tanto no conforme al validar, lo esperado hasta que el Nivel 4 lo regenere. Pendiente del Nivel 4: validador cross-unidad R1-R5 como script, regeneración real del canónico, reactivación.
- **2026-05-22** — Fase 2 Nivel 4: proyección `mapa` de la Capa 1 (v11.63). Cierra la laguna declarada en v11.62. `scripts/generar_reciclaje_capa1.py` añade la proyección de nivel `mapa` (`REDISEÑO` §4.2/§4.5): siembra hilos/eventos desde el índice de `nc1-curso.json` con resolución **conservadora** — una entrada del índice se resuelve a su título canónico de registry solo si la coincidencia es inequívoca (§11.4 invariantes 2-3: la Capa 1 no inventa ni adivina); las entradas que no resuelven (lemas verbales embebidos en el campo `gramatica` del índice, divergencias de naming índice↔registry, pron a nivel de subcategoría) se reportan como avisos en stdout, sin generar hilo. `nivel_analisis` deja de estar hardcodeado: `finalizar_niveles()` lo fija según el grado de población — `auto` si el hilo tiene algún evento respaldado por inventario, `mapa` si solo está declarado en el índice. Dry-run: 118 hilos (auto 118 / mapa 0 — el curso está íntegramente cubierto por inventarios, así que ningún hilo queda solo-`mapa`) / 283 eventos / 41 eventos con `procedencia_indice: declarado` confirmado vía el resolver / 25 avisos / validación OK; 0 regresiones en los spot checks de v11.62. El canónico `nc1-reciclaje.json` sigue sin reescribirse. Con esto la Capa 1 cubre sus dos proyecciones (`mapa` + `auto`). Pendiente del Nivel 4: validadores estructural y cross-unidad R1-R5 como script, regeneración real del canónico, reactivación.
- **2026-05-22** — Fase 2 Nivel 4: generador de Capa 1 validado en dry-run (v11.62). Primer paso del Nivel 4 (implementación en código) — **checkpoint de herramienta validada, no reactivación de fase 2**. Nuevo `scripts/generar_reciclaje_capa1.py`: generador de la Capa 1 en modo íntegro. Produce la proyección mecánica de nivel **`auto`** de `nc1-reciclaje.json` (hilos de los 5 bloques desde los inventarios, eventos, `evidencias`, `formas` verbales por tiempo, `procedencia_indice: declarado`), preserva `propuestas[]`, y valida la salida contra `schema-reciclaje.md` + las 10 invariantes §11.4 antes de escribir. Dry-run: 118 hilos / 283 eventos / 0 descartados / validación OK; 7 spot checks semánticos OK (`_grupo` solo en gramática, `formas` por tiempo, perífrasis desde actividad, `procedencia_indice` solo `declarado`, `etiquetas` siempre `[]`, sin `explicacion`/`detalle`, archivo viejo no contamina salvo `propuestas[]`). **Laguna conocida:** la proyección de nivel **`mapa`** desde `nc1-curso.json` (§11, `REDISEÑO` §4.2) aún no se materializa — el índice del curso se usa solo para el triage `declarado` y todos los hilos salen `auto`; documentada en el docstring del script y en el CHANGELOG. **El canónico `nc1-reciclaje.json` NO se reescribe** — esa es la decisión operativa de reactivar fase 2, que sigue PAUSADA y pendiente. Nota empírica diferida añadida a `REDISEÑO-EN-CURSO.md` §7.4 (volcado literal de `formas_trabajadas` → posibles duplicados por mayúscula/minúscula). Pendiente del Nivel 4: proyección `mapa`, validadores estructural y cross-unidad R1-R5 como script, regeneración real del canónico, reactivación.
- **2026-05-20** — Fix de consistencia en `fases/1-extraccion-inventario/CLAUDE.md` (v11.30). La tabla "Para qué consultar qué archivo" citaba "(regla crítica 6)" al describir que la política de naming canónico aplica a los 4 registries, pero esa es la regla crítica **7** ("Canon canónico literal es autoridad de naming"); la 6 es "Documentación de fase 1 obligatoria". Corregida la referencia cruzada. Sin cambios funcionales. (Nota: el fix llegó vía un chat con contexto de versión desactualizado — hablaba de "v10.144" y "gap 9 de fase 2"; el cambio en sí es válido y se integró, pero no existe ningún commit v10.144 pendiente: el proyecto va por v11.30).
- **2026-05-20** — Residuos `ROADMAP.md`/`GITHUB-MANIFEST.md` en REVIEW y PROCESO-MAESTRO (v11.29). Micro-lote de cierre tras v11.27 (hallazgo del revisor). Quedaban dos rastros de los archivos retirados: la tabla maestra de archivos del sistema en este REVIEW los listaba como filas "⚠ Heredado, sin tocar"; el árbol de estructura de raíz en PROCESO-MAESTRO los mostraba como parte de la estructura actual. `GITHUB-MANIFEST.md` ya no existe (eliminado v11.27) y `ROADMAP.md` nunca existió — ambas referencias retiradas de las dos ubicaciones. Cierra el carril de limpieza de raíz (v11.15-v11.29).
- **2026-05-20** — Corrección del nombre de repo B en la autoridad documental (v11.28). CLAUDE.md, README.md y PROCESO-MAESTRO.md nombraban repo B como `guia-sistema-trabajo` — el nombre planeado en la migración v11.13 — pero el otro chat lo renombró a `temporal-antiguo-guia-ia` al ejecutar v11.14. La autoridad documental apuntaba a una carpeta inexistente. Análisis profundo de CLAUDE.md (a petición del autor): además del nombre, se planteó si había deriva conceptual — CLAUDE.md modela repo B como "sistema de trabajo vivo donde sucede la redacción", mientras el nombre `temporal-antiguo` sugería un archivo. El autor confirmó **Caso 1**: repo B es el sistema de trabajo vivo; el "Modelo de dos repos A/B" y el "Flujo de publicación canónica" siguen conceptualmente válidos, solo el nombre estaba mal. Corregidas 7 referencias activas (CLAUDE.md ×4: estructura del modelo, flujo de publicación, regla de oro 5, "Lo que NO se hace"; README.md ×2; PROCESO-MAESTRO.md ×1: nota de migración). Bitácora histórica de REVIEW (entrada v11.13) intacta — en su momento el nombre planeado sí era `guia-sistema-trabajo`. Hallazgo menor corregido en la misma pasada: el comentario de `integrar_unidad.py` en Comandos básicos de CLAUDE.md decía "desde su worktree" (el flujo de extracción cambió a directa desde U5) y "flag --regenerar-reciclaje para forzar" (ese flag quedó en cuarentena en v11.19); actualizado.
- **2026-05-20** — Eliminado `GITHUB-MANIFEST.md` obsoleto (v11.27). `GITHUB-MANIFEST.md` (118 líneas, gitignorado, nunca versionado) era el manifiesto de qué archivos se subían a GitHub para desplegar el sistema de agentes CrewAI en Railway — fechado 2026-03-16. Todo lo que listaba ya no aplica: `scripts/crewai/`, `scripts/importar_inventario.py`, `probar_modelos.py` (migrados a repo B), `eval/` (movido a repo B en v11.17), `Dockerfile`/`railway.toml` (eliminados en v11.21), `datos/inventarios/U03-inventario.json` (ruta vieja — los inventarios viven en `unidades/UX/`). Mismo perfil que `.github/copilot-instructions.md` (borrado en v11.16): documento muerto que desinformaba. Eliminado. `.gitignore`: la sección "Archivos de planificación local" tenía `ROADMAP.md` (inexistente) y `GITHUB-MANIFEST.md` como ignores — retiradas ambas líneas muertas; `.github/` se conserva ignorado (sin workflows versionados por ahora).
- **2026-05-20** — Borrado de backups muertos `.bak.v10.150` (v11.26). Efecto colateral de v11.25 detectado por el revisor: al sustituir `*.bak.v10.145`/`*.bak.v10.150` por el genérico `*.bak`, los 6 archivos `unidades/U{1,2,3,7,8,9}/U*-nc1-inventario.json.bak.v10.150` dejaron de estar ignorados (el patrón `*.bak` no casa con nombres que terminan en `.v10.150`). Son backups de la migración de saneamiento v10.150, cerrada; los inventarios están en v11.x y validan 0/0/0. Eliminados — no se re-ignoran (es cruft muerto, no algo a conservar). El patrón `*.bak` se mantiene para backups futuros.
- **2026-05-20** — Limpieza `.gitignore` + coherencia de estructura (v11.25). `.gitignore`: retirados `*.bak.v10.145` y `*.bak.v10.150` (patrones de backup de migraciones cerradas, sin archivos que cubrir) → sustituidos por un genérico `*.bak` que cubre cualquier backup futuro. Coherencia documental: `ROADMAP.md` y `GITHUB-MANIFEST.md` estaban gitignorados como "planificación local" pero `README.md` y `CLAUDE.md` los listaban en la estructura del repo como si fueran archivos del proyecto — doble verdad. `ROADMAP.md` ni siquiera existe; `GITHUB-MANIFEST.md` existe (6.8K) pero no está integrado en la autoridad documental ni en el flujo vigente. Versionarlo abriría una decisión nueva (para qué sirve, quién lo mantiene); en su lugar se retiran ambos de la estructura en README/CLAUDE — coherente con que ya están gitignorados. Los patrones binarios/diseño del `.gitignore` (`*.indd`, `*.psd`, `*.ai`, `*.aux`, etc.) se dejan intactos: ignores defensivos baratos contra commits accidentales de binarios pesados.
- **2026-05-20** — Adelgazado de `.env.example` para repo A (v11.24). `.env.example` pasó de ~70 a ~24 líneas. Diagnóstico: repo A es dashboard local + validador — no usa agentes en su flujo vigente, no necesita claves de API, ninguna variable es obligatoria (el dashboard arranca sin `.env`). Verificado contra el uso real de `os.environ`/`getenv` en `diagrama.py` y `scripts/`. Retirado del ejemplo: API keys (ANTHROPIC/GROQ/DEEPSEEK — del sistema de agentes, pertenecen al `.env.example` de repo B), bloque Crew Recurvo (RECURVO_LLM_*), bloque DeepEval, `PORT=8080` (puerto viejo pre-v10.127 + Railway retirado). Conservado como opcional: `PORT`/`HOST`/`EXTRA_UNIDADES_PATHS`/`DEBUG` (servidor del dashboard) y un bloque final comentado `DATABASE_URL`/`LANGFUSE_*` — superficies de BD y trazas heredadas del sistema de agentes, fuera del flujo vigente de repo A pero aún leídas por el código de `diagrama.py` (`os.environ` líneas 154 y 161). Ajuste de redacción del revisor incorporado: "no usa agentes en su flujo vigente" (no "no ejecuta agentes" — la superficie de lanzamiento de agentes sigue existiendo heredada y dormida en `diagrama.py`/`index.html`). El `.env` real con claves queda intacto y gitignorado. Las API keys retiradas deben vivir en el `.env.example` de repo B (trabajo del otro chat).
- **2026-05-20** — Residuo `.dockerignore` en el paso E3 de REVIEW (v11.23). Hallazgo del revisor tras v11.22: el paso futuro E3 ("Eliminar `viejo/`") tenía una Meta-lista de archivos a limpiar que aún nombraba `.dockerignore` — archivo inexistente desde v11.21. Es un rastro en el plan vivo, no en bitácora. Añadida nota inline: "`.dockerignore` ya no aplica: eliminado con el stack de despliegue en v11.21". Último rastro de `.dockerignore` cerrado.
- **2026-05-20** — Residuo `.dockerignore` en PROCESO-MAESTRO (v11.22). Hallazgo del revisor tras v11.21: `PROCESO-MAESTRO.md:255` aún mencionaba `.dockerignore` en una lista de tarea no marcada como superada. `.dockerignore` ya no existe en repo A. Añadida nota inline "tarea histórica ya superada; `.dockerignore` eliminado en v11.21". Cierra el último rastro del stack de despliegue.
- **2026-05-20** — Retirada del stack de despliegue Docker/Railway (v11.21). Repo A ya no se despliega en la nube — el dashboard es herramienta local (`python3 diagrama.py`). Eliminados `Dockerfile`, `.dockerignore`, `railway.toml`. Razón (acordada con autor): el despliegue tenía sentido cuando se compartía el dashboard con el equipo editorial y los módulos de agentes vivían en este repo; tras la migración a dos repos (los agentes están en repo B) y la decisión de no desplegar repo A, el stack quedó sin uso — y `.dockerignore` arrastraba además 7 líneas `viejo/` muertas. Ni se mueve a repo B (es archival, no se despliega) ni se conserva: se elimina; el `Dockerfile` es trivial de recrear si algún día se necesita. `requirements.txt` se mantiene (lo necesita el `.venv` local). Referencias actualizadas: `CLAUDE.md` (estructura), `README.md` (estructura + fila del stack: "Ejecución — local, sin despliegue"), `PROCESO-MAESTRO.md` (estructura), `REVIEW.md` (estado global de Infraestructura, sección B5 marcada ⊘ SUPERADO con banner explicativo, tabla de meta-actualización, tablas de archivos/código). Hallazgo del revisor incorporado: el lote incluyó REVIEW.md (no solo CLAUDE/README/PROCESO-MAESTRO) y se ajustó la frase de README que decía "Railway disponible si se reactiva" — habría quedado contradiciendo la retirada. Bitácora histórica de CHANGELOG/REVIEW intacta (registro de cuando B5 estuvo activo).
- **2026-05-20** — Renombrado de scripts: `sanear_inventario.py` y `matcher.py` (v11.20). Lote 3 (naming) y cierre de la revisión de `scripts/`. `cleanup_v150.py` → `sanear_inventario.py`: el `v150` sugería un one-shot pero es la herramienta de saneamiento §5.10/§5.11 activa del flujo de fase 1. `migrate_at_r_v10145.py` → `matcher.py`: su nombre era de la CLI de migración @R v10.145 (one-shot cumplida), pero su valor vivo es la librería matcher (expand_needle, match_substring, norm, gather_text, etc.) que importa `sanear_inventario.py`. Renombrado limpio con `git mv` (no alias transitorio — evita doble mantenimiento) + actualización exhaustiva de referencias activas: `CLAUDE.md` (Comandos básicos), `prompt.md` (paso 5 del flujo), `validar_inventario.py` (2 comentarios), `schema-inventario.md` (§A.3), el `import` de `sanear_inventario.py` y los docstrings de ambos archivos (con nota "Antes `X`"). El histórico de CHANGELOG/REVIEW no se reescribe — los nombres viejos eran correctos en su época. Verificado: `import matcher` desde `sanear_inventario.py` OK, `sanear_inventario.py --unit 0` corre, validador U0 → 0/0/0. **Revisión de `scripts/` completa**: Lote 1 (archivar one-shot real), Lote 2 (cuarentena del roto), Lote 3 (naming) — los 3 cerrados.
- **2026-05-20** — Cuarentena de `regenerar_reciclaje_vocabulario.py` (v11.19). Lote 2 de la revisión de `scripts/` (dictamen del revisor). El script está roto: asume SHAPE v10.114 (pre-rediseño de fase 1 — `campo_semantico`, 3 sub-bloques, enfoque `fonetica`); los inventarios actuales son post-v10.153. No se archiva porque sigue colgado del flag opcional `--regenerar-reciclaje` de `integrar_unidad.py`. Cambios: (1) función `_cuarentena()` al inicio de `main()` — fail-fast: imprime mensaje explícito de bloqueo y `sys.exit(2)` salvo override de mantenimiento `RECICLAJE_VOCAB_OVERRIDE=1`; (2) `integrar_unidad.py` ya no dispara el script roto cuando se pasa `--regenerar-reciclaje` — imprime aviso de cuarentena y completa la integración del inventario sin abortar ni hacer rollback (la integración ya estaba validada; no se cae por un script obsoleto); el commit pasa a ser solo el inventario (antes añadía `nc1-reciclaje.json` que ya no se regenera). `regenerar_reciclaje_mapa.py` se deja intacto — sigue operativo (lee `nc1-curso.json`, sin cambio de shape), pausa técnica conocida de fase 2. Pendiente: Lote 3 (naming — `cleanup_v150.py` y `migrate_at_r_v10145.py`, este último realmente el módulo matcher).
- **2026-05-20** — Archivado del one-shot `inicializar_canon_semantico.py` (v11.18). Lote 1 de la revisión de `scripts/` (dictamen del revisor: 3 lotes separados). `inicializar_canon_semantico.py` — one-off que pobló el canon semántico (`campos-semanticos-canonicos.json`); cumplido su cometido, el canon se mantiene a mano desde entonces — movido a `docs/historico/scripts-one-shot/` (con README que explica la carpeta). Referencias activas corregidas **antes** de mover: `validar_inventario.py` (el mensaje de ayuda del error "canon ausente" citaba el script; ahora dice "el canon está versionado en git, restaurar el archivo") y `PROCESO-MAESTRO.md` (nota de archivado). La entrada de bitácora del 2026-05-11 que lo menciona se deja intacta (registro histórico). Hallazgo que corrigió el dictamen: `migrate_at_r_v10145.py` NO se archiva pese al nombre one-shot — `cleanup_v150.py:27` lo importa como módulo matcher (8 funciones); archivarlo rompería una herramienta activa. Su renombrado (refleja mal su rol real) queda para un lote posterior de naming. Validador U0 → 0/0/0 tras el movimiento.
- **2026-05-20** — `eval/` movido a `temporal-antiguo-guia-ia` (v11.17). `eval/` (3 archivos, 28K — `promptfoo.yaml`, `evaluar_tarjetas.py`, `provider_crewai.py`) era tooling de evaluación del sistema CrewAI/Recurvo, no infraestructura viva de este repo. Estaba roto desde v11.14: `provider_crewai.py` hace `sys.path.insert` a `scripts/crewai/`, que se migró a `temporal-antiguo-guia-ia` con el borrado de `viejo/`. Copiado a `temporal-antiguo-guia-ia` (commit `cf5da5e`, pusheado) y eliminado de aquí con `git rm`. `CLAUDE.md` y `README.md`: retirada la referencia a `eval/` de la estructura del repositorio (estaba listado como infraestructura, quedó desactualizado). Fase 3 (tarjetas), cuando se construya, tendrá evaluación propia si la necesita; no depende de este `eval/` atado a Recurvo.
- **2026-05-20** — Higiene `.gitignore` + limpieza de carpetas sueltas (v11.16). Pasada de revisión de carpetas de raíz. `.gitignore`: añadidos `.venv/` (654M, virtualenv regenerable que estaba untracked sin ignorar — riesgo de commit accidental de 654M) y `.deepeval/` (carpeta que el framework DeepEval recrea sola). Eliminadas carpetas/archivos sin valor: `.deepeval/` (vacía), `.github/copilot-instructions.md` (obsoleto — describía un workspace antiguo con rutas inexistentes y era para GitHub Copilot, no Claude Code; la autoridad del proyecto es CLAUDE.md), `.claude/claude-code-chat-images/` (26 screenshots viejos de chats, 6.1M, sin valor de proyecto). `.venv/` se mantiene en disco (virtualenv activo necesario para ejecutar el proyecto).
- **2026-05-20** — Limpieza de `.claude/` tras la migración (v11.15). Cierre del último fleco de la migración a dos repos. `.claude/` de repo A se reduce a la config de infraestructura/inventarios: `commands/check-fase1.md` + `settings.json`. Retirados de repo A los archivos de trabajo editorial y del sistema CrewAI antiguo — verificados previamente idénticos (mismo contenido; repo B tiene las rutas `viejo/`→`unidades/` adaptadas, correcto): `rules/agent-prompt-design.md`, `rules/tool-design.md`, `rules/criterios-generacion-texto.md`, `agents/auditor-seccion.md`, `skills/auditar-seccion/SKILL.md`, `commands/audit.md`. Borrado el `.bak` huérfano `criterios-generacion-texto.md.bak.pre-saneamiento`. `.gitignore`: la línea `.claude/` (que ignoraba la carpeta entera) se sustituye por ignores selectivos (`settings.local.json`, `claude-code-chat-images/`) — ahora la config del proyecto (`settings.json` + `commands/check-fase1.md`) se versiona. Esos 6 archivos ya viven en repo B `temporal-antiguo-guia-ia`. Migración a dos repos completamente cerrada.
- **2026-05-20** — Migración a dos repos · Mitad 2 paso 2: borrado físico de `viejo/` (v11.14). `viejo/` eliminado de repo A (disco + índice git): 8 archivos tracked + el resto ignorado. Copia íntegra verificada por `diff -rq` (sin diferencias) en repo B `temporal-antiguo-guia-ia` — local `~/Desktop/temporal-antiguo-guia-ia`, GitHub privado `adminmc2/temporal-antiguo-guia-ia`, commits `8164c2f` (copia inicial) + `6cdf342` (sync de 3 archivos modificados tras la copia). `.gitignore`: retirado el bloque "Contenido editorial" completo (~15 líneas `viejo/...`) + la línea de PDFs `viejo/unidades/**/fuente/`. Hook `.git/hooks/pre-commit` (bloqueaba commits con rutas `viejo/`) retirado — sin objeto al desaparecer `viejo/`. Repo A queda sin `viejo/`; el sistema de trabajo es ya exclusivamente repo B. Migración a dos repos cerrada. Pendiente menor: archivo `.psd` de 57 MB en repo B supera el recomendado de GitHub (50 MB) — candidato a Git LFS o gitignore en pasada futura; script de publicación B→A (§6.8) sigue pospuesto.
- **2026-05-20** — Migración a dos repos · Mitad 2 paso 1: coherencia documental (v11.13). Repo A queda autocoherente sin `viejo/` como fuente de trabajo (aunque `viejo/` sigue físicamente una iteración más; borrado en v11.14). CLAUDE.md: nuevo bloque "Modelo de dos repos A/B", flujo de publicación reapuntado a repo B (`guia-sistema-trabajo`), reglas de oro y "Lo que NO se hace" sin "editar en viejo", `viejo/` fuera de la estructura del repo. README.md: sección "Modelo de dos repositorios" sustituye a "Sobre el sistema anterior". diagrama.py: dict `AGENTS` (código muerto que apuntaba a `viejo/repertorios/`) retirado; comentario documenta la feature "repertorios por sección" como diferida (reintroducir desde repo B vía env var `GUIA_TRABAJO_REPO`). PROCESO-MAESTRO.md: nota de migración global — sus ~82 referencias `viejo/...` son punteros históricos a material que ahora vive en repo B; reescritura completa pendiente de pasada posterior dedicada (acordado con el autor). Criterio de cierre cumplido: ningún documento activo presenta `viejo/` como zona de trabajo vigente. Decisiones excluidas de este commit: hook `pre-commit` (config local de git, no versionada — fuera del contrato del review) y script de publicación B→A (§6.8, opcional, pospuesto). Próximo: v11.14 borrado físico de `viejo/` + ajuste `.gitignore`.
- **2026-05-20** — Versionar mirrors editoriales en repo A (v11.12). Las propuestas publicadas (`unidades/U{0,1,2,4,5}/propuesta/*.md`) y recursos (`recursos/tarjetas/*.csv`) estaban untracked — fuera de git pese a ser el entregable de repo A. Commiteados: 37 archivos, 6362 líneas. Preludio de la Mitad 2 de la migración a dos repos (A entregable / B sistema de trabajo); contexto en `CONTEXTO-MIGRACION-MITAD-2.md`.
- **2026-05-20** — Compactación de CHANGELOG (v11.11). `CHANGELOG.md` de 4252 a ~250 líneas. Bloque v10.40–v10.164 (cierre fase 1 U0-U9 + trabajo paralelo fase 2) movido a `docs/historico/CHANGELOG-fase1-cierre-y-fase2-paralela.md` con cabecera fuerte: declara que es la fuente documental de lo hecho en ambas fases. Header del activo gana índice de históricos por rango (pre-v10.40 / v10.40-v10.164 / v11.0+). Corte validado por contenido. Sin tocar README/CLAUDE/REVIEW (cuerpo).
- **2026-05-20** — Jerarquía de autoridad documental + regla editorial (v11.10). Cierre del problema de crecimiento perpetuo de CHANGELOG/REVIEW. Sin rearchivar: cabeceras de ambos archivos declaran su rol (registro/estado, no autoridad) + regla editorial de entradas cortas; CLAUDE.md § Documentos clave gana columna ¿Autoridad? y bloque de regla editorial; README añade línea de navegación al histórico. Autoridad operativa = CLAUDE.md.
- **2026-05-20** — Dashboard Inventarios: tarjetas con A+B+C+D (v11.9). Las cards de la vista Inventarios ampliadas para mostrar contexto operativo y editorial de cada unidad de un vistazo. Bloque A — Stats del inventario: `📋 N actividades · N cuadros · N decisiones` (lectura directa del JSON). Bloque B — Consolidados resumidos: recuento de categorías P/R en las 4 dimensiones (Léxico, Gramática, Pron/Orto, Verbos). Bloque C — Propuesta editorial mini-grid: 7 chips compactos `Voc ✓ · Gram ⏳ · Com · · ...` reutilizando el scanner ya existente de fase 1; cada chip con tooltip del estado y paleta coherente con la grid "Estado de unidades" del proyecto. Bloque D — Keywords del inventario: hasta 5 chips con canónicos léxicos y gramaticales principales (ej. U6: Establecimientos · Marcadores de lugar · Profesiones · Imperativo (tú)). Cambios: `diagrama.py` `_scan_zona` añade 4 campos al payload por unidad (`stats`, `consolidados`, `keywords`, `propuesta`); reutiliza `scan_section` sin duplicar lógica. `web/index.html` `loadInventarios`: card más ancha (260→340px minmax) y renderiza los 4 bloques compactamente; archivo path con ellipsis. Higiene commit: `diagrama.py` + `web/index.html` + meta docs.
- **2026-05-20** — Sync de puerto 8080→8081 fuera del subsistema dashboard (v11.8). Hallazgo del revisor tras v11.7: aunque el subsistema dashboard quedó sincronizado, seguían 4 referencias activas a 8080 fuera de ese subsistema, mientras `diagrama.py` PORT = 8081 desde v10.127. Cambios: `README.md:80` (comando de arranque), `Dockerfile:16` (`EXPOSE 8081`, antes 8080 no coincidía con el puerto al que liga el server), `fases/1-extraccion-inventario/CLAUDE.md:38` (comando de validación visual), `fases/2-reciclaje/CLAUDE.md:54` (comando de revisión de timeline). Verificación: `grep -rn "8080"` en `*.md` / `*.py` / `Dockerfile` (excluyendo `viejo/`, `docs/historico/`, y entradas históricas de CHANGELOG/REVIEW) → 0 coincidencias activas. Higiene commit: `README.md` + `Dockerfile` + 2 × CLAUDE.md de fase + meta docs.
- **2026-05-19** — Cleanup-sync de 3 drifts menores tras auditoría v11.6 (v11.7). Auditoría posterior a v11.6 (revisor) detectó tres drifts del mismo subsistema: (1) clave huérfana `"reflexion": "Reflexión"` en `SECTION_LABELS` de `diagrama.py` — residuo inerte tras retirada de columna Reflexión en v11.1, no afectaba runtime pero acumulaba mini-deuda; retirada. (2) Puerto del dashboard inconsistente en `CLAUDE.md`: `:88` decía `8080` y `:153` decía `8081`; unificado a `8081` (PORT real en `diagrama.py:29`); también corregida docstring de `diagrama.py:5`. (3) Estructura del repositorio en `CLAUDE.md:24` mencionaba solo 6 secciones canónicas en `propuesta/` (vocab, gram, com, dest, cult, eval), pero el dashboard pinta una 7.ª columna `itinerarios` desde v11.1; documentada como opcional (planificación cross-unidad). Higiene commit: `diagrama.py` + `CLAUDE.md` + meta docs. Sin drifts pendientes; arquitectura coherente con docs. Próximo: reactivar fase 2 o producto editorial.
- **2026-05-19** — Sync CLAUDE.md raíz tras cierre de deuda matcher (v11.6). Hallazgo del revisor tras v11.5: la sección "Estado fase 1 — Deudas residuales" de CLAUDE.md raíz seguía afirmando las 3 deudas matcher como abiertas, aunque ya quedaron resueltas en v11.3-v11.5. Bug puramente documental — no rompe runtime ni validación, pero deja una fuente normativa incorrecta para sesiones futuras (CLAUDE.md es la autoridad que cargan automáticamente todas las nuevas sesiones de Claude Code). Fix: reescrita la sección con dos bloques: "Deuda matcher: CERRADA en v11.3-v11.5" (con check de cada bug y la versión que lo resolvió) y "Deudas todavía abiertas" (canónicos huérfanos: Abreviaturas de los diccionarios, vegetariano; auditoría retroactiva U0-U5). Higiene commit: solo `CLAUDE.md` + meta docs.
- **2026-05-19** — Deuda matcher bug 3 (claves de dict) resuelta — **cierre completo de la deuda matcher** (v11.5). Tercer y último fix de la deuda técnica del matcher catalogada al cierre de fase 1. Problema: `_gather_text(o, out)` iteraba sobre dicts con `o.values()`, nunca recogía las claves. Caso real U6 v10.158: agenda `{"Lunes": "Piscina", "Martes": "Supermercado", ...}` — el matcher veía las actividades pero no los días, y `Días de la semana` quedaba con 1 solo item (Lunes capturado en otra actividad). Fix: `_gather_text` ahora itera con `o.items()` y añade la clave al output si es string, además de recursar sobre el valor. Resultado: para futuros inventarios, dicts con claves significativas (días, personajes, secciones, etc.) ya no obligan a reestructurar a lista de objetos. Las 10 unidades siguen 0/0/0 sin regresión. Decisión: U6 agenda NO se revierte a dict — el workaround de v10.158 (lista de objetos) se mantiene porque revertir es un cambio estructural sin ganancia funcional (el matcher ya lee ambos shapes) y otros consumidores potenciales pueden depender del shape actual; el fix elimina la necesidad del workaround para nuevos inventarios, que es lo importante. Trade-off aceptado: recoger claves de dict aumenta levemente la superficie de match; las claves del proyecto son normalmente etiquetas semánticas (`Lunes`, `personaje`, `texto_modelo`); si una clave técnica coincidiera con un item por casualidad, generaría falso positivo (riesgo bajo). **Cierre de la deuda matcher**: las 3 deudas técnicas catalogadas al cierre de fase 1 quedan resueltas en v11.3-v11.5 (acentos, imágenes, claves de dict). Higiene commit: solo `scripts/validar_inventario.py` + meta docs. Próximo: reactivar fase 2 (reciclaje) o continuar con producto editorial.
- **2026-05-19** — Deuda matcher bug 2 (imágenes) resuelta (v11.4). Segundo fix de la deuda técnica del matcher catalogada al cierre de fase 1. Problema: el validador `_activity_input_text` solo iteraba sobre `INPUT_FIELDS_LIST` que NO incluía `imagen`. Caso real U9 v10.164: `morado/-a` aparecía en `p101-act04` como color de prendas, pero solo dentro de `imagen.descripcion` ("Siete fotos de prendas... 1 falda morada, ... 7 jersey morado"). El validador no lo veía → no se podía codificar y el contenido material se perdía. Razón editorial del fix (autor en chat): la descripción de imagen es contexto editorial real que el alumno ve; para entender una actividad es necesario saber qué muestra la imagen — no es metadato. El campo `respuestas`, en contraste, tiene función distinta (clave para profesor / contexto para agentes), pero esa decisión se mantiene fuera del alcance de este fix. Fix: `scripts/validar_inventario.py` añade `"imagen"` a `INPUT_FIELDS_LIST`; `_gather_text` recurre sobre el dict `imagen` y recoge automáticamente todas las strings (incluido `descripcion`). Resultado: `morado/-a` en U9 codificado en bucket Colores recurrente con fuente `p101-act4`; refs de actividad p101-act04 ampliadas con Colores; las 10 unidades siguen 0/0/0 sin regresión. Trade-off aceptado: algunas descripciones de imagen son verbosas (ej. p100-act01 con tres trajes tradicionales); podrían generar falsos positivos puntuales para lemas cortos; riesgo bajo y simétrico al que ya existe con `texto_completo`. Pendiente: bug 3 (matcher no recoge claves de dict, caso `agenda` U6 v10.158). Higiene commit: `scripts/validar_inventario.py` + `unidades/U9/U9-nc1-inventario.json` + meta docs.
- **2026-05-19** — Deuda matcher bug 1 (acentos) resuelta (v11.3). Primer fix de la deuda técnica catalogada al cierre de fase 1. Problema: `_norm_text` solo aplicaba lowercase + collapse de whitespace, sin normalizar tildes. Caso real U8 v10.163: la palabra del libro era `marrones` (plural sin tilde) y el item canónico debía ser `marrón` (singular con tilde por §5.11), pero la diferencia `ó`/`o` impedía el match substring → workaround obligado: escribir `marrones` como item (violando §5.11). Fix: nueva función `_strip_accents(s)` en `scripts/validar_inventario.py` que descompone NFD y descarta marks de tilde aguda/grave/circumflex, preservando `ñ` (re-componiendo `n + U+0303` → `ñ`); `_norm_text(s)` ahora la aplica tras lowercase + whitespace collapse, simétricamente a needle y haystack. Resultado: `marrón` (item singular) atrapa `marrones` en texto; las 10 unidades siguen validando 0/0/0 sin regresión. Rectificación de workaround: `unidades/U8/U8-nc1-inventario.json` revierte `marrones` → `marrón` (forma singular canónica restaurada en bucket Colores recurrente). Trade-off aceptado: la normalización acepta como match casos como `tu`↔`tú` (posesivo vs pronombre sujeto), pero el inventario actual disambigúa por fuentes; si emergiera una falsa coincidencia, se documentaría. Pendiente: bug 2 (matcher no recoge `image.descripcion`, caso `morado/-a` U9) y bug 3 (matcher no recoge claves de dict, caso `agenda` U6). Higiene commit: `scripts/validar_inventario.py` + `unidades/U8/U8-nc1-inventario.json` + meta docs.
- **2026-05-19** — Dashboard auto-refresh por polling con hash (v11.2). Tras los ajustes UX de v11.1, el dashboard no detectaba cambios en `propuesta/*.md` sin recargar manualmente. Implementado auto-refresh: cada 5 segundos el frontend hace `fetch('/api/diagrams')` en silencio; el endpoint ya devolvía un campo `hash` (md5 del status object) calculado en backend (`diagrama.py`); si el hash recibido es igual al último renderizado → no se re-renderiza (sin DOM churn); si cambió → re-renderiza grid + tabs + diagrama. Cambios: `web/index.html` — `loadProjectData()` ahora acepta `{silent: true}` para polling, guarda `_projectHash`, añadido `setInterval(..., 5000)`. Resultado: cualquier modificación (creación, edición, borrado, añadir/quitar `*pendiente*`) se refleja en ≤5s sin recargar la pestaña. Sin nuevas dependencias. Higiene commit: solo `web/index.html` + meta docs. Próximo: decidir entre deuda matcher (`_expand_needle` / `_gather_text`) o reactivación de fase 2.
- **2026-05-19** — Dashboard "Estado de unidades" ajustes UX (v11.1). Tras revisión visual del autor sobre el panel: (1) retirada de columna "Reflexión" — no se trabaja como sección editorial independiente, generaba ruido; grid pasa de 8 a 7 columnas. (2) Criterio `complete` simplificado — el conteo de líneas no es señal de calidad; nuevo criterio: archivo existe sin marcadores `*pendiente*` → `complete`; con pendientes → `in-progress`; sin archivo → `missing`. (3) Celdas con marcas semánticas: `✓` (complete), `N⏳` (in-progress con N pendientes), `·` (missing). El número de líneas dejó de mostrarse porque no aporta info accionable. (4) U0 atípica reconocida — `unidades/U0/propuesta/punto-de-partida.md` propaga a las 7 celdas de U0 (cubre la unidad entera). (5) CSS grid alineado a 7 columnas (`grid-template-columns: 50px repeat(7, 1fr)`) — el ajuste sin esto provocaba desplazamiento de etiquetas U0/U1/... dentro de las celdas. Resultado: dashboard pasa de 0/80 (v10.164 con scanner roto) a 31/70 completas automáticamente. Higiene commit: `diagrama.py` + `web/index.html` + meta docs. Próximo: v11.2 auto-refresh.
- **2026-05-19** — Milestone post-fase 1: bump v11.0 + fix scanner dashboard (v11.0). Tras el cierre de fase 1 en v10.164 (d512066, U0-U9 validando 0/0/0, contrato canónico estabilizado, documentación operativa lista), bump major a v11.0 para marcar el hito: las nuevas iteraciones se centran en infraestructura y producto, no en saneamiento retrospectivo. Fix scanner del dashboard en `diagrama.py` `scan_section`: buscaba el patrón legacy `unidades/UN/UN-<seccion>*.md` y devolvía `missing` para todo el material editorial nuevo, que vive en `unidades/UN/propuesta/<seccion>.md` según el flujo de publicación canónica. Ahora busca primero en `propuesta/<seccion>.md` (nueva canónica) y luego en el patrón legacy. Resultado verificable: el dashboard pasa de 0/80 completas a 20/80 completas automáticamente (U1, U2, U4, U5 con 5 secciones cada una en `complete` + 4 con `in-progress` en evaluación). U0 atípica + U3, U6-U9 sin propuesta = `missing` legítimo. Próximo paso (v11.1): auto-refresh del dashboard cuando se añadan archivos en `propuesta/`. Higiene commit: solo `diagrama.py` + meta docs.
- **2026-05-19** — Cierre fase 1 v10.164 ya documentado en entrada del 2026-05-18 (hito previo).
- **2026-05-18** — **U9 cerrada — fin de fase 1 (extracción canónica U0-U9 completada)** (v10.164). Última unidad del curso NC1. Migración Opción C legacy→canónico post-v10.153 (10 páginas p92-101, 41 actividades, 6 cuadros, 8 _decisiones_ia). Verbos `ir`/`estar` en Pretérito indefinido — registry verbal v1.6 ya lo soportaba, sin alta nueva. Decisiones autónomas registry-driven: Marcadores temporales del pasado, Acentuación (agudas/llanas/esdrújulas), Interrogativos — todos ya en registries con _apariciones.U9. Rectificación revisor (4 puntos antes del cierre): (1) notación /-a en Colores con fem materialmente atestada (rojo/-a, blanco/-a, negro/-a; amarillo se queda masc por única flexión en U9); cleanup_v150 había revertido las barras automáticamente, corregido manualmente; (2) 'oscuro' reubicado de Colores → Adjetivos descriptivos (no es color sino modificador en 'colores oscuros', 'ropa oscura'); (3) bucket 'Adjetivos descriptivos' recurrente creado con 8 items materialmente atestados que el extractor original había omitido (alto/-a, ancho/-a, corto/-a, deportivo/-a, estrecho/-a, grande, largo/-a, oscuro/-a) — mismo perfil sistémico que U5 v10.162 y U8 v10.163; (4) refs de actividad actualizadas (p93-act07, p98-act01, p100-act02). Nueva deuda matcher documentada: 'morado/-a' (p101-act04 image 'falda morada'/'jersey morado') no codificado porque image.descripcion no está en INPUT_FIELDS_LIST de _gather_text — pendiente ampliar contrato o aceptar pérdida material. **Cierre fase 1: U0-U9 saneadas con contrato canónico v10.145 + §5.10 + §5.11. 4 registries (v1.8 / v1.5 / v1.1 / v1.6) + 4 PCIC A1 + 6 archivos operativos vigentes. Validador alineado con la parte mecanizable. Deudas residuales: matcher (acentos, claves de dict, image.descripcion), Abreviaturas de los diccionarios sin canónico (§0.1), vegetariano sin canónico (§0.1), auditoría retroactiva U0-U5 parcialmente cubierta.** Validador U9 → 0/0/0. Higiene commit: solo U9 + meta docs.
- **2026-05-18** — U8 cerrada: saneamiento estructural + rectificación editorial (v10.163). Tercera extracción real bajo el flujo IA-first post-rediseño. U8 ya estaba en shape post-v10.115 (no requirió migración estructural completa); el agente aplicó cleanup_v150 que retiró 44 errores §5.10/§5.11 heredados del legacy (fuentes sin aparición literal en `nariz`, adjetivos físicos, doler/gustar, pronombres OI, adverbios cantidad) + eliminó items huérfanos (amarillo, marrón en bucket erróneo) + unificó dedo/dedos. Validador 0/0/0 tras cleanup. Rectificación detectada por revisor antes de cerrar (3 puntos): (1) p85-act07/08 con enfoque 'gramatica' contradecía la decisión D-Abreviaturas-de-diccionarios-deferida ya documentada (debían ir como 'pronunciacion_ortografia') → corregido; (2) bucket 'Adjetivos descriptivos' mezclaba colores (rojo, verde, azul, gris, blanco, negro) con descriptivos físicos/carácter → trasladados a nuevo bucket 'Colores' recurrente desde U1 (mismo precedente que U5 v10.162); además recuperados amarillo/-a (p83-act8) y marrones (p88-act3/05, p89-act8) que cleanup había retirado por fuentes inválidas (deuda matcher documentada: _expand_needle no normaliza acentos ó→o, por lo que marrón no atrapa marrones; se usa la forma plural como item); (3) normalización de flexiones a notación barra masc/fem en 28 lemas de Adjetivos descriptivos (alto/-a, bajo/-a, ... famoso/-a) consistente con U5 v10.162 y precedente moderno/-a U6 v10.158. Refs de actividad actualizadas con 'Colores' en p82-act03, p83-act05, p83-act08, p86-act01, p88-act03, p88-act05, p89-act07, p89-act08, p91-act06. Decisiones autónomas relevantes: Construcción gustar/doler única categoría (registry-driven), Adverbios de cantidad incluye muy + mucho/-a/-os/-as (flexiones distintas), El cuerpo humano (no Partes del cuerpo) por naming literal del índice. Deudas residuales: Abreviaturas de los diccionarios sin canónico (§0.1 al autor), matcher acentos (caso marrón/marrones), mismo perfil bug sistémico que U5 (colores mal encajados + flexiones simples). Validador U8 → 0/0/0. Higiene commit: solo U8 + meta docs.
- **2026-05-18** — Rectificación U5 'Adjetivos descriptivos' + categoría 'Colores' recurrente (v10.162). Hallazgo del autor en revisión: el bucket tenía solo 6 items (antiguo, grande, lento, pequeño, rojo, rápido) mientras la descripción prometía 14. Causa raíz: extractor capturó palabras_recuadro de p57-act7 + lemas ubicuos pero perdió toda la actividad fundacional p53-act6 (relaciona antónimos con 10 adjetivos). Rectificaciones: (1) 8 items añadidos al bucket (alto, bonito/-a, cómodo/-a, feo, incómodo, moderno/-a, nuevo/-a, viejo); (2) antiguo/pequeño rectificados a notación barra masc/fem; (3) 'rojo' trasladado a 'Colores' (recurrente desde U1, no es adjetivo descriptivo sino color); (4) fuentes recalculadas por aparición literal (p53-act6, p53-act7, p55-act5, p55-act6, p55-act8, p57-act6, p57-act7, p58-act1, p58-act2, p61-act1, p61-act4); (5) ref p57-act7 ampliada con 'Colores'; (6) entrada P19 en _decisiones_ia documentando rectificación y causa raíz. Mismo perfil de bug que U6 'Días de la semana' (v10.158), U4 'Envases y recipientes' (v10.159), U4 'madrileño → Gentilicios' / 'colombianas → colombiano/-a' (v10.159) — refuerza deuda auditoría retroactiva U0-U5 buscando patrones análogos (items perdidos en columnas_relaciona, items_libro con flexiones, campos huérfanos por canónico inexistente). Validador U5 → 0/0/0. Higiene commit: solo U5 + meta docs.
- **2026-05-18** — U7 extraída + rectificación pron/orto + sync registry (v10.161). Segunda extracción real bajo el flujo IA-first post-rediseño (precedente U6 v10.158). Modo: directa desde chat coordinador (sin worktree), Opción C estilo U6 (legacy pre-v10.115 como referencia de transcripción + PDF de verificación + JSON canónico nuevo en shape post-v10.153). U7: 10 páginas (p72-81), 46 actividades, 4 cuadros, 8 _decisiones_ia, 29 lemas verbales (todos canónicos del registry v1.6, sin altas). Consolidados léxicos P: Meses del año + Animales domésticos y salvajes; R: Días de la semana. Gramática P: Preposiciones; R: Hay + Interrogativos. Pron/orto P (tras rectificación): Signos de puntuación + Mayúsculas + Acentuación + Entonación. Rectificación detectada por revisor: la extracción inicial agrupaba 6 items (., ',', ¿?, ¡!, tilde, mayúscula) bajo 'Signos de puntuación' siguiendo la presentación pedagógica de p75-act8, pero el registry canónico separa en 3 categorías. Aplicado split: tilde → Acentuación (nueva principal U7, identificación inicial); mayúscula → Mayúsculas (nueva principal U7); punto/coma/¿?/¡! quedan en Signos de puntuación. Sincronización del registry pron/orto v1.0→v1.1 con _apariciones.U7 para Acentuación y Mayúsculas + refinamiento Signos de puntuación + nueva subcategoría 'identificación inicial' en ambas + _deuda_pcic_ref documentada en Signos de puntuación (punto/coma sin cobertura en _pcic_ref/subcategorías — no bloqueante para v10.161). Decisiones autónomas con precedente: Verbos reflexivos y Oposición salir/volver no generan categoría gramatical canónica (viven lema-a-lema en tiempos_y_verbos); Signos de puntuación en pron/orto pese a estar bajo Gramática en el índice del libro (precedente U6 Letras homófonas). Validador U7 → 0/0/0. Higiene commit: registry pron/orto + U7 + meta docs.
- **2026-05-18** — Cierre fino U6 + estado global actualizado (v10.160). Tres flecos detectados por el revisor tras visto bueno parcial de U6: (1) REVIEW.md §Estado global decía "U6-U9 pendientes" mientras la bitácora ya registraba U6 extraída → corregido a "U0-U6 saneadas, U7-U9 pendientes"; (2) descripción de `Establecimientos` en U6 prometía ampliación con "hospital, cafetería, gimnasio" pero `hospital` no figuraba como item del consolidado (solo en Profesiones y lugares de trabajo) → añadido a Establecimientos con fuentes p63-act8 (columna 'Lugares de trabajo') y p71-act4 ('médico/-a → en el hospital'); doble función reconocida en descripción; (3) worktree `proc-u6-wip` con delta local sobre U6 → diagnosticado como versión obsoleta pre-rename (pre-v10.158), superada por main. Decisión: cierre operativo del worktree pendiente de confirmación del autor (descarte + remove + delete rama). Validador U6 → 0/0/0.
- **2026-05-18** — Lote rectificación editorial U4 + alta canónica `Envases y recipientes` (v10.159). Auditoría del autor en chat sobre U4 detectó dos focos: (a) items mal encajados en categorías léxicas, (b) campo léxico nuclear no codificado por ausencia de canónico. Resultado: registry léxico v1.7→v1.8 con alta de `Envases y recipientes` (origen excepcion, precedente metodológico Gentilicios/La hora; PCIC A1 §Alimentación no recoge envases como subcategoría dedicada). Cuatro rectificaciones en U4-nc1-inventario.json: (1) Restaurante → Establecimientos (comedor escolar, puesto, quiosco, pizzería; quioscos→quiosco §5.11; refs p48-act2/p50-act1/p50-act3 actualizadas); (2) Adjetivos de nacionalidad → Gentilicios (madrileño, canónico v10.124); (3) colombianas → colombiano/-a (precedente moderno/-a v10.158, deuda matcher §5.10/§5.11 flexiones fem); (4) retirada de `vegetariano` (no es nacionalidad ni gentilicio, sin canónico vigente — escalado a deuda). Alta de `Envases y recipientes` como principal en U4 con 9 items (botella, paquete, trozo, vaso, cartón, taza, lata, bote, bolsa, fuentes p47-act5+p47-act6, refs de actividad actualizadas). Causa raíz del bug original: extractor U4 perdió campo léxico nuclear de p47 por ausencia de canónico + omisión §0.1 (escalado en chat). Validador U4 → 0/0/0. Higiene commit: limitado a registry + U4 + meta docs. Deudas: vegetariano sin canónico, matcher §5.10/§5.11 flexiones, auditoría retroactiva U0-U5 buscando patrones análogos.
- **2026-05-18** — U6 extraída + correcciones editoriales + canónicos renombrados (v10.158). Primera extracción real tras todo el rediseño de fase 1. Migración desde shape legacy a canónico post-v10.153 vía Opción C en worktree separado. U6: 44 actividades, 5 cuadros, 14 _decisiones_ia. Validador 0/0/0. Cambios estructurales en registry léxico v1.6→v1.7: `Establecimientos:...` y `Marcadores de lugar:...` renombrados a forma corta con aliases largos preservados (patrón ya usado en Números cardinales / Saludos y despedidas). Propagación: U5 (3 refs), U6 (26 refs). Correcciones detectadas en revisión visual del autor: (1) moderno/-a como notación pragmática por incoherencia §5.10A/§5.11 en flexiones masc/fem no substring; (2) Días de la semana 1→7 items tras descubrir que el extractor solo capturó Lunes; (3) datos.agenda en p66-act4 reestructurada dict→lista para que el matcher recoja los días (deuda _gather_text); (4) p65-act4 confirmado como error editorial NC1 (8 items_libro vs 10 respuestas), documentado. Deudas técnicas diferidas: matcher §5.10/§5.11 flexiones, _gather_text claves de dict, auditoría retroactiva U0-U5.
- **2026-05-17** — Ampliaciones contrato preparatorias U6 (v10.157). Segundo cambio estructural preparatorio (tras v10.156 `Imperativo (tú)`). Decisiones del extractor U6 vía §0.1: (A) registry verbal +11 lemas con anclaje material en U6 — poner, beber, dar, dejar, cortar, pegar, conectar, decorar, comprar, recorrer, pasear; `_meta.version` 1.5→1.6, total 49→60 lemas; (B) enum `tipo` +`lee` para casos de input puro lector sin acción posterior (p68-act1, p69-act4); schema §5 + validador `TIPOS_VALIDOS` actualizados, 20→21 valores. Excluidos correctamente: `andar` (solo gerundio), `salir/levantarse/sentarse/ducharse/llamar` (anticipación U7 PRE), verbos de metalengua. Decisiones complementarias de codificación (sin cambio contrato): p69-act7 → `escucha` con nota kinésica; p63-act05/08 → `datos.columnas_relaciona` (§4.3.1). Validador U0-U5 → 0/0/0. Worktree U6 debe rebasear para sincronizar.
- **2026-05-17** — Registry gramatical: `Imperativo (tú)` añadido (v10.156). Cambio preparatorio para U6. Propuesta-en-chat §0.1 del extractor de U6: `nc1-curso.json` declara `Imperativo (tú)` como contenido gramatical principal de U6, pero `gramatica-canonica.json` v1.4 no incluía la categoría. Dictamen del autor + revisor (coincidentes): añadir como categoría canónica. Naming canónico literal del índice (B+C: no renombrar a "Imperativo afirmativo (tú)" para preservar regla de naming literal). La distinción afirmativo/negativo se documenta en `_nota` del registry + en `descripcion["U6"]` cuando se codifique. Decisiones complementarias del extractor: (a) `Presente de verbos irregulares: cerrar, ir, venir, hacer, jugar, dormir` NO se añade al registry gramatical — la irregularidad es propiedad del lema verbal y vive en `tiempos_y_verbos_consolidado` vía `rasgo_por_tiempo`; (b) `Profesiones y lugares de trabajo` ya es canónico del registry léxico, en U6 pasa a `principal`. Validador U0-U5 → 0/0/0. Próximo: extractor de U6 sincroniza worktree y continúa paso 3.
- **2026-05-17** — Reglas: salvedad de excepción autorizada (§5.1.1 + §6.3) + rectificaciones U1/U3 (v10.155). Tras revisión del autor del concepto `recurrente` (glosario + reglas §5.1.1), detectó casos donde una categoría es principal en unidad posterior según índice del libro (lo que §5.1.1 + §6.3 prohíbe codificar como recurrente) pero pedagógicamente sostiene el principal de la unidad actual. Decisión: introducir salvedad estrecha autorizada por §0.1. Cambios: (1) `reglas-operativas.md` §5.1.1 + §6.3 con salvedad alineada en ambas (mantiene regla general estricta + abre excepción rara + exige descripción + entrada `_decisiones_ia`); (2) U3 `recurrente["Profesiones y lugares de trabajo"]` con descripción rectificada (antes invocaba §5.1.1 (b) incorrectamente) + nueva entrada P-Profesiones-anticipacion-U6 en _decisiones_ia; (3) U1 `recurrente["Adjetivos de nacionalidad"]` con descripción rectificada sin duplicar P3/P12/P13 históricos. U4 `Restaurante` confirmado como categoría legítima (no anticipación). U5 `Establecimientos...` queda para decidir después. Validador U0-U5 → 0/0/0.
- **2026-05-17** — Rectificación editorial U5: `Tipos de vivienda` vs `Alojamiento` (v10.154). Tercera rectificación U5 detectada por el autor antes de lanzar U6. Diagnóstico: la categoría `Tipos de vivienda` tenía 8 items mezclados — solo `apartamento` es vivienda permanente material; los otros 7 (hotel, albergue juvenil, crucero, casa rural, caravana, camping, granja) aparecen en p53-act8/9 como alojamiento turístico con definiciones explícitas ("Alojamiento con ruedas...", "Barco para viajes de vacaciones..."), contradiciendo la decisión P (v10.139): "no fusionar; son canónicos distintos". `chalé` no estaba codificado pese a estar atestado en p58-act2 respuestas ("Elisa vive en un chalé"). Acción: 7 items movidos al canónico `Alojamiento` (activado por primera vez del registry pcic_a1); `chalé` añadido a `Tipos de vivienda`; refs en activity actualizadas según items materialmente presentes. Sin cambios en registry léxico. Confirma decisión P del autor v10.139. Validador U5 → 0/0/0.
- **2026-05-17** — Saneamiento documental fase 1 + cleanup integrado en flujo oficial (v10.153). Dictamen del revisor antes de lanzar U6: la documentación operativa de fase 1 seguía describiendo un validador desalineado y un proceso de validación manual transitoria que ya no aplica desde el Lote 3E (v10.151). Cuatro archivos saneados: `prompt.md` (nota transitoria retirada + flujo de pasos reordenado de 6 a 7 con cleanup_v150.py integrado como paso 5 entre escritura del JSON candidato y validador final), `CLAUDE.md` de fase 1 (validador como gate real, reglas críticas #4 y #5 reformuladas sin mención al estado transitorio), `schema-inventario.md` (aviso top sin promesa de alineación absoluta), `glosario.md` (nota transitoria del §13 sustituida por estado actual). Redacciones aprobadas por el revisor con tono sobrio: "alineado en la parte mecanizable / gate de cierre", no "sin deuda". Sin cambios materiales en JSONs/scripts/registries. Validador U0-U5 → 0/0/0. Próximo paso natural: U6 piloto.
- **2026-05-17** — Auditoría completa del registry léxico cerrada sin retiradas (v10.152b). Protocolo aplicado: cruzar 102 entradas con uso en U0-U5 y con vocabulario declarado en `nc1-curso.json`. Resultado: 26 conservar por `origen: indice`, 8 atestadas en U0-U5, 16 reserva por encaje con U6-U9 declarado, 4 excepciones previas. Las 48 entradas pcic_a1 sin uso explícito se conservan por dictamen del autor: cubren contenido A1 que NC1 trabaja bajo otros nombres canónicos. Decisión registrada. Sin cambios materiales. Auditoría del registry cerrada. Próximo paso natural: extracción de U6-U9.
- **2026-05-17** — Registry léxico: retirada de `Adverbios de posición` (v10.152a). Tras la rectificación U5 en v10.152, el canónico léxico `Adverbios de posición` quedó sin uso activo. Verificación previa: las 6 menciones en U5 son todas en `_decisiones_ia` (narrativa histórica), ninguna es referencia activa. Cobertura semántica preservada en `gramatica-canonica.json` bajo `Adverbios y locuciones de lugar` (PCIC A1 §8). Registry: 103 → 102 entradas; _meta.version 1.5 → 1.6. Validador U0-U5 → 0/0/0. Pendiente: revisión de entradas pcic_a1 sin uso en U0-U5.
- **2026-05-17** — Rectificación editorial U5 + U3 (v10.152). Surgida de auditoría del registry léxico. **Frente A (U5):** los marcadores espaciales (encima, debajo, delante, detrás, al lado) vivían en `vocabulario_consolidado.principal["Marcadores de lugar: a la izquierda, ..., cerca"]`. Pero el índice del libro coloca "Posición" en gramática, no vocabulario, y PCIC A1 §8 los categoriza como adverbios/locuciones adverbiales de lugar. El bloque léxico contenía además items que NO pertenecen a "Marcadores de lugar" del índice (al lado/delante/detrás solapan; encima/debajo no; izquierda/derecha/enfrente/entre/lejos/cerca del registry léxico no están en U5). Corrección: bloque vocab retirado; 5 items reales trasladados a `gramatica_consolidada.principal["Adverbios y locuciones de lugar"]` (categoría canónica preexistente, NO se crea nueva). 11 refs vocab retiradas, 2 gram añadidas. **Frente B (U3):** registry léxico canonizó `La hora` en v10.129 pero U3 nunca la pobló (contenido diluido bajo `Números cardinales`). Corrección: nueva categoría con 12 items atestados en cuadro@p37#1 + p37-act5/6/7, solo lemas con aparición literal (§5.10 A). Primera codificación tenía 4 errores de fuente (`@R` mal asignado cuando la expresión está en audio.transcripcion = input); validador extendido los detectó. Validador U0-U5 → 0/0/0. Pendiente auditoría completa registry: `Adverbios de posición` huérfano + entradas pcic_a1 sin uso.
- **2026-05-17** — Lote 3E: validador absorbe §5.10 A + §5.11 + fix orden FASE 2/1 (v10.151). Cierre técnico del contrato v10.145+§5.10+§5.11. `scripts/validar_inventario.py` deja de ser gate parcial: nueva función `_validar_510_511()` con réplica autónoma de `_expand_needle`, `_match_in_text`, `_is_gramatica_categoria_A`, `_detectar_flexion_par`. Chequea: (a) aparición literal de items Cat A en contenido didáctico §5.2 + respuestas[] + cuerpo de cuadros; (b) pares de flexiones en `vocabulario_consolidado` sin unificar. Bug del orden corregido en `cleanup_v150.py`: FASE 2 PASO A (reescritura `lema/-suf`) debe correr antes de FASE 1 para que la verificación vea la palabra canónica, no la verbatim. Re-saneamiento detectó 11+11+4=26 fuentes residuales en U1/U2/U3 (caso `mexicano/-a` con fuente que solo atestaba `mexicana`). §5.10 B y excepciones léxicas de §5.11 permanecen como deuda editorial sostenida por protocolo §0.1 (declarado en schema §A.3). Validador U0-U5 con chequeos nuevos → 0/0/0. Limpieza previa: todos los `.bak.v10.145/v10.150` eliminados; pattern añadido a `.gitignore`.
- **2026-05-17** — Lote 3D-cleanup cerrado U0/U1/U3/U5 agrupados (v10.150c). Tercer y último apply del Lote 3D-cleanup. Sin cambios en el script desde v10.150b. Resultados U0=5/0/0/0, U1=48/23/0/11, U3=52/6/0/8, U5=23/2/5/0 (formato: fuentes/items/reescrituras/pares). 0 categorías vacías en las 4. Estado consolidado del lote completo (a+b+c): 236 fuentes A retiradas + 46 items completos retirados + 31 reescrituras de notación `lema/-suf` + 19 pares unificados + 0 categorías vacías en las 6 unidades U0-U5. Validador 0/0/0 en todas. Cierre operativo del contrato v10.145 (cambio de semántica @R) + §5.10 (aparición material A/B) + §5.11 (unificación de flexiones). Pendiente: Lote 3E (extender validador para absorber parte automatizable de §5.10 A + §5.11, declarado en `schema-inventario.md` §A.3/§A.4); auditoría del registry `campos-semanticos-canonicos.json` (fuera de scope de 3D-cleanup por dictamen del revisor).
- **2026-05-17** — Lote 3D-cleanup U2 + correcciones del script (v10.150b). Tras primer intento de apply U2, el revisor detectó `gramatica_consolidada.recurrente.Artículos determinados` con `items: []` y `fuentes: []` (entrada muerta). Rollback inmediato. Dos correcciones al script `cleanup_v150.py`: (1) **FASE 4** nueva que retira categorías sin items del consolidado y limpia sus labels en `actividad.X` / `cuadro.X`. (2) **Mutación siempre activa del dict en memoria** (antes solo en `--apply`) para que dry-run muestre categorías vaciadas como consecuencia de FASE 1. (3) **Heurística `is_gramatica_categoria_A` reformulada conservadora**: la versión v10.150a tenía falsos positivos graves (`abuelo/abuela`, `¿Qué...?`, `Oposición ser / estar` clasificados como A). Primer dry-run con FASE 4 detectó pérdida de categorías principales en U3 (Interrogativos) y U5 (Oposición ser/estar). Nueva heurística rechaza `/`, `?`, `...`, `+`, paréntesis, IPA, item idéntico a categoría, >3 palabras o >25 chars. U2 con correcciones: 54 fuentes + 9 items + 26 reescrituras `lema/-suf`, 0 categorías vacías, validador 0/0/0, diff 60/-73. U4 (v10.150a) no requiere revisión: lo retirado sigue siendo Cat A. Próximo: v10.150c U0/U1/U3/U5.
- **2026-05-17** — Lote 3D-cleanup piloto U4 (v10.150a). Primer apply de §5.10 + §5.11 retroactivo. Script `cleanup_v150.py` nuevo con 3 FASES (retirar inferencias, unificar flexiones, saneamiento). U4: 54 fuentes A retiradas + 6 items completos retirados (3 vocab + 3 objetos verbales en listas tipadas sin formas). Validador 0/0/0. Diff: 41 insertions / 97 deletions. Iteración pre-apply: primer intento falló validador porque dejaba `formas_trabajadas: []`; fix aplicado para retirar objeto verbal entero cuando la lista queda vacía. Pron fuera del alcance (toda Cat B). Próximos: v10.150b U2 (volumen alto: 106+28+26), v10.150c U0/U1/U3/U5 agrupados.
- **2026-05-17** — Doc: §5.10 — exclusión de `instruccion_original` reformulada como remitencia (v10.149c). Dictamen del revisor: la cláusula *"PROHIBIDO usar `instruccion_original` ni cualquier metalengua excluida por §5.2"* en el bloque "Errores prohibidos" de §5.10 presentaba como novedad algo que ya vive en §5.2 generalizada (v10.144). Mezclar confirmación con cambios sustantivos dificultaba el dictamen. Reformulación: se retira la viñeta del bloque de prohibiciones y se sustituye por una nota de remitencia explícita ("§5.10 no introduce esa prohibición; se apoya en ella"). Sin cambios en schema, scripts ni JSONs.
- **2026-05-17** — Doc: Apéndice transitorio del schema saneado (v10.149b). Dictamen del revisor sobre v10.149/149a: añadir deuda §5.10/§5.11 al Apéndice sin sanear primero el relato existente reproduciría la desalineación contractual que el proyecto ha venido corrigiendo desde v10.145. Tres correcciones aplicadas: (a) §A.1 reescrita — el validador YA está alineado con gran parte del shape (4 bloques top-level, enums vigentes, formato de fuentes con @R, tipo_cuadro, _migracion_rediseno); la deuda restante es parcial y específica. (b) §A.3 reescrita como "deuda restante del validador" — limpiada de pendientes ya absorbidos; §5.10 declarada con distinción A (automatizable, pendiente portar `expand_needle` al validador) / B (no validable, sostenida por juicio editorial); §5.11 declarada como detección automatizable parcial con excepciones léxicas manuales. (c) §A.4 ampliada — la retirada del Apéndice exige absorción automática de la parte mecanizable Y protocolo explícito documentado para la parte editorial no automatizable. Evita auto-imposición de cierre imposible. Sin cambios en `reglas-operativas.md`, scripts ni JSONs. Validador 0/0/0.
- **2026-05-17** — Doc: §5.11 corregida (v10.149a). Dictamen del revisor sobre v10.149: la redacción "se unifican siempre" contradecía (a) el precedente v10.135b que aplicó la unificación solo cuando había ≥2 flexiones atestadas; (b) §5.10 (aparición material) porque permitía inventar `argentino` cuando solo `argentina` está atestado; (c) la propia excepción interna "lemas con una sola forma se mantienen verbatim". Reformulación: la unificación SE APLICA cuando 2+ flexiones del mismo lema están atestadas; cuando solo hay una forma atestada, se mantiene verbatim sin inferir la otra. Notación `lema/-a` se reescribe al masculino solo si esa forma está atestada. Cambios en `reglas-operativas.md` §5.11 y `schema-inventario.md` §9.1 (cross-ref reformulado). Sin cambios en JSONs.
- **2026-05-17** — Contrato: §5.10 + §5.11 añadidas (v10.149). Tras auditoría de 459 anomalías generadas por el matcher (vocab 143, verbos 25, gramática 252, pron 39), el autor y el revisor cierran el contrato v10.145 con dos reglas operativas nuevas. **§5.10** (aparición material) distingue Categoría A (superficie textual: lemas, formas, realizaciones gramaticales — exige literalidad) vs Categoría B (etiquetas del registry, paradigmas editoriales, notación técnica — exige justificación pedagógica). Criterio discriminante para gramática: "¿el alumno lo lee, escribe, dice o ve literal en el material?". **§5.11** canoniza la unificación de flexiones a **lema canónico singular** (masculino singular en adjetivos/gentilicios, singular conservando género léxico en sustantivos — `manzana` se queda `manzana`, no se masculiniza). Regla aplicada en v10.135b a U4, ahora regla permanente. Cross-ref en schema §9.1. **No hay cambios materiales en JSONs** en este commit. **Próximo lote (Lote 3D-cleanup):** aplicación retroactiva de §5.10 (~205 fuentes Cat A retiradas estimadas en U0-U5; ~25-50 unificaciones §5.11 en U0-U3 y U5). La redacción de §5.10 se iteró 4 veces con el revisor para resolver: compatibilidad con §5.1.3, scope (listas tipadas → consolidados), discriminador gramática (incluir cuadros), referencia a §5.2 en lugar de inventario duplicado.
- **2026-05-17** — Lote 3D: matcher mejorado + re-migración vocab U2/U3 + gramática modo A (v10.148). `expand_needle` añadido al script: expande lemas con barra-sufijo (`argentino/-a` → `{argentino, argentina}`, `francés/-esa`, `alemán/-ana`, `español/-a`) y paréntesis (`A mí me gusta(n)` → `{A mí me gusta, A mí me gustan}`). Anomalías vocab reducidas de 246 a 143 (–103). **3D1 vocab:** re-migración puntual U2 (17 A + 15 B nuevos) y U3 (1 A + 4 B nuevos) — 37 fuentes nuevas detectables por matcher mejorado. **3D2 gramática modo A:** U1=2, U2=3, U3=9, total 14 reemplazos. Primera migración de `gramatica_consolidada`. Saneamiento colateral en U4 gramática: una inconsistencia preexistente (fuente en item pero no en cat.fuentes agregado) corregida por el recompute. Gate ampliado: `APPLY_ALLOWED = {vocab, verbos, gramatica}`. **Dictámenes del revisor explícitos**: gramática modo B (68 candidatos) no autorizado por volumen y 252 anomalías restantes (paradigmas con barras múltiples); pronunciación no autorizada como lote (apertura desigual, 10 A solo en U3 — "caso local prometedor", no dimensión migrable). Validador U0-U5 → 0/0/0.
- **2026-05-16** — Lote 3B2: verbos modo B (dual retroactivo) en U1-U5 (v10.147). Tras Lote 3B1 (modo A) cerrado y revisión del autor, se aplica modo B con `--include-dual` ahora autorizado también para verbos. 103 duales añadidos: U1=13, U2=20, U3=39, U4=16, U5=15. Validador 0/0/0 en todas. Orden de apply U1/U2/U5 → U3/U4. Cierre del Lote 3B completo para verbos sobre las fuentes detectables por el matcher: 21 (A) + 103 (B) = 124 fuentes @R añadidas a `tiempos_y_verbos_consolidado`. **Las 25 anomalías heredadas de 3B1 (verbos consolidados donde ninguna `forma_trabajada` aparece literal) permanecen como deuda no resuelta, no como parte cerrada del matcher**. Causa probable: deuda preexistente del extractor o `formas_trabajadas` incompleto. Análoga a las 131 anomalías de vocab U2. Lote 3C (gramática y pron) sigue congelado por falta de matcher por bloque definido.
- **2026-05-16** — Lote 3B1: verbos modo A en U1-U5 (v10.146). Aplicación del modo A (respuesta-only → @R) al bloque tiempos_y_verbos_consolidado. Primera migración fuera de vocabulario. Matcher por bloque: `formas_trabajadas` en lugar del lema infinitivo (hallazgo del autor que invalidó el 3B-mini previo). Reemplazos: U1=2, U2=3, U3=13, U4=2, U5=1, total=21. Validador 0/0/0 en todas. Orden aplicado por dictamen del revisor: U1/U2/U5 primero, U3/U4 después (anomalías concentradas). Anomalías documentadas como deuda separada: 25 fuentes / 337 = 7 % (tasa baja vs vocab U2 41 %). Patrón: verbos consolidados sobre actividades donde ninguna forma_trabajada listada aparece literal — análogo a deuda heredada del extractor. Lote 3B2 (modo B / dual-tracking retroactivo, 103 candidatos) diferido por dictamen del revisor. Gramática y pron siguen congeladas. Script extendido: `iter_items` para verbos devuelve lista de formas_trabajadas; `classify_fuente` admite target string o lista; gate de apply ampliado a `{vocab, verbos}`; modo B prohibido explícitamente para verbos.
- **2026-05-16** — Doc: precisión semántica de cifras Lote 3A (v10.145d). Auditoría del revisor detectó ambigüedad: "332 duales" no era total en HEAD sino delta añadido por Lote 3A. Conteo verificado en HEAD: vocabulario U0-U5 tiene 332 duales (pares plain+@R) + 169 respuesta-only puros = 501 @R totales. Pre-Lote 3A había 0 duales + 169 solo-@R = 169 @R totales. Por unidad post-3A: U0=11 @R (0 dual / 11 solo), U1=72 (48/24), U2=55 (42/13), U3=67 (42/25), U4=182 (125/57), U5=114 (75/39). Sin cambios materiales. Validador 0/0/0.
- **2026-05-16** — Lote 3A cierre U2 (v10.145c). 42 duales aplicados al `vocabulario_consolidado` de U2. Las 131 anomalías de dry-run **no se resuelven** y se documentan como **dos deudas distintas**: (a) limitación del matcher para lemas con barra de flexión (`argentino/-a`, `francés/-esa`, ~70-90 casos) — la palabra sí está en la actividad pero el matcher literal no la encuentra; (b) deuda heredada del extractor original (~30-50 casos) — fuentes codificadas por campo semántico sin aparición material del lema (ej. `lunes` en p23-act12). La mejora del matcher queda como tarea separada por dictamen del revisor (no mezclar cierre de datos con rediseño de script). Total Lote 3A consolidado: U0=0, U1=48, U2=42, U3=42, U4=125, U5=75, **332 duales**. Validador U0-U5 → 0/0/0.
- **2026-05-16** — Lote 3A: dual-tracking retroactivo en `vocabulario_consolidado` U1, U3, U4, U5 (v10.145b). Script extendido con `--include-dual` (solo `--block vocab`). Política: para cada fuente `pNN-actMM` cuya palabra aparece en input Y `respuestas[]`, añadir `pNN-actMM@R` alongside del plain. Duales commiteados: U1=48, U3=42, U4=125 (gate), U5=75, total=290. Validador 0/0/0 en todas las incluidas. **U2 excluida del commit** por 131 anomalías en dry-run (volumen elevado) — los 42 duales se calcularon en working tree pero no se commitean; pendiente gate específico de U2 (revisar causa de anomalías + muestra de diff + revisión visual). Lote 3B (estudio reabierto) pendiente: matcher por `formas_trabajadas` para verbos; gramática y pron congeladas. Hallazgo del autor que reorientó el plan: el 3B-mini previo (verbos/gramática/pron, 0 candidatos / 197 anomalías) estaba mal planteado — buscaba el lema infinitivo en texto cuando debe buscar formas_trabajadas conjugadas.
- **2026-05-16** — Lote 2: migración de datos U0-U5 en `vocabulario_consolidado` (v10.145a). Herramienta: `scripts/migrate_at_r_v10145.py` (CLI con selector unidad/bloque, dry-run default, `--apply` solo vocab en esta fase, backup automático, validador integrado, reformateo canónico). Modo A (solo respuesta-only). Reemplazos: U0=11, U1=24, U2=12, U3=25, U4=40, U5=38, total=150. Validador 0/0/0 en todas. Dual-tracking preexistente preservado. Cuadros intactos. Caso "cocina" U5 verificado: las 3 fuentes solo-respuesta (p52-act2, p59-act4, p61-act3) pasan a @R. Fix bloqueante aplicado pre-apply: recomputación de `cat.fuentes` agregadas tras mutar items (revisor detectó incoherencia con shape del schema). Lote 3 pendiente: habilitar verbos/gramática/pron tras smoke + decisión sobre dual-tracking retroactivo (125 candidatos B-only solo en vocab U4).
- **2026-05-16** — Cambio de contrato: `@R` redefinido como marcador de localización (v10.145, Lote 1). Diagnóstico del autor sobre caso material "cocina" U5 (8 fuentes, 3 con la palabra solo en respuestas y tipo no-productivo, todas pintadas en gris en dashboard pese a ser respuestas) y dictamen del revisor: el contrato anterior acoplaba `@R` al `tipo` (5 productivos), generando falsos negativos sistemáticos en actividades no-productivas con palabra revelada en respuestas. Nueva semántica: `@R` indica únicamente que la palabra aparece solo en `respuestas[]`, sin restricción por tipo; la productividad vive en `actividad.tipo` y se cruza en runtime. **Lote 1 (atómico, 3 archivos núcleo + 2 docs meta):** `schema-inventario.md` §9.5 redefinido + apéndice transitorio actualizado; `reglas-operativas.md` §6.5 reescrita (chequeo OBLIGATORIO contra productivos retirado; dual-tracking generalizado); `scripts/validar_inventario.py` con constante `TIPOS_PRODUCTIVOS_AR` borrada y gate eliminado. Validador U0-U5 → 0/0/0 sin tocar JSONs. **Lote 2 pendiente:** migración programática de datos U0-U5 (≥138 casos estimados en `vocabulario_consolidado` que pasarán a llevar `@R`, mayoritariamente `completa_huecos`, `responder_preguntas_cerradas`, `escucha_y_responde`, `relaciona`).
- **2026-05-16** — U5: consolidación de bloques léxicos solapados + §5.2 generalizada + gate de invocación (v10.144). Hallazgo del autor durante revisión de U5: `Marcadores de lugar` marcado como recurrente desde U2 pero U2 no enseña esos lemas (solo `izquierda/derecha` como claves técnicas de `columnas_relaciona` y `delante` en una instrucción de aula). Causa raíz: §5.2 (exclusión por metalengua de instrucción) limitada a verbos. Adicionalmente coexistía `Adverbios de posición` con item placeholder vacío. **Cambios materiales:** `Adverbios de posición` retirado de U5; `Marcadores de lugar` recurrente → principal con items reales; 9 refs renombradas; descripción del bloque gramatical paralelo actualizada; nueva P17 en `_decisiones_ia`. **Cambios estructurales:** §5.2 generalizada a las 4 dimensiones (léxico, verbos, gramática, pron/orto) con tabla explícita metalengua vs contenido didáctico, incluyendo cuadros (`titulo` metalingüístico excluido, cuerpo incluido). Casebook §4.1 con caso "delante en U2". `prompt.md` con gate de arranque obligatorio (ejecutor declara lectura de los 3 contratos vivos + invocador cita literalmente prompt.md). `CLAUDE.md` fase 1 con nueva regla crítica 6 "Documentación de fase 1 siempre obligatoria"; antigua regla 6 → 7. Registry léxico NO tocado (decisión del autor). Validador U5 → 0/0/0.
- **2026-05-16** — U5: corrección sistémica de `id` de actividades + auditoría §6.5 (v10.143). Aplicación a U5 del mismo saneamiento de U4. Diferencia clave: en U5 tanto `id` como `fuentes` usaban índice local sin padding (a diferencia de U4 donde solo el `id` lo usaba), por lo que la propagación a fuentes SÍ era necesaria. 46 actividades renombradas (22 con cambio efectivo de valor) a `U5-p<pagina>-act<numero>`; fuentes propagadas en los 4 bloques consolidados con `@R` preservado; refs internas (`secciones.X.actividades_ids`, etc.) propagadas por walker recursivo. Auditoría §6.5 dual: 0 "de más" (los 19 `@R` existentes son todos legítimos) y 0 "de menos" (U5 no tiene caso prototípico análogo a `U4-p49-act9`; las 3 productivas con `respuestas[]` no tienen léxico exclusivo del solucionario). Sin cambios en registries. Validador U5 → 0/0/0. Deuda restante: U6-U9.
- **2026-05-16** — U4: `@R` faltantes en `p49-act9` añadidos + §6.5 reforzada (v10.142d). Hallazgo del autor revisando "verdura": aparecía sin `@R` cuando la actividad `p49-act9` (`interaccion_oral`, subtipo `busqueda_informacion_y_mediacion`) tiene un input escueto y todas las palabras de la categoría aparecen efectivamente solo en las respuestas. Auditoría dual (de más + de menos) con exclusión correcta de items ya dual-trackeados: 12 entradas faltantes / 11 lemas únicos (leche dupe en `Alimentos` + `recurrente.Bebida`) en `vocabulario_consolidado` (Alimentos: galletas, pasta, leche, fruta, verdura, bocadillo, arroz, cereales, tostadas, cacao; Comidas preparadas: ensalada; recurrente.Bebida: leche). Fix: fuente `p49-act9` añadida con sufijo `@R` (dual-tracking con la fuente sin sufijo ya presente). §6.5 reforzada con 3 aclaraciones explícitas para prevenir el mismo error en futuras extracciones: (a) dual-tracking input+respuesta única en misma actividad; (b) marcador "Posibles respuestas:" cuenta como respuesta a efectos de §6.5; (c) caso prototípico de `interaccion_oral`/`tarea_final` con subtipo de mediación o búsqueda de información (input escueto + respuestas con todo el vocabulario). Validador U4 → 0/0/0.
- **2026-05-15** — U4: saneamiento §6.5 que v10.138 no detectó por bug de lookup (v10.142c). Hallazgo del autor tras v10.142b: "cola" seguía con fuente `p47-act5@R` cuando la actividad numero 5 es `relaciona` (no productivo). Diagnóstico: v10.138 lookup hacía `pNN-actMM → UN-pNN-act<padded>`; al no coincidir con el id real (que era `act01`), resolvía erróneamente a otro `act05` (5to local, numero=9, productivo) y mantenía el @R. Ahora que v10.142b alineó id con numero, el lookup funciona. Fix: 20 @R retirados de U4 (10 en `relaciona` + 10 en `completa_huecos`); 12 @R legítimos en tipos productivos conservados. Validador U0-U4 → 0/0/0. U5 pendiente (siguiente lote).
- **2026-05-15** — U4: fix del fix de v10.142 (v10.142b). Hallazgo del autor: tras v10.142, "cola" aparecía con fuente `p47-act9@R` cuando debería ser `p47-act5@R`. Diagnóstico: el agente de U4 usó dos convenciones (id=índice local, fuentes=numero del libro). Mi script v10.142 asumió ambos campos en convención local y propagó fuentes innecesariamente, rompiéndolas. Fix: revertir JSON de U4 al estado pre-v10.142 y reaplicar SOLO el rename de id (49 actividades), sin tocar fuentes (que ya estaban correctas). Aprendizaje aplicado a U5: el script solo modifica `id`, no propaga fuentes. Validador U0-U5 → 0/0/0.
- **2026-05-15** — U4: corrección sistémica de `id` de actividades (v10.142). Bug detectado por el autor durante revisión visual de U5 (fuente `p53-act2` no correspondía a la actividad real "act 7" del libro). Auditoría: U0-U2 con convención correcta (`id = U<n>-p<pagina>-act<numero>`, sin padding, `numero` = número impreso en el libro); U3 también consistente; U4 y U5 con convención incorrecta (índice local de la página, con padding). U4 fix: 49 actividades renombradas, fuentes y `secciones.X.actividades_ids` propagados. Ej. `U4-p47-act08` → `U4-p47-act12`, fuentes `p47-act8@R` → `p47-act12@R`. U5 se arregla en lote siguiente (v10.143). Validador U0-U4 → 0/0/0.
- **2026-05-15** — U5: doble codificación de Adverbios — corrige interpretación de v10.141 (v10.141b). Aclaración del autor: el bloque debe estar en las dos dimensiones del consolidado simultáneamente, no solo en léxico. Cambio: restaurado `gramatica_consolidada.principal["Adverbios y locuciones de lugar"]` con mismos items/fuentes que el bloque léxico ya presente. 9 refs añadidas a activity/cuadro.gramatica (cuadro@p54, p54-act03, p55-act01/04/05, p56-act01, p58-act01, p59-act03, p61-act02). P16 reescrita reflejando la doble codificación: U5 trabaja (a) las palabras-léxico (qué se dice) Y (b) la categoría adverbial (cómo funciona sintácticamente con `estar`). Sin cambios en registries. Validador U0-U5 → 0/0/0.
- **2026-05-15** — `Posición` renombrada/reclasificada — A + B2 (v10.141). Hallazgo del autor: la categoría `Posición` en gramatica_consolidada.principal de U5 no es contenido gramatical propiamente — son palabras de marcador espacial (encima, debajo, detrás, delante, al lado) que NC1 A1.1 enseña como léxico, no como categoría adverbial abstracta. Doble cambio: (A) registry gramatical: `Posición` renombrado a `Adverbios y locuciones de lugar` (mismo _pcic_ref, items, _apariciones; queda disponible para unidades futuras que enseñen la clase adverbial), _meta.version 1.3→1.4; (B2) registry léxico: nueva entrada `Adverbios de posición` (origen pcic_a1) con los mismos items, distinta de `Marcadores de lugar: a la izquierda…` (canónico U2, otro alcance), _meta.version 1.4→1.5. En U5: bloque movido de gramatica_consolidada → vocabulario_consolidado con descripción reescrita; 9 refs en activity/cuadro.gramatica retiradas + `Adverbios de posición` añadido a activity/cuadro.vocabulario; nueva P16 en `_decisiones_ia` documenta. Pedagogía: encima/debajo/etc. en A1.1 se aprenden como léxico, no como categoría gramatical. Validador U0/U1/U2/U3/U4/U5 → 0/0/0.
- **2026-05-15** — U5: auditoría léxico-verbal + adición de `gustar` (recurrente) y `dormir` (excepción §5.2) (v10.140). Intuición del autor tras v10.139: "muy pocos verbos para 46 actividades". Audit detectó 2 lemas omitidos por el agente con apariciones reales: `gustar` (canónico U2/U3/U4 PRE, recurrente legítimo en U5 — p58-act02/03 y p59-act05, construcción gustar genuina) y `dormir` (canónico U6 PRE, excepción §5.2 aprobada por el autor mismo patrón U2/U3 — p58-act01/03 'mi perro duerme en mi habitación'). Falsos positivos del audit no codificados: `comer` ("como" comparativo), `cocinar`/`ducharse` (sustantivos = partes de la casa), `usar` (sustantivo deverbal "uso de"). Cambios: 5 entradas verbales nuevas en 4 actividades de U5 + 2 bloques nuevos en `tiempos_y_verbos_consolidado` (8 lemas totales, antes 6); P15 en `_decisiones_ia` documentando; registry verbal con U5:["PRE"] para gustar y dormir; @R filtrado contra §6.5. Validador U5 → 0/0/0.
- **2026-05-15** — Migración de U5 a shape v10.117 + propagación U5 a `tener` y `vivir` (v10.139). Sexta unidad re-procesada con el flujo de worktree aislado, esta vez en 3 fases sin gates intermedios (`../guia-proc-U5/`, rama `proc-u5-wip`, candidato `39cdd77`). U5: 10 páginas (52-61), 46 actividades, 4 cuadros, autoevaluación p61; vocab 4 principales (Partes de la casa, Mobiliario, Adjetivos descriptivos, Tipos de vivienda) + 2 recurrentes (Marcadores de lugar…, Establecimientos…); gramática 2 principales (Posición, Oposición ser/estar) + 4 recurrentes; pron/orto sonido /x/; 6 lemas verbales; 14 entradas `_decisiones_ia`. Propagación registry: `tener` y `vivir` con U5:["PRE"] añadido (recurrentes con paradigma activo); _meta.version verbos 1.3→1.4. Decisión del autor: NO fusionar `Tipos de vivienda` + `Alojamiento` (opción A) — son canónicos conceptualmente distintos (vivienda permanente vs estancia temporal), `Alojamiento` queda disponible para unidades futuras de viajes/turismo. Validador U0-U5 → 0/0/0. Worktree y rama `proc-u5-wip` conservados. Deuda restante: U6-U9.
- **2026-05-15** — Saneamiento de `@R` en data — retirada de los 587 sufijos que infringen §6.5 (v10.138). Cierre operativo de la deuda señalada en v10.137. Aplicación literal de §6.5: `@R` solo válido en fuentes de actividades de los 5 tipos productivos (produccion_escrita_guiada, expresion_escrita_libre, expresion_oral_libre, interaccion_oral, tarea_final). Auditoría: 627 `@R` totales en U0-U4 (U0:12 · U1:150 · U2:120 · U3:152 · U4:193), de los cuales 587 estaban en tipos NO productivos (completa_huecos, responder_preguntas_cerradas, verdadero_falso, clasifica, relaciona, ordena, escucha_y_repite) o en refs a cuadros (donde @R está prohibido). Acción: retirados los 587 (U0:12 · U1:148 · U2:115 · U3:152 · U4:160); las 40 fuentes con `@R` en tipo productivo se mantienen. Forma: `pNN-actMM@R` → `pNN-actMM`, con deduplicación. Sin cambios en items, descripciones, decisiones_ia, listas tipadas ni registries. Compatibilidad con dashboard (v10.137) preservada: las 40 fuentes válidas siguen mostrando badge azul + tooltip + aria-label combinado. Segundo criterio de §6.5 (palabra únicamente en respuestas) no aplicado sobre las 40 preservadas — queda como saneamiento opcional posterior. Validador U0/U1/U2/U3/U4 → 0/0/0.
- **2026-05-15** — Dashboard: retirada del texto `·R` en badges + pista no-cromática preservada (v10.137). Ajuste cosmético en `web/index.html`. Sin tocar datos: los 587 sufijos `@R` en U0-U4 quedan intactos. Cambios: (1) retirado pseudo-elemento CSS `.item-card-source.source-respuesta::after { content: " ·R"; ... }`; (2) render JS (3 sitios, líneas 1173/1251/1329) ahora añade `title="Fuente ligada a respuesta"` + `aria-label="${lab}, fuente ligada a respuesta"` cuando `isR` es true. El aria-label combina el contenido del badge con la nota semántica (no solo la nota), por hallazgo del revisor — si dijera solo "Fuente ligada a respuesta", las tecnologías asistivas anunciarían únicamente esa frase y perderían el dato útil del badge (la referencia concreta `p21-act5`, etc.). Distinciones que cargan la semántica de `@R` ahora: color (clase CSS) + hover (title) + asistivas (aria-label combinado) + DOM (clase) + JSON (sufijo). Sin retrocompatibilidad rota. Conflicto de numeración: main avanzó a v10.136 (REDISEÑO fase 2 §4) durante este trabajo; este lote toma v10.137. Trazabilidad: el saneamiento de `@R` en data (159 bugs §6.5 detectados en U4 sobre tipos no productivos) queda pendiente como trabajo separado.
- **2026-05-15** — U4: fusión de compuestos léxicos al lema base (v10.135c). Continuación del saneamiento de U4 tras v10.135b. Hallazgo del autor: compuestos tipo `X de Y` / `X + adj` / `X + marca` no aportan separados cuando su base léxica ya está atestada. Regla: 23 compuestos fusionados al lema-cabeza (huevo frito→huevo, zumo de naranja/piña→zumo, hamburguesa Brookling→hamburguesa, ensalada mediterránea→ensalada, sopa de cocido madrileño→sopa, macarrones de Bolonia→macarrones, arroz a la cubana/con tomate/chino→arroz, pollo a la catalana/asado→pollo, filete a la plancha→filete, tarta de chocolate→tarta, chocolate caliente→chocolate, tomate frito/salsa de tomate→tomate, patatas fritas→patatas, atún con tomate→atún, harina de maíz→maíz, pasta con tomate→pasta, menú del día→menú, primer/segundo plato→plato, puestos callejeros→puesto). Compuestos léxicos fijos sin lema-cabeza atestado (frutos secos, perritos calientes, comedor escolar) se mantienen. Resultado: 119→96 items (Alimentos 60→50, Comidas preparadas 23→16, Restaurante 15→11, Bebida 11→9). Sin retroactivo en U1/U2/U3. Validador U4 → 0/0/0.
- **2026-05-15** — U4: colapso de flexiones sing/plural y masc/fem en `vocabulario_consolidado` (v10.135b). Hallazgo del autor: aparecían como items separados huevo/huevos, tomate/tomates, pizza/pizzas, italiano/italiana, peruano/peruana, etc. Regla aplicada: items del consolidado se colapsan al lema masc sing cuando dos o más flexiones del mismo lema están atestadas en la unidad. Fuentes fusionadas. Compuestos multi-palabra (huevo frito, zumo de naranja, pollo a la catalana, etc.) y formas únicas sin par (colombianas, patatas, brasileño, madrileño, etc.) se mantienen verbatim. Resultado: Alimentos 73→60, Comidas preparadas 26→23, Restaurante 18→15, Adjetivos de nacionalidad 16→10. Total 144→119 items (-25). Sin retroactivo en U1/U2/U3. Validador U4 → 0/0/0.
- **2026-05-15** — Migración de U4 a shape v10.117 + propagación U4 a 6 lemas verbales (v10.135). Quinta unidad re-procesada con el flujo de worktree aislado (`../guia-proc-U4/`, rama `proc-u4-wip`, candidato `09af577`, integración por copia controlada + reformateo). U4: 10 páginas (42-51), 49 actividades, 6 cuadros; vocab 2 principales (Alimentos, Comidas preparadas) + 3 recurrentes (Bebida, Restaurante, Adjetivos de nacionalidad); gramática 5 principales (Construcción gustar/doler, Pronombres átonos de OI, Artículos indeterminados, Nombres contables e incontables, Hay) + 2 recurrentes (Concordancia de género, Interrogativos); pron/orto Sonidos y correspondencias ortográficas; 8 lemas verbales (gustar, querer, comer, hacer, ser, tener, llamarse, ir); 10 entradas `_decisiones_ia`. Propagación al registry verbal: U4:["PRE"] añadido a comer/ser/tener/llamarse (recurrentes activados) + hacer/ir (excepción §5.2 anticipación aprobada, mismo patrón U2/U3); _meta.version 1.2→1.3. Decisiones del autor sobre propuestas §0.1: (1) Bebida vs Bebidas → se mantiene `Bebida` literal del registry; (2) Artículos indeterminados → verificado U1/U2/U3 no la trabajan canónicamente, U4 es primera aparición correcta; (3) `salir` anticipación a U7 documentada sin acción. Validador U0/U1/U2/U3/U4 → 0/0/0. Cambio de flujo a partir de U5: desde U5 el coordinador extrae directamente desde el chat sin worktree ni chat aparte; memoria guardada en `feedback_extraccion_directa_post_u4.md`. Deuda restante: U5-U9 con shape legacy pendientes.
- **2026-05-15** — Sincronización de P-decisiones tras retirada de "Nombres propios" (v10.134). Hallazgo del revisor: tras v10.131 (retirada del canon `Nombres propios`), las entradas antiguas P4 (U1) y P3 (U2) seguían afirmando la reasignación/ampliación de la categoría — contradicción interna con la nueva P-Nombres-propios-retirada que coexistía en el mismo `_decisiones_ia`. Cambios: P4 de U1 reescrita (Nombre ya no se codifica en ningún bloque canónico; la reasignación intermedia a gramatica_consolidada queda invalidada por v10.131); P3 de U2 reescrita (la ampliación a antropónimos+topónimos queda sin efecto). Ambas con cruce explícito a P-Nombres-propios-retirada. Sin cambios en JSONs estructurales ni registries. Validador U0/U1/U2/U3 → 0/0/0.
- **2026-05-15** — Retirada del canon de "Nombres propios" gramatical (v10.131). Dictamen editorial del autor: la categoría `Nombres propios` añadida al canon gramatical en v10.123 (antropónimos U1) y ampliada en v10.128 (topónimos U2) se retira por completo. El libro NC1 no trabaja pedagógicamente la categoría como contenido — los antropónimos y topónimos aparecen en textos verbatim pero el manual no enseña una regla gramatical específica sobre nombres propios (la regla "sin-artículo determinado" es inferencia, no contenido didáctico expuesto). Aplicarla cada vez que aparece un personaje convertía la categoría en comodín pedagógicamente vacío. Cambios: entrada eliminada del registry (18→17 categorías, _meta.version 1.2→1.3); bloque retirado de gramatica_consolidada en U1 (principal), U2 (principal) y U3 (recurrente); 26+21+29=76 referencias retiradas de actividad/cuadro.gramatica; nueva entrada en `_decisiones_ia` de cada unidad documentando la retirada. NO se restaura `Nombre` en vocabulario (la decisión v10.123 de no codificar antropónimos como léxico se mantiene). Los nombres propios siguen verbatim en los textos del libro; no se consolidan en ningún bloque canónico. Revierte parcialmente v10.123/v10.123b/v10.128. Validador U0/U1/U2/U3 → 0/0/0.
- **2026-05-15** — Dashboard: respuestas sin numeración duplicada cuando el libro ya las numera (v10.130). Hallazgo del autor: en respuestas con prefijo numérico del libro (`"1 brasileña"`, `"2 colombiana"`...), el `<ol>` del dashboard añadía su propia numeración → duplicación visual `"1. 1 brasileña"`. Solución en `web/index.html`: detección heurística (≥50% de respuestas con `^\d+[\s.)]`) → render como lista plana sin numeración automática; resto mantiene `<ol>`. JSON intacto (regla crítica 1: verbatim del libro).
- **2026-05-15** — Migración de U3 a shape v10.117 + canonización "La hora" + nuevo lema "interesar" + propagación U3 a 7 lemas verbales (v10.129). Cuarta unidad re-procesada con el flujo de worktree aislado (`../guia-proc-U3/`, rama `proc-u3-wip`, candidato `98b68d4`, integración por copia controlada + reformateo). U3: 10 páginas (32-41), 47 actividades, 5 cuadros, vocab 1 principal (Parientes) + 9 recurrentes, gramática 3 principales (Interrogativos, Posesivos, Pronombre sujeto) + 4 recurrentes, pron/orto Sonidos y correspondencias ortográficas (fonema /θ/), 15 lemas verbales, 13 entradas `_decisiones_ia`, §5.2 metalengua aplicada sistemáticamente. Decisiones del autor sobre propuestas §0.1 abiertas: (1B) excepción §5.2 anticipación para jugar/venir/hacer conservados (mismo patrón que ir/estar/gustar/hablar en U2); (2) canonización de "La hora" como nueva categoría léxica (origen: excepcion, PCIC A1 no tiene entrada dedicada; total 101→102, _meta.version 1.3→1.4); (3) "interesar" añadido como lema independiente al registry verbal (tipo gustar, apariciones.U3:["PRE"]; total 48→49 verbos, _meta.version 1.1→1.2; en U3 separado de gustar en p40-act1 y como entrada propia en consolidado). Propagación U3:["PRE"] a 7 lemas existentes: tener/llamarse/estar/gustar (recurrentes) + jugar/venir/hacer (excepción §5.2). Validador U0/U1/U2/U3 → 0/0/0. Worktree y rama `proc-u3-wip` conservados (pendiente eliminarlos). Deuda restante: U4-U9 con shape legacy pendientes.
- **2026-05-15** — Dashboard: render legible de arrays de objetos en `datos` (v10.128e). Hallazgo del autor revisando U2: bloques tipo `textos_personajes` se renderizaban como JSON crudo (`{"personaje":"PROFESORA","texto":"Hola..."}`) en lugar de diálogo legible. Causa: el fallback genérico de `renderDato` en `web/index.html` hacía `JSON.stringify` sin detectar la shape. Solución: 5 detectores de shape antes del fallback genérico — diálogos `{personaje,texto}`, pares `{informal,formal}`, fichas `{ficha|numero,campos[]}`, quiz `{pregunta,opciones[]}`, y fallback mejorado a pares clave:valor. Aplica retroactivamente a todos los inventarios. Sin cambios en `diagrama.py` ni JSONs.
- **2026-05-15** — Retirada del "Caso histórico" en la regla de metalengua de instrucción (v10.128d). Micro-fix por dictamen del autor: la regla añadida en v10.128c incluía un párrafo final con referencias a P-decisiones de unidades concretas (P4-revisada U0, P1 U1, P4 U2). Narrativa retrospectiva no encaja en un contrato operativo prescriptivo. Retirado el párrafo; la regla queda solo descriptiva/prescriptiva, coherente con el resto de §5.2. Sin cambios materiales en la regla. Sin cambios en JSONs. Validador U0/U1/U2 → 0/0/0.
- **2026-05-15** — Formalización de la regla "metalengua de instrucción no es fuente de codificación" en §5.2 (v10.128c). Deuda doctrinal detectada por el autor: la regla se había aplicado al menos cuatro veces (P4-revisada U0, P1 U1, P4 U2, y la propia revisión actual sobre `ir`) sin estar formalizada en ningún contrato vivo. Búsqueda exhaustiva confirmada en `prompt.md`, `schema-inventario.md`, `reglas-operativas.md`, `convenciones-y-casos.md`, `glosario.md` y `CLAUDE.md`: solo `convenciones-y-casos.md:125` mencionaba metalengua pero referida a marcadores editoriales, no a verbos. Añadida subsección "Regla de exclusión por metalengua de instrucción" tras las reglas de anticipación y casos típicos en `reglas-operativas.md §5.2`: declara que las formas verbales que aparecen únicamente en el enunciado/instrucción del libro NO entran ni en `tiempos_y_verbos[]` ni propagan apariciones en `verbos-canonicos.json`; aplica a cualquier lema con independencia del modo; solo se codifican formas del contenido didáctico (diálogos, textos, modelos, audios, ejercicios, items_libro, respuestas modelo); incluye lista provisional de 25 verbos imperativos típicos de enunciado en NC1; caso ambiguo (mismo lema en enunciado y contenido) → §0.1; casos históricos documentados (U0/U1/U2 con `ir` en U2 codificado solo por contenido real). Sin cambios en JSONs de unidades — la regla codifica lo ya aplicado. Validador U0/U1/U2 → 0/0/0. Beneficio: U3-U9 podrán citar §5.2 directamente sin redescubrir la regla.
- **2026-05-15** — Micro-fix de coherencia tras v10.128 (v10.128b). Hallazgo del autor sobre `vocabulario_consolidado.recurrente` de U2: (1) `Nacionalidad` (canónico PCIC A1 §1.7) eliminado — el agente lo justificaba en "ya canónica en U1" (falso: U1 trabaja `Adjetivos de nacionalidad` y `Gentilicios`, no `Nacionalidad`); falso amigo entre dos entradas del registry léxico que cubren material parcialmente solapado pero pertenecen a dimensiones distintas (origen indice vs origen pcic_a1); la materia identitaria queda cubierta sin pérdida por los principales `Adjetivos de nacionalidad` y `Países hispanohablantes`; eliminadas 6 referencias en `actividad.vocabulario`/`cuadro.vocabulario`. (2) `Centros e instituciones educativas` — descripción reescrita: el agente decía "Recurrente: U2 introduce intensivamente" (contradicción interna); verificación mostró que la categoría no aparece como vocabulario canónico en ninguna unidad U0-U9 y vive principalmente como soporte de la sección `cultura` ('La ESO en España'); descripción ahora justifica correctamente la clasificación por el criterio (b) de §5.1.1 ("no forma parte del principal declarado en unidades posteriores pero es relevante para comprender textos y ejercicios de esta unidad"). Nueva entrada P14 en `_decisiones_ia`. Sin cambios en registries (§5.1.1 ya contempla este caso). Validador U0/U1/U2 → 0/0/0.
- **2026-05-15** — Migración de U2 a shape v10.117 + ampliación "Nombres propios" a topónimos + U2 en apariciones verbales (v10.128). Tercera unidad re-procesada con el flujo de worktree aislado (`../guia-proc-U2/`, rama `proc-u2-wip`, agente dry-run, commit candidato `dd6f8af`, integración por copia controlada + reformateo canónico vía `format_inventario.py` + actualización coordinada de dos registries). U2: 10 páginas (22-31), 52 actividades, 4 cuadros, vocab 6 principales + 4 recurrentes, gramática 3 principales + 4 recurrentes, pron/orto Vocales y Mayúsculas, 9 lemas verbales (ser/tener/llamarse/vivir/estudiar/ir/estar/gustar/hablar), 8 entradas en `_decisiones_ia`. Cierre de P3: `Nombres propios` en `gramatica-canonica.json` ampliado de `["antropónimos"]` a `["antropónimos","topónimos"]` (retirada la cláusula diferidora introducida en v10.123b; materializa la canonización pendiente vinculada a "Países hispanohablantes" anticipada en U2); `_pcic_ref`, `_apariciones.U2` y `_nota` actualizados con anclajes pedagógicos (antropónimos con `llamarse`; topónimos con `ser + de + topónimo`); _meta.version 1.1→1.2. Cierre de P4: 7 lemmas con apariciones nuevas U2:["PRE"] en `verbos-canonicos.json` — los 6 reportados por el agente (`vivir`, `estudiar`, `ir`, `estar`, `gustar`, `hablar`) + `llamarse` que el agente omitió (registry solo tenía U1, materialmente aparece en U2); _meta.version 1.0→1.1. Notas: audio 30 sin transcripción (§6.6) y léxico heterogéneo en p30-act3 (§5.7) gestionados sin marca bloqueante. Validador U0/U1/U2 → 0/0/0. Worktree y rama `proc-u2-wip` conservados tras integración (pendiente eliminarlos). Deuda restante: U3-U9 con shape legacy pendientes de migración.
- **2026-05-15** — Dashboard: diagramas auto-actualizados desde filesystem + paleta cat-tag + leyenda HTML (v10.127). Saneamiento del dashboard tras dictamen del autor sobre los mermaids antiguos (paleta brillante saturada que chocaba con MD3 oliva-crema; contenido hardcoded; intento minimalista que dejó la leyenda como subgraph rompiendo el layout). Cambios: puerto 8080→8081; cuatro funciones `discover_*` que leen el filesystem en cada hit (fases con carpeta, registries, scripts, inventarios); `mermaid_level1/2/3` reescritos con auto-discovery + paleta `CHIPS` (los 8 tonos `.cat-tag` reales del proyecto) + shape redondeado `("...")` + tipografía Inter 13px; convención estable de tonos por rol (azul fuente · lila spec · menta IA · verde validado · amarillo código · melocoton humano · gris sin estado); leyenda sacada del mermaid y renderizada como chips HTML en `web/index.html` (clases `.cat-tag.p-<tone>` ya existentes en CSS, sin duplicar paleta); `build_diagrams_json` expone `legends` por diagrama; `renderDiagram(k)` popula la tira de chips antes del SVG. Pendientes: `mermaid_level4` (estado por unidad), `mermaid_database`, y estado por fase con detalle (en curso/pausada/pendiente/cerrada) → siguiente iteración. Colisión de numeración: v10.126 fue usado mientras trabajaba por un commit paralelo de fase 2, este lote toma v10.127.
- **2026-05-15** — Creación del nuevo `REDISEÑO-EN-CURSO.md` de fase 2 bajo modelo IA-first (v10.126). Primer paso del rediseño de fase 2. Mismo patrón que fase 1 (reglas/convenciones): el viejo se renombra a `REDISEÑO-EN-CURSO-viejo.md` (reservorio histórico) y el nuevo nace con el nombre canónico, en construcción paso a paso conforme el autor indica decisiones. §1 cerrado en este lote — Modelo de trabajo: granularidad por unidad; ciclo Chat A (fase 1, prompt-dry-run) → Chat B (fase 2, prompt envoltorio pendiente) → integración a main; ambos chats en el mismo worktree aislado; arquitectura interna híbrida (Capa 1 script + Capa 2 sesión IA) heredada del viejo; hitos cross-unidad: por unidad + cada 3 unidades + global final; régimen temporal dual (manual ahora / agentic en futuro) con mismos contratos; alcance NC1. Refs actualizadas en docs vivos (CLAUDE fase 2, gramatica-canonica.json _meta, PROCESO-MAESTRO árbol). Cabecera REVIEW sincronizada. Próximos pasos del rediseño los indicará el autor uno por uno (prompt envoltorio fase 2, política de hitos exacta, cierre P1, R1-R5, §8, persistencia de propuestas decisionales).
- **2026-05-15** — Blindaje del serializador de inventarios + reformateo canónico de U0/U1 (v10.125). Cierre de deuda técnica detectada por el revisor tras v10.124b: el enfoque post-regex de los scripts ad-hoc de v10.123/v10.124 tenía un bug latente que pegaba items consecutivos en una sola línea, manifestado dos veces (cerrado ad-hoc en v10.123b y v10.124b). Solución estructural: nuevo `scripts/format_inventario.py` con serialización recursiva tree-aware (decide inline vs multi-línea según el shape del nodo, no por regex) + sanity check de round-trip estructural antes de escribir + modo CLI. Convención operativa para U2-U9: cualquier script que modifique inventarios debe usar `format_inventory_json` de este módulo. Reformateado U0 y U1 con el serializador nuevo (diff cosmético, contenido idéntico). Validador U0/U1 → 0/0/0.
- **2026-05-15** — Micro-fix de coherencia tras v10.124 (v10.124b). Dos hallazgos del revisor: (1) contradicción interna en la trazabilidad — `Adjetivos de nacionalidad.descripcion.U1` y la entrada P3 de `_decisiones_ia` seguían afirmando que la categoría se trabaja en p14-act4, pero v10.124 retiró la canon ref de la actividad por reclasificarla como morfología pura; ambos textos reescritos para retirar p14-act4 de los "sitios de trabajo léxico" y aclarar que los items genuinos en sus respuestas se conservan por aparición material, no por trabajo pedagógicamente dirigido; P3 marcada como parcialmente revisada en v10.124 con cruce a P13; (2) regresión cosmética — el serializador custom de v10.124 reintrodujo el bug de items multi-objeto en una línea que v10.123b había cerrado; reaplicado el separador (18 líneas reformateadas). Validador U0/U1 → 0/0/0. v10.124 queda aprobado "con reservas" cerradas aquí.
- **2026-05-15** — Saneamiento de "Adjetivos de nacionalidad" en U1 + nueva categoría "Gentilicios" (v10.124). Hallazgo del autor: el bloque incluía `campeón`/`campeona` (sustantivos comunes confundidos por el agente con gentilicios al ver el patrón morfológico `-ón/-ona` en p14-act4 — tabla de moción de género mezclando profesor/niño/amiga/campeón/griega/alemán/brasileño), `mexicanas` (plural redundante con `mexicana`) y `madrileña`/`africana` (gentilicios no de país: ciudad y continente). Cambios: (1) registry léxico con nueva categoría `Gentilicios` (origen: excepcion, total 100→101, _meta.version 1.2→1.3) y `nota` explicando la distinción nacionalidad/país vs gentilicio regional/continental; (2) U1.vocabulario_consolidado.recurrente: eliminados campeón/campeona/mexicanas, movidos madrileña/africana al nuevo bloque `Gentilicios` con descripción del uso; (3) reclasificación de p14-act4: retirado "Adjetivos de nacionalidad" de su vocabulario — la actividad enseña morfología de género, no léxico-nacional; los gentilicios genuinos que aparecen (griego/-a, alemán/-a, brasileño/-a) se conservan en el bloque consolidado por aparición material en respuestas; (4) añadido "Gentilicios" al vocabulario de p12-act1 (madrileña en autodescripción de Katrina), p12-act2 (modelo de presentación) y p15-act6 (africana en respuestas); (5) nueva entrada P13 en `_decisiones_ia` de U1 documentando el saneamiento. Validador U0/U1 → 0/0/0.
- **2026-05-15** — Micro-fix de coherencia tras v10.123 (v10.123b). Tres ajustes detectados por el revisor: (1) P4 en `_decisiones_ia` de U1 reescrita para reflejar la nueva decisión real (Edad+Número de teléfono en vocabulario, Nombre reasignado a gramática) — antes contradecía materialmente el cambio aplicado; (2) `Nombres propios` en `gramatica-canonica.json` con alcance restringido a `items: ["antropónimos"]` + `_pcic_ref` y `_nota` aclarando que los topónimos quedan diferidos hasta que una unidad los canonice materialmente (probablemente U2 con países hispanohablantes); (3) fix cosmético del serializador custom de v10.123 que había pegado múltiples objetos `items` en una sola línea — separados a una línea por objeto con indentación correcta. Validador U0/U1 → 0/0/0. Commit v10.123 queda aprobado "con reservas" cerradas en este v10.123b.
- **2026-05-15** — Reasignación editorial de "Nombre" en U1: vocabulario → gramática "Nombres propios" (v10.123). Hallazgo del autor sobre el inventario U1 v10.121: la entrada `Nombre` en `vocabulario_consolidado.principal` no es un campo léxico genuino — los nombres propios concretos (David, Javier, María...) son antropónimos que no se enseñan uno a uno. Su valor pedagógico es **gramatical**: regla "nombres propios sin artículo determinado en español" + estructura con verbo `llamarse`. Decisión: mover a `gramatica_consolidada.principal` como nuevo canónico `Nombres propios`; Edad y Número de teléfono se quedan en vocabulario (sí tienen items rastreables). Búsqueda en PCIC A1 confirmó que `§1.1.1 Nombres propios` existe explícitamente (antropónimos + topónimos); registry `gramatica-canonica.json` no la tenía → añadida como categoría 18 con `origen: pcic_a1`. En U1: bloque `Nombre` retirado del vocabulario consolidado y reinsertado como `Nombres propios` en gramática consolidada con descripción específica del uso (regla sin-artículo, anclaje en cuadros@p14#1 y @p20#1 tú/usted); 15 actividades + 2 cuadros con `vocabulario: [...,"Nombre",...]` actualizados (Nombre retirado de vocabulario, Nombres propios añadido al inicio de gramatica); las 5 referencias literales `"Nombre"` dentro de `datos.fichas_modelo[*].campos` conservadas intactas por ser rótulos verbatim del libro. Reformat colateral del JSON U1 por uso de script con serializador custom (diff grande ≈757+/803-, contenido idéntico salvo la reasignación). Validador U0/U1 → 0/0/0. Topónimos diferidos a fase 2 / unidades posteriores (anticipación documentada). Decisión consistente con el principio §0.0 de literalidad y con la convención de auditoría "cita canónica vs mención temática" de CLAUDE.md fase 1.
- **2026-05-15** — Retirada de la fixture `unidades/U1-propuesta/` (v10.122). Cumplió su función diagnóstica en el rediseño v10.111-v10.114 (estresar el shape nuevo antes de tener inventarios canónicos en shape v10.117); con U1 canónica ya migrada en v10.121 (0/0/0), la fixture queda obsoleta. Estaba además en shape legacy pre-v10.115 (sin `_fixture_exploratoria`, sin los 3 bloques top-level nuevos), por lo que mantenerla requeriría re-migración sin valor. Misma decisión que con U2p/U3p en v10.113. Limpieza colateral: línea del árbol de carpetas de `PROCESO-MAESTRO.md` retirada. Referencias genéricas a variantes "Np" en `diagrama.py` y `web/index.html` conservadas (sirven a fixtures futuras). Referencias históricas en CHANGELOG/REVIEW conservadas (bitácora válida).
- **2026-05-15** — Migración de U1 a shape v10.117 + canonización "Números cardinales" con rename retroactivo en U0 (v10.121). Segunda unidad re-procesada con el flujo de worktree aislado (`../guia-proc-U1/`, rama `proc-u1-wip`, agente dry-run en chat limpio, commit candidato `6132896`, integración a main por copia controlada). U1: 10 páginas (12-21), 42 actividades, 7 cuadros; vocab principal 8 canónicos (Objetos de clase, Nombres de las letras, Nombre, Edad, Número de teléfono, Colores, Números cardinales, Saludos y despedidas) + recurrente 2 (Adjetivos de nacionalidad, Para la clase); gramática 4 principal (Artículos determinados, Concordancia de género, Pronombre sujeto, Concordancia de número) + 1 recurrente (Interrogativos); pron/orto 2 principal (Entonación, Signos de puntuación) + 1 recurrente; tres lemas verbales (ser, llamarse, tener); 13 entradas en `_decisiones_ia` con P1-P12 resueltas + anticipaciones fase 2. Las 6 deudas legacy reportadas por el agente eran la misma causa (referencias a `Números cardinales` no canónico). Cierre por dictamen editorial: rename del canónico `Números 0-10` → `Números cardinales` (origen: indice, aliases_indice: ["Números 0-10", "Números 1-20", "Números 21-100"]) en `campos-semanticos-canonicos.json` (version 1.1 → 1.2; total entradas se mantiene en 100); retoque retroactivo en `unidades/U0/U0-nc1-inventario.json` (rename de la clave `vocabulario_consolidado.principal."Números 0-10"` y de la referencia `cuadro@p11.vocabulario`). Edición poscopia del candidato U1: P10 de `_decisiones_ia` reescrita reflejando el cierre coordinador (patrón aplicado igual que con P1(b) en U0/v10.119). Deuda §A.3 declarada para saneamiento posterior (no bloquea): `verbos-canonicos.json` (`leer.apariciones.U1`), `gramatica-canonica.json` (`Interrogativos._apariciones` falta U1), `pronunciacion-ortografia-canonica.json` (`Signos de puntuación → _apariciones.U1` con material débil P8). Validador final: U0 → 0/0/0 · U1 → 0/0/0. Worktree y rama `proc-u1-wip` eliminados tras integración limpia. Conflicto de numeración detectado durante la integración: main avanzó con v10.120 y v10.120b (fase 2) mientras procesábamos U1, por lo que esta entrada toma el número v10.121. Deuda restante en el bloque de re-extracción: U2-U9 con shape legacy todavía pendientes de migración mediante el mismo flujo.
- **2026-05-15** — Limpieza documental de fase 2 tras rediseño de fase 1 (v10.120). Sin cambios operativos en fase 2 (sigue pausada por decisión 36 v10.108). Drift declarado explícitamente: CLAUDE.md fase 2 regla 1 retira `campo_semantico` (eliminado v10.115) y sustituye por refs a los 4 bloques top-level consolidados + 4 listas tipadas; banner "Estado actual" añadido al inicio; reglas-reciclaje.md con cláusula transitoria; scripts `regenerar_reciclaje_vocabulario.py` y `regenerar_reciclaje_mapa.py` con banners de docstring declarando shape v10.114 asumido. Reactivación condicionada a U1-U9 migradas al shape v10.117 (U0 ya en v10.119) + adaptación scripts + cierre P1 REDISEÑO + regeneración íntegra `nc1-reciclaje.json`.
- **2026-05-15** — Migración de U0 a shape v10.117 + nuevo canónico léxico "Saludos y despedidas" (v10.119). Primera unidad re-procesada con el flujo de worktree aislado (`../guia-proc-U0/`, rama `proc-u0-wip`, agente dry-run en chat limpio, commit candidato 485f9e5, integración a main por copia controlada). U0 reemplaza el JSON legacy (46 errores estructurales: faltaban 3 bloques top-level consolidados, faltaban las 4 listas tipadas por actividad, enfoque `fonetica` ya no en enum) por el candidato del worktree con shape canónico completo (paginas 8-11, 10 actividades, 1 cuadro, `_nota_unidad_atipica` por §7 reglas, 11 entradas en `_decisiones_ia` con P1-P10 resueltas + anticipaciones para fase 2). 4 de las 5 deudas legacy se cerraron por remapeo del agente a canónicos vigentes (Abecedario→Abecedario español, Números→Números 0-10, Instrucciones de aula→Para la clase, "Vocabulario internacional (cognados)" absorbido por shape sin recurrente). 5ª deuda resuelta por dictamen editorial: nuevo canónico **"Saludos y despedidas"** (origen: excepcion, aliases_indice: ["Saludos"]) añadido a `campos-semanticos-canonicos.json` (99 → 100 entradas, version 1.0 → 1.1). En el JSON de U0: clave del bloque y referencia del cuadro p11 usan el canónico nuevo; el campo `titulo: "Saludos"` del cuadro se mantiene verbatim del libro. Validador final: **0 errores · 0 avisos · 0 legacy** sobre U0. Worktree `../guia-proc-U0/` y rama `proc-u0-wip` se conservan tras la integración para auditoría/trazabilidad; pendiente eliminarlos cuando el autor lo autorice. Deuda restante en el bloque de re-extracción: U1-U9 con shape legacy v10.114 todavía pendientes de migración (input esperado para próximos lotes).
- **2026-05-15** — Auditoría completa de fase 1 + cierre de las 3 deudas activas (v10.118). Auditoría sistemática 11 pasos (baseline, coherencia estructural, refs §X.Y, single source of truth, docs↔registries, registries↔PCIC, registries↔nc1-curso, CHANGELOG/REVIEW, docs raíz, gaps funcionales). Saneamiento de drift en zonas vivas: prompt.md (nota transitoria validador acotada), CLAUDE.md (reglas críticas 4/5/6 con cláusulas transitorias + convención de auditoría cita canónica vs mención temática), reglas-operativas.md (nota huecos numeración + nueva §0.0 principio de literalidad + §5.7 heterogeneidad semántica), schema-inventario.md (A.4↔A.5 reordenados + enum tiempo reformulado), glosario.md (ejemplos canónicos corregidos + tabla enums 4 valores), convenciones-y-casos.md (refs §0.0 explícitas), PROCESO-MAESTRO.md (4 desincronizaciones zonas vivas + árbol expandido), registries (`_meta` añadido a léxico y verbos para paridad + `_pcic_ref` por categoría en pron-orto + Colores añadido al léxico con origen excepcion). Cierre de las 3 deudas activas que quedaban tras v10.117: (1) heterogeneidad semántica → §5.7; (2) `validar_inventario.py` refactorizado al shape v10.117 (4 bloques top-level, 4 listas tipadas, enum tiempo de 4 valores, tipo_cuadro/enfoque renombrados, tiempos_y_verbos shape, estructura_perifrastica, tolerancia _fixture_*, chequeo @R por tipo); (3) `scripts/verificar_integridad.py` nuevo con los 9 chequeos de schema §A.3 (incluido propagación de auditoría legacy R1). Deuda restante única: re-extracción de U0-U9 con shape v10.117 (input esperado para el próximo lote de procesamiento de unidades, abordable desde otro chat).
- **2026-05-15** — Materialización de registries gramatical y pron/ortografía (v10.117). Cierre de deuda heredada desde v10.111: `gramatica-canonica.json` y `pronunciacion-ortografia-canonica.json` dejan de ser esqueletos vacíos. Pron/orto poblado con 7 categorías canónicas (volcado directo de pieza 4 §16.2 del REDISEÑO archivado). Gramática poblado con 17 categorías aplicando pieza 3 §14.0 (scaffold + PCIC): 15 entradas gramaticales puras de la matriz §14.1 + Pronombre sujeto (PCIC §7.1.1) + Pronombres átonos de OI (PCIC §7.1.3 serie completa). Tras dictamen del revisor se retiraron 3 sobreextensiones (conjunciones copulativas, conjunciones disyuntivas, adverbios afirm/neg): son uso pragmático no enseñado, no van al canon de fase 1. Su tratamiento se traslada como responsabilidad analítica a fase 2: nueva §8 en `fases/2-reciclaje/REDISEÑO-EN-CURSO.md` documenta la política ("siempre presentes no indexados"), lista inicial de 3 candidatos y reglas operativas. Coherencia interna corregida: `_pcic_ref` de Pronombres átonos de OI ajustado de "serie me, te, le" a "serie completa me/te/le/nos/os/les" para reflejar la cobertura material. Sin cambios en docs operativos (schema, reglas, glosario, convenciones).
- **2026-05-15** — Reset de `convenciones-y-casos.md` bajo modelo IA-first (v10.116). Cierre del lote de rediseño de fase 1 (continuación de v10.115). Archivo reescrito desde cero como tercera capa viva de soporte con **precedencia explícita schema > reglas > convenciones**. §0 nuevo (naturaleza + precedencia), §1 convenciones de transcripción reordenadas y depuradas (§1.1 literalidad, §1.2 primer ítem resuelto, §1.3 textos con tabla decisional, §1.4 sopas, §1.5 marcadores editoriales), §2 ejemplos canónicos por tipo, §3 unidad atípica (autoridad en reglas §7), §4 casebook con §4.3 pendientes de shape v10.115. Cambios: §1.7 vieja absorbida en reglas §2.3; §5 vieja absorbida en reglas §10; §1.1 vieja (sílaba tónica + `datos._nota`) eliminada por decisión del autor; §1.8 vieja (`columnas_relaciona`) reubicada en §4.3.1 pendiente. Cascada documental en `prompt.md`, `CLAUDE.md` fase 1, `reglas-operativas.md` y `schema-inventario.md` con jerarquía explícita en conflicto. Archivado de `convenciones-y-casos-viejo.md` y `reglas-operativas-viejo.md` a `docs/historico/` con mapeos de absorción. Coherencia con glosario: cobertura verificada vía principio "saco abierto" + 1 puntero selectivo desde `datos` hacia §1.3. Verificación atómica de refs: cero rotas; las menciones a los `-viejo` en CHANGELOG son bitácora histórica preservada. Validación material: §1.1-§1.4 con caso material en U0/U1/U3/U4; §1.3.3 `textos_personajes` y §1.5 marcadores editoriales conservadas en §1 sin caso material en shape v10.115 por valor preventivo (anclaje histórico U5). Lote de rediseño de fase 1 cerrado. Próximo: convenciones de fase 2 (reciclaje) cuando se reactive.
- **2026-05-14** — Reset de `reglas-operativas.md` bajo modelo IA-first + refinamientos derivados de la prueba (v10.115). Hito de cierre del lote de rediseño operativo de fase 1. Archivo reescrito desde cero como autoridad operativa única (§0 política transversal, §1-§10 cuerpo, banner reducido a 2 follow-ups vivos). Familia `@R` realineada a 5 tipos productivos (no 6) en reglas/schema/glosario. §5.6 acotado a léxico con exclusiones explícitas para los 3 bloques no-léxicos. `reglas-operativas-viejo.md` congelado como reservorio histórico. Schema §A.3 depurado del falso pendiente de Regla 11. `prompt.md` y `CLAUDE.md` de fase 1 saneados para reflejar la deuda transitoria de `convenciones-y-casos.md`. **Prueba de certificación sobre U4-propuesta pp.42-46** (worktree paralelo, chat limpio): 9 ejes discriminativos aprobados, bug crítico de `@R` mecánico eliminado por endurecimiento §6.5 (chequeo previo del `tipo`). **Opción B para perífrasis** gatillada por defecto de salida: enum `tiempo` queda con 4 valores (`Perífrasis` retirado por no ser tiempo), añadido campo opcional `estructura_perifrastica` en `actividad.tiempos_y_verbos[]`, infinitivo complemento queda implícito y no se registra como entrada separada. **§5.2 con regla de anticipación por lema**: un verbo entra solo si su lema es canónico de la unidad actual o anterior; si es de unidad posterior, no entra aunque la forma sea visible. Resuelve asimetría `ir`/`hacer`. Fixture aprobada archivada en `docs/historico/pruebas/2026-05-14-prueba-aprobada-v10.115/`. `REDISEÑO-CONTENIDOS-LINGUISTICOS-EN-CURSO.md` archivado a `docs/historico/` tras absorberse todas sus piezas en los contratos vivos. Lote siguiente pendiente: reset de `convenciones-y-casos.md`.
- **2026-05-13** — Archivo de 2 documentos absorbidos en `docs/historico/` (v10.114): `schema-inventario-viejo.md` y `PROPUESTA-PIEZA-2-IA-FIRST.md`. `REDISEÑO-CONTENIDOS-LINGUISTICOS-EN-CURSO.md` se mantiene en fase 1. Referencias en `CLAUDE.md` de fase 1 actualizadas.
- **2026-05-13** — Retirada de fixtures U2p y U3p (v10.113). Tras cumplir su función diagnóstica y consolidar las lecciones en `reglas-operativas.md` §5.12-§5.14, se eliminan del repositorio. U1-propuesta se mantiene intacta con su banner especial en el dashboard. Sin cambios en contrato documental.
- **2026-05-13** — Rediseño fase 1: hito transitorio (v10.111 contrato documental + v10.112 dashboard y fixtures). **Schema NC1 cerrado en cuerpo (§1-§14); rediseño completo de fase 1 NO cerrado** mientras siga abierta la deuda del Apéndice transitorio §A.1-§A.5 (alineación validador, migración de reglas-operativas, registries por poblar, suite automatizada de verificación). Lo conseguido en este hito: cuerpo del schema cerrado, glosario creado, reglas §5.10-§5.14 integradas como obligatorias con sub-A/B/C por dimensión, prompt con nota transitoria de doble desalineación y pasos 3a/3b, 4 PCIC sources copiados, 3 fixtures exploratorias (U1p, U2p, U3p) visibles en dashboard, dashboard adaptado al shape nuevo (3 bugs de render fixed + mapping genérico Np). Próximo paso: completar U3-propuesta con D4-D6 surgidas (Asignaturas, Escuela, Profesiones-Lugares como recurrente) o pasar a Paso 2 (migración integral de reglas-operativas).
- **2026-05-08** — Borrado de `unidades/U3/U3-nc1-inventario.json` (v10.88). El PDF de U3 que se había usado para la extracción inicial contenía errores (deuda técnica anotada en `nc1-curso.json:_nota`). El autor proporciona PDF correcto (gitignored, reemplazado en disco) y borra el JSON viejo. Main temporalmente sin inventario de U3 hasta la re-extracción por ejecutor 2 en worktree paralelo `extract/U3`. Validador U0/U1/U2 → 0/0.
- **2026-05-08** — **Integración de U2 a main** (v10.87, merge `--no-ff --no-commit` desde `extract/U2`). Primera integración del carril paralelo de extracciones tras v10.79. Contenido: `unidades/U2/U2-nc1-inventario.json` (10 páginas, 52 actividades, 6 cuadros, validador 0/0). Hallazgos cerrados antes de integrar: 7 correcciones del extractor original + 5 refinamientos de regla (v10.83-v10.86) + verificación final de U2-p29-act04 contra PDF (`expresion_escrita`, no oral). Estado: U0/U1/U2/U3 trackeadas y validando 0/0. Worktree `extract/U2` y rama se mantienen intactos. Próximo: B1.5 (ejecutor 1) + re-extracción de U3 (ejecutor 2 cuando llegue PDF).
- **2026-05-08** — Refinamiento §2.2 regla 3: individual vs parejas en respuesta a preguntas (v10.86). Caso disparador: riesgo residual del revisor sobre v10.85. Si U2-p29-act04 es intercambio en parejas, cambia tipo (no solo destreza). Decisión: bifurcar regla 3 en 3 casos: cerradas / individual / parejas. Las preguntas a verificar contra PDF de U2-p29-act04 ahora son 2 (solo o pareja; escribe o habla). 1 bullet añadido, 1 modificado, sin bloat. Single source of truth en §2.2. Validador 0/0.
- **2026-05-08** — Refinamiento §2.3: `responder_preguntas_abiertas` con destreza condicional al enunciado (v10.85). Caso disparador: hallazgo C en U2-p29-act04. Antes la regla decía siempre `expresion_escrita`; ahora bifurca: `expresion_escrita` si el libro pide escribir, `expresion_oral` si pide responder oralmente. 1 línea modificada en §2.3, 0 párrafos añadidos. Sin cambios en JSONs. U2-p29-act04 concreto pendiente de verificación contra PDF. Validador 0/0.
- **2026-05-08** — Cierre limpio de v10.83: alineación de 2 referencias vivas (v10.83b). El revisor detectó que `prompt.md:54` y `convenciones-y-casos.md:141` seguían diciendo "Para aprender → siempre actividad" sin la bifurcación, contradiciendo v10.83 desde otros entry points. Fix: `prompt.md` reescrito para nombrar los criterios sin reducirlos (la regla canónica vive en reglas-operativas §4); `convenciones-y-casos.md` §4.1 reescrita para contextualizar el caso histórico (tenía verbo imperativo) y citar la bifurcación actual. Single source of truth restablecida. Validador 0/0.
- **2026-05-08** — Schema §4 (cuadros): `texto_intro` documentado + `titulo` nullable + `lista_reglas` (v10.84). Hallazgos del ejecutor 2 en U2: (1) cuadros lista_reglas con encabezado verbatim no documentado; (2) cuadro p29 sin título; (3) tipo `lista_reglas` no en lista de ejemplos. Cambios quirúrgicos en schema-inventario.md §4 (3 líneas modificadas, 1 nueva, 0 párrafos añadidos): `titulo` ahora `<str | null>`; `lista_reglas` añadido a ejemplos de tipo; nueva línea `texto_intro: <str opcional>`. Resuelve también el hallazgo A previo (cuadro sin título). Sin cambios funcionales (contenido es estructura libre). Validador 0/0.
- **2026-05-08** — Refinamiento de regla "Para aprender" tras hallazgo del ejecutor 2 en U2 (v10.83). Caso disparador: U2-p25 "Uso de las mayúsculas" es puramente informativo (lista de reglas, sin verbo imperativo al alumno) → no encajaba en regla "siempre actividad". Decisión cerrada: bifurcar por naturaleza (con tarea → actividad; solo informativa → cuadro). Cambios en reglas-operativas.md: §1 regla 2 reformulada como índice + puntero; §4 reescrita con tabla de bifurcación + criterio decisional + ejemplos canónicos. Eliminada duplicación previa entre §1 y §4. **Sin añadir contenido** (reformulación). Auditoría U0/U1/U3: sin reclasificaciones necesarias (U3-p37-act09 "Mira el cuaderno de Ronaldo... Escribe..." tiene verbo imperativo, sigue siendo actividad legítima). U1 tiene gap de "Recursos para la clase" no extraído; fix futuro de inventario. Validador 0/0.
- **2026-05-08** — **Refinamiento de B1.4 tras dictamen del revisor** (v10.82b). 2 hallazgos cerrados: (1) schema documentado vs JSON real no coincidían (`apendice` usa `seccion` no `titulo`; `_nota` top-level no estaba documentado) → decisión 35 y B1.4 actualizados al schema real; (2) la regla "no divergencia antes de cerrar" contradecía el repo (U3 paginación 32-41 vs 34-43; U1/U3 mezclan PARA APRENDER en gramática) → regla refinada para hacer `nc1-curso.json` canónico y permitir divergencias legítimas anotadas como deuda técnica en `_nota`. Causa identificada del U3 paginación: PDF actual tiene errores, será re-extraído por ejecutor 2. No se toca JSON ni PDF de U3 ahora. Sin cambios funcionales. Validador 0/0.
- **2026-05-08** — **B1.4 cerrado**: creado `unidades/nc1-curso.json` (v10.82, decisión 35 en PROCESO-MAESTRO Parte 4). Índice editorial global del curso, derivado del Scope and Sequence oficial del libro impreso (no de meta-documentación). 10 unidades + apéndice. Contenido literal sin expansión MCER. Schema cerrado. Hallazgos anotados (no bloqueantes): páginas en `viejo/00-curso-general.md` eran imprecisas; `contenidos_indice` de U1/U3 mezclan "PARA APRENDER" con gramática (fix futuro). Próximo: B1.5 (diseño de `nc1-reciclaje.json`) con `nc1-curso.json` ya disponible para mapear flujos de reciclaje.
- **2026-05-08** — Dashboard: badge "extracción en curso" para unidades de worktrees paralelos (v10.81). Las unidades servidas vía `EXTRA_UNIDADES_PATHS` (`zona: "extra"`) ahora muestran un badge ámbar *"🔄 Extracción en curso (worktree paralelo)"* en lugar del path absoluto. Para unidades de main (`zona: ""`) se mantiene el path relativo. Al integrar una unidad a main, el cambio es automático: main gana sobre extras y el badge desaparece. Validador 0/0.
- **2026-05-08** — Dashboard: eliminado el badge de versión (v10.80b). v10.80 había hecho el badge dinámico, pero seguían apareciendo 2 indicadores en el header. Decisión del autor: dejar solo el indicador derecho que ya muestra versión + hora viva. Eliminados el span del badge y el JS que lo actualizaba. Una sola fuente visible de versión. Validador 0/0.
- **2026-05-08** — Dashboard: badge de versión dinámico (v10.80). Tras v10.79 el header mostraba dos versiones distintas: badge verde con `v10.78` hardcoded (desde v10.78) y el indicador derecho dinámico `v10.79 — hh:mm:ss`. Fix: badge ahora con `id="version-badge"` que se rellena en `init()` desde `/api/version`. Una sola fuente de verdad. Sin cambios funcionales. Validador 0/0.
- **2026-05-11** — **Canon semántico activado en fase 1** (v10.108 + v10.108b + v10.108c, commits `46534c7` / `5e963cd` / este). Implementación cerrada de la decisión 36 (E-final). 6 archivos modificados (schema + reglas + validador + prompt + CLAUDE.md de fase 1 + ajuste menor en PROCESO-MAESTRO), 0 archivos doc nuevos. Validador con 3 canales en rollout R1; U0-U9 siguen 0/0 con auditoría legacy informativa pendiente de saneamiento (carril B, paso futuro). Caso negativo controlado verificado. Canon íntegro 98 entradas. Sincronización documental cerrada en v10.108c (autodocumentada).
- **2026-05-11** — **Decisión de diseño cerrada: canon semántico en fase 1 (propuesta E-final).** Tras 5 iteraciones revisor↔ejecutor y un giro arquitectónico, el canon de nombres permitidos para `campo_semantico` y claves de `vocabulario_consolidado` vive dentro de fase 1, integrado en sus documentos existentes sin crear archivos doc nuevos. Artefactos nuevos: `fases/1-extraccion-inventario/campos-semanticos-canonicos.json` (SoT de datos), `scripts/canon.py` (módulo compartido con 4 funciones: cargar, validar, escribir atómico, detectar pendientes), `scripts/inicializar_canon_semantico.py` (one-off poblando desde `nc1-curso.json` + subset PCIC A1). Modificaciones quirúrgicas en `CLAUDE.md`/`prompt.md`/`reglas-operativas.md`/`schema-inventario.md` de fase 1 + endurecimiento del validador con 3 canales (errores, avisos, **auditoría legacy**) + rollout R1/R2/R3 + carril A (extracción canónica desde origen) + carril B (saneamiento retrospectivo U0-U9). Dashboard solo lectura para cola de pendientes. Marca `_pendiente_canon` transitoria de worktree con triple defensa (schema, regla, validador) que bloquea cierre del inventario. Fase 2 **pausada** hasta cerrar canon. Implementación pendiente.
- **2026-05-11** — **U9 integrada a main** (v10.104) vía `integrar_unidad.py` (commit `ea4cb51`). 41 actividades, 6 cuadros, validador 0/0. Reciclaje sin cambios (92 mapa + 89 auto = 181) — U9 no introduce campos semánticos nuevos. **Curso completo extraído (U0-U9 integradas)**. Sincronización documental + autodocumentación en v10.104b.
- **2026-05-10** — Sincronización documental U8 (v10.103b) + autodocumentación + retirada de referencia a artefacto local (v10.103c).
- **2026-05-10** — **U8 integrada a main** (v10.103) vía `integrar_unidad.py` (commit `3f3e626`). 46 actividades, 5 cuadros, validador 0/0. Reciclaje actualizado: 181 hilos (92 mapa + 89 auto). Tercera integración limpia con el flujo automatizado.
- **2026-05-10** — **U7 integrada a main** (v10.102) vía `integrar_unidad.py` (commit `590c9f3`). 46 actividades, 4 cuadros, validador 0/0. Reciclaje actualizado: 171 hilos (92 mapa + 79 auto). Segunda integración limpia con el flujo automatizado, sin desviaciones.
- **2026-05-10** — Refuerzo del checklist de cierre en `fases/1-extraccion-inventario/prompt.md` (v10.101). Tras auditar los tres fixes de U6, análisis: reglas canónicas existen y son suficientes, fallos invisibles al validador. Añadidos 2 ítems al checklist (ejemplo no duplicado en respuestas; cardinalidad literal de items_libro). Sin cambios de reglas.
- **2026-05-10** — **U6 integrada a main** vía `integrar_unidad.py` (44 actividades, 4 cuadros, autoevaluación presente, validador 0/0). Primera integración con el flujo automatizado de v10.99. Reciclaje actualizado: 161 hilos totales (92 mapa + 69 auto). Tres fixes de auditoría del ejecutor 2 incluidos en el inventario final: ejemplo duplicado eliminado en `respuestas` de U6-p64-act01 y U6-p64-act02; ítem inventado #9 eliminado en U6-p65-act04. U6 usa `columnas_relaciona` (canónico desde v10.98) en U6-p63-act05, U6-p63-act08, U6-p68-act02 y U6-p71-act05.
- **2026-05-10** — Fase 2 reciclaje base automatizada + script de integración (v10.97-v10.99c). Fase 2 creada con contrato completo (`fases/2-reciclaje/CLAUDE.md` + `reglas-reciclaje.md`). Scripts: `regenerar_reciclaje_mapa.py` (nivel mapa desde `nc1-curso.json`, una vez) + `regenerar_reciclaje_vocabulario.py` (nivel auto, encadenado por `integrar_unidad.py`). Script `integrar_unidad.py` automatiza integración completa: copia, valida, actualiza reciclaje, commit aislado, con restauración de main en cualquier fallo. `nc1-reciclaje.json`: 92 hilos mapa + 59 auto = 151 hilos. U5 integrada (v10.97). Campo canónico `columnas_relaciona` añadido a fase 1 (v10.98), migración U1/U5.
- **2026-05-10** — Dashboard: `EXTRA_UNIDADES_PATHS` para servir worktrees paralelos (v10.79). Permite ver inventarios en working tree de extracciones en curso (ej. `extract/U2`) sin tener que integrarlos a main. Variable de entorno con paths separados por `:`. Main tiene prioridad; las unidades solo en extra se marcan `zona='extra'`. Refresco del navegador muestra cambios del worktree en tiempo real. Cuando la unidad se integra, automáticamente pasa a servirse desde main. Validador U0/U1/U3 → 0/0. Sin cambios en código de fase 1.
- **2026-05-08** — Dashboard: enfoque visible en inventarios + badge de versión (v10.78). Cambios pequeños alineados con el contrato post-refactor: badge `v10.78` en el header; campo `enfoque` añadido a cada actividad debajo de Destreza; renderizado correcto de `destreza` cuando es array (separador ` · `). Cambios en `web/index.html` solo. Sin afectar a worktrees de extracciones paralelas (los 5 archivos de fase 1 siguen idénticos). Necesario reiniciar `diagrama.py` desde el repo principal para que el dashboard sirva la versión nueva (el proceso anterior estaba sirviendo desde el worktree `extract/U2`).
- **2026-05-07 19:55** — Micro-fix de 2 imprecisiones tras dictamen del revisor sobre v10.77 (v10.77b, commit `becaa69`). (1) Timestamps con hora ficticia `14:00` corregidos al momento real del commit `40c8a4c` (19:20 +0200) en 4 sitios: REVIEW.md:9, REVIEW.md:434, PROCESO-MAESTRO.md:660, CHANGELOG.md:24. (2) Árbol vivo de PROCESO-MAESTRO.md:472 ampliado con `REVIEW-bloque-A-cerrado.md` que faltaba en el listado de `docs/historico/`. Sin cambios funcionales. Validador U0/U1/U3 → 0/0. Cierre real de la serie v10.72-v10.77b.
- **2026-05-07 19:20** — Compactación del Bloque A cerrado de REVIEW + 3 cabeceras desincronizadas (v10.77, commit `40c8a4c`). Última pieza de la serie de limpieza documental v10.72-v10.77. Aplicada la receta del revisor con sus 4 ajustes: (1) corte por encabezado `## Bloque A` hasta antes de `## Bloque B` (99 líneas, 8.022 chars) extraído literal a `docs/historico/REVIEW-bloque-A-cerrado.md` (111 líneas con cabecera); (2) sustitución en REVIEW vivo por resumen de 12 líneas con estado A1-A4, resultado vivo y referencias; (3) las 3 cabeceras desincronizadas absorbidas en este mismo commit (REVIEW.md:9, PROCESO-MAESTRO.md:448 y PROCESO-MAESTRO.md:630); (4) `PROCESO-MAESTRO.md:648` reformulado ("Detalle vivo y bitácora en REVIEW" → "Resumen vivo en REVIEW; detalle íntegro en docs/historico; bitácora cronológica general permanece en REVIEW"). REVIEW.md 580 → 493 líneas (-15%, ~2K tokens). Validador U0/U1/U3 → 0/0. Bitácora cronológica general intacta. Cierre de la serie. Ahorro acumulado v10.72-v10.77: ~32K tokens (-37%). Base limpia para extracciones paralelas en otros chats.
- **2026-05-07 13:45** — Cierre de drift vivo en PROCESO-MAESTRO tras dictamen del revisor sobre v10.76 (v10.76b). 3 zonas vivas seguían desactualizadas: (1) Decisión 17 Parte 4 con "17 valores" → 20 valores + mención explícita de los 3 ejes ortogonales tipo/destreza/enfoque; (2) Árbol actual Parte 3 con "U3 única poblada" → estado real con U0/U1/U3 todas validando 0/0, los 5 archivos vivos de fase 1 post-refactor, y `docs/historico/`; (3) Síntesis Parte 6 con "Bloque A pendiente (validar U3, probar U4, bugs B1-B4)" → Bloque A ✅ cerrado con detalle de A1-A4 y referencia a REVIEW. Bitácora del documento intacta (revisionismo prohibido). Validador U0/U1/U3 → 0/0. Próximo: v10.77 condicional con base documental viva limpia.
- **2026-05-07 13:30** — Archivado de árboles históricos y Parte 5.bis de PROCESO-MAESTRO (v10.76). Continuación de la limpieza documental tras v10.75. Tres bloques de PROCESO-MAESTRO.md auto-etiquetados como históricos en su propio contenido se trasladan a `docs/historico/`, dejando punteros cortos donde estaban: (1) Árbol intermedio (487-533, ~2.161 chars); (2) Árbol antes del split (535-668, ~3.960 chars); (3) Parte 5.bis "Histórico de la estrategia de migración (CERRADA)" (788-806, ~1.301 chars). Destinos: `docs/historico/PROCESO-MAESTRO-arboles-historicos.md` (los 2 árboles, 196 líneas) y `docs/historico/PROCESO-MAESTRO-parte5bis-migracion.md` (27 líneas). Árbol actual (vivo, Parte 3 línea 450), Decisiones cerradas (Parte 4), Decisiones pendientes (Parte 5) y Bitácora del documento intactos. PROCESO-MAESTRO 868 → 677 líneas (-22%). Validador U0/U1/U3 → 0/0. Próximo: v10.77 (compactar bloque A cerrado de REVIEW, decisión condicional).
- **2026-05-07 13:15** — Corrección de 2 inexactitudes menores en el acta de v10.75 (v10.75b). El revisor detectó que la verificación post-archivado había quedado registrada como `diagrama._read_version() = 10.74` cuando la real tras el commit es `10.75` (la primera entrada viva pasó a ser v10.75); y que la métrica final decía "977 líneas" cuando el archivo resultante tiene 1.004 (la diferencia es la propia entrada v10.75 + el puntero al histórico). Ambas corregidas en el acta de v10.75 (CHANGELOG líneas 23, 25 y bitácora REVIEW 13:00). El archivado en sí está bien hecho; solo precisión documental. Validador 0/0. Próximo: v10.76.
- **2026-05-07 13:00** — Archivado del CHANGELOG pre-v10.40 (v10.75). Limpieza documental autorizada por el revisor tras v10.74/v10.74b. Trasladadas 1.535 líneas (~112K chars) de la línea 978 al final del CHANGELOG a `docs/historico/CHANGELOG-pre-refactor.md`, sin reescribir. Puntero corto añadido bajo el título del CHANGELOG vivo. Verificaciones: 0 links internos rotos pre-edit; `diagrama._read_version()` devuelve `10.75` post-commit; validador U0/U1/U3 → 0/0. CHANGELOG vivo pasa de 2.512 a 1.004 líneas tras añadir entrada y puntero (-60%, ~28K tokens). Próximo: v10.76 (árboles históricos + Parte 5.bis de PROCESO-MAESTRO).
- **2026-05-07 12:30** — Cierre del pase de coherencia tras dictamen del revisor sobre v10.74 (v10.74b). v10.74 había sido parcial en 2 sitios reales: (1) la enumeración de 20 tipos seguía listando 17 y el shape de actividad seguía sin `enfoque` y con `destreza` como string en líneas 163-180 de PROCESO-MAESTRO; (2) la línea 779 reabría el modelo viejo de "Plantilla HTML del informe por unidad". Fix: enumeración completa de 20 tipos, `destreza` como lista alfabética con enum de 6, `enfoque` añadido con enum de 6, ambos con referencia a schema §5b/§5c y cita de versiones (v10.25/v10.59/v10.60/v10.64); línea 779 reescrita a "Refinamiento visual de la vista HTML dinámica". Validador U0/U1/U3 → 0/0. Pase de coherencia ahora cerrado limpio. Próximo: v10.75 con base documental limpia.
- **2026-05-07 12:00** — **Pase de coherencia documental** (v10.74). Bloqueante para la limpieza/compactación posterior y para la apertura de chats paralelos de extracción. Drift identificado en 7 sitios de PROCESO-MAESTRO (líneas 88, 128, 163, 231, 701) y REVIEW (líneas 45, 112): "validar_inventario.py a escribir" cuando es operativo, "taxonomía cerrada de 17 tipos" cuando son 20, "Plantilla HTML por unidad / tarea pendiente" cuando es vista dinámica del dashboard, A4 marcado como "EN CURSO" cuando cerró en v10.69. Todos corregidos a estado real. Parte 2 de Fase 1 actualizada a estado real, NO reducida a puntero (eso es paso futuro). Hits residuales legítimos confirmados: scripts globales (`regenerar_tarjetas_globales.py`, `regenerar_pildoras_globales.py`) que siguen pendientes y bitácora histórica intocable. Validador U0/U1/U3 → 0/0 sin cambios. Próximo: v10.75 (archivar CHANGELOG pre-v10.40) en worktree `docs/cleanup`. Paralelización de extracciones autorizada tras push de v10.74.
- **2026-05-07 11:30** — Cierre de referencias rotas en `schema-inventario.md` tras v10.72 (v10.73). Hallazgo del revisor: el archivado de v10.72 dejó 2 refs rotas en líneas 7 y 377 del schema apuntando a `REFACTOR-PROPUESTA.md` ya archivado. Decisión: formulación estable y atemporal (no re-apuntar al nuevo path) porque el schema describe contrato vigente del producto, no debería encadenarse a artefactos del proceso de refactor. Fix quirúrgico de 2 líneas. Validador U0/U1/U3 → 0/0. Sin más refs vivas a REFACTOR-*.md fuera de docs/historico. Próximo: push autorizado + re-extracción de U2 en rama nueva.
- **2026-05-07 11:00** — Archivado de documentos del refactor cerrado (v10.72). Los 2 artefactos del proceso (REFACTOR-PROPUESTA.md, REFACTOR-WORKTREE.md) movidos con `git mv` a `docs/historico/refactor-prompt-fase1/`, conservando historial. La carpeta `fases/1-extraccion-inventario/` queda solo con los 5 archivos operativos vivos (CLAUDE.md, prompt.md, schema, reglas, convenciones). Cross-references vivas actualizadas en PROCESO-MAESTRO.md decisión 34 y REVIEW.md sección A4 (líneas 118-150). Bitácora histórica y CHANGELOG anteriores NO se reescriben (mencionan los paths originales en su momento, no es revisionismo). Validador U0/U1/U3 → 0/0 post-archivado. `viejo/` sin tocar (regla del proyecto).
- **2026-05-07 10:30** — Ajuste de redacción en PROCESO-MAESTRO sobre informe HTML (v10.71). Drift detectado: la decisión 25 (línea 705) y la bitácora 2026-05-05 (línea 845) hablaban de "generar además un informe HTML visual" como si la fase 1 produjera un segundo artefacto. Implementación real (verificada en `diagrama.py` y `web/index.html`): el JSON es el único output de la fase; la "vista HTML" es renderización dinámica del dashboard al vuelo. Fix quirúrgico de 2 líneas en PROCESO-MAESTRO. Archivos de fase 1 sin tocar (ya correctos). Sin cambios funcionales.
- **2026-05-07 10:00** — Regla 3 de `CLAUDE.md` de fase reformulada (v10.70) tras observación post-merge del revisor sobre duplicación contractual entre entry points. La regla original prohibía duplicación entre `prompt.md`/schema/reglas/convenciones pero excluía tácitamente al propio `CLAUDE.md`. Auditoría previa: 7 bloques contractuales aceptables (Grupo A) + 0 hallazgos operativos (Grupo B). Fix quirúrgico de 1 línea: regla 3 reescrita según formulación del revisor (entry points pueden repetir hechos y reglas mínimas de contrato; lógica operativa duplicada fuera del archivo canónico sigue siendo bug). Sin tocar prompt.md ni archivos de soporte. Refactor totalmente cerrado.
- **2026-05-07 09:30** — **A4.6 cerrado: merge `refactor/prompt-fase-1` → `main` ejecutado** (commit `110e722`). Receta confirmada con el revisor: `git merge --no-ff` para preservar el rastro completo del refactor. Diff del merge: 14 archivos, +2555/–838 líneas. Archivos creados en `main`: `fases/1-extraccion-inventario/{schema-inventario,reglas-operativas,convenciones-y-casos}.md` + `REFACTOR-WORKTREE.md`. Archivos modificados: `prompt.md`, `CLAUDE.md` de fase, `validar_inventario.py`, los 3 oráculos U0/U1/U3, CHANGELOG, REVIEW, PROCESO-MAESTRO. **Validador en `main` post-merge:** U0 ✅, U1 ✅, U3 ✅, todos 0/0. **Gate A4 entero cerrado** (los 5 sub-gates del bloque "Gate de cierre de A4 entero" ya en ✅). Sin push automático — la decisión de empujar a remoto queda al autor. Branch `refactor/prompt-fase-1` y worktree `../guia-didactica-refactor/` se mantienen intactos por si hace falta inspeccionar el rastro. **Refactor de fase 1 cerrado.**
- **2026-05-07 09:00** — **Actas A4.5 y A4.5.5 cerradas (v10.68 docs).** **A4.5 — Prueba empírica:** los 3 oráculos U0/U1/U3 reclasificados al contrato actualizado (3 ejes ortogonales tipo/destreza/enfoque) en 4 commits iterativos con validación del revisor: v10.64 (U0 piloto, 10 act + ampliación taxonomía 19→20 con `escucha`), v10.65 (U1, 42 act + 3 fixes A1/A2/A3 del revisor + regla nueva de desempate §2.2.5), v10.66 (U3, 47 act + 3 refinamientos §2.2.1-3), v10.67 (cierre de bloqueante sobre `escucha_y_responde`, 3 reclasificaciones). Validador U0/U1/U3 → 0/0. Sin pérdida de decisiones semánticas cerradas. **A4.5.5 — Cross-check schema↔validador:** script de paridad ejecutado sobre las 5 enumeraciones cerradas → **5/5 idénticas, 0 divergencias** (`tipo` 20, `destreza` 6, `enfoque` 6, `tipo_cuadro` 5, `seccion` 7). El gate ineludible queda cumplido. Próximo: A4.6 (merge a main, requiere confirmación del autor).
- **2026-05-07 08:30** — **Cierre del bloqueante del revisor sobre uso de `escucha_y_responde` en 3 oráculos** (v10.67). Tras dictamen del revisor sobre v10.66: la definición de `escucha_y_responde` (respuesta oral, sin texto delante) chocaba con su uso real en U0-p11-act08, U0-p11-act09 (dictados con slots `_____`, deberían ser `completa_huecos` por regla 5) y U3-p39-act06 (alumno escribe antes de escuchar, no es respuesta a estímulo auditivo). 3 reclasificaciones aplicadas: U0-p11-act08/09 → `completa_huecos`, U3-p39-act06 → `produccion_escrita_guiada`. Definición de `escucha_y_responde` se mantiene (nicho legítimo). Cross-check informal del revisor confirma alineación schema↔validador en 20 tipos / 6 destrezas / 6 enfoques. U0/U1/U3 → 0/0. Sin otros hallazgos bloqueantes. Próximo: A4.5 (acta) + A4.5.5 (cross-check formal) + A4.6 (merge).
- **2026-05-07 08:00** — **Cierre de U3 (47 actividades) y refinamiento de desempates en §2.2** (v10.66). Reclasificación completa de U3: destrezas alfabéticas + enfoque + 11 cambios de tipo (10 "Completa..." → `completa_huecos` por regla nueva v10.65; U3-p39-act05 `escucha_y_repite` → `completa_huecos` tras decisión del autor en A1). U3-p41-act05 se mantiene en `produccion_escrita_guiada` por ambigüedad relojes-como-slots. **§2.2 refinada en 3 puntos** según decisiones del autor sobre A3 y A5: regla 1 (ver_video solo sin manipulación posterior), regla 2 (manipulación manda en cualquier punto del enunciado, no solo posterior), regla 3 (responder_preguntas_* con criterio explícito de destreza: texto-fuente → comprensión sin expresion_escrita; sin texto-fuente → expresion_escrita). Branch ahora con U0/U1/U3 todos ✅. Próximo: A4.5 (acta de prueba empírica) + A4.5.5 (cross-check schema↔validador) + merge a main.
- **2026-05-07 07:30** — **U1 reclasificada (42 actividades) + 3 fixes del revisor + regla nueva de desempate** (v10.65). Tras la primera pasada de U1, dictamen del revisor con 3 desajustes contractuales reales: (A1) U1-p12-act3 mal clasificada como `escucha_y_repite` cuando el enunciado encadena "escucha y repite + escucha y escribe número", obliga a `relaciona` por regla "última acción"; sin `expresion_escrita` por regla anti-sobreasignación. (A2) ambigüedad real en frontera `completa_huecos` vs `produccion_escrita_guiada` — el revisor propone regla explícita: huecos/celdas/slots predefinidos → `completa_huecos`; sin huecos, alumno construye → `produccion_escrita_guiada`. (A3) U1-p14-act3 "Forma frases" pertenece a `produccion_escrita_guiada` (ejemplo explícito en §2.2), no a `relaciona`. Fixes aplicados: 7 actividades con tipo corregido + regla nueva §2.2 punto 5 (desempate). Confirmación del autor: A1 = `relaciona`. Validador U1 → 0/0. Estado: U0 ✅, U1 ✅, U3 ❌ (pendiente). Próximo: U3.
- **2026-05-07 07:00** — **U0 reclasificada como piloto + taxonomía `tipo` 19→20** (v10.64). Prueba empírica del nuevo contrato destreza/enfoque sobre U0 (4 páginas, 10 actividades atípicas pre-A1.1). El sistema funciona end-to-end. Decisiones cerradas durante la prueba: (1) **`escucha` añadido a la taxonomía** (caso disparador U0-p8-act01: input puro auditivo sin lectura ni repetición; ningún tipo existente encajaba limpiamente). Decisión registrada en PROCESO-MAESTRO bitácora (regla §2.4). (2) **Regla `expresion_escrita` reformulada** — listas de palabras evocadas SÍ cuentan como `expresion_escrita` (caso U0-p8-act02 "escribe la lista de países que recuerdas"); distinción contenido propio vs transcripción. (3) **Heurística vocab/fonetica documentada** — agrupación por campo léxico → vocabulario, agrupación por dificultad fonética / deletrear / dictado → fonetica (dictado siempre fonetica incluso si es contenido léxico). U0-p8-act01 reclasificada `escucha_y_repite` → `escucha`. Validador U0 → 0/0. Branch sigue rojo en U1/U3 (pendientes de reclasificar). Próximo: U1.
- **2026-05-07 06:30** — Fix de drift post-v10.60 en CLAUDE.md de fase (v10.63). Re-revisión del componente 1 con criterio "contrato vs operativo": las 5 "Reglas críticas" pasan el test (literalidad, no inventar, single source of truth, validar antes de cerrar, no divergencia schema↔validador) — ninguna reescribe casuística migrable, no se tocan. Sí drift menor en la tabla de navegación: la columna "¿Cómo decido X?" listaba solo `tipo` y `tipo_cuadro`; tras v10.60 hay 4 ejes decisionales. Fix quirúrgico de una línea: añadidos `destreza` y `enfoque`. CLAUDE.md de fase 60 líneas (norte 40-60 cumplido). Próximo: componente 4 (reglas-operativas.md).
- **2026-05-07 06:00** — **Cierre del componente 3 (schema-inventario.md)** con 3 fixes accionables (v10.62). **F0 (Bloqueante)**: divergencia real schema↔validador en `_nota_unidad_atipica` — schema §11 la declara opcional contractual, validador no la tenía en `CLAVES_TOP_OPCIONALES`, U0 emitía aviso espurio. Fix: añadida al set; U0 valida sin avisos. **F1 (Medio)**: §13 desactualizada tras v10.60 — no listaba las 2 nuevas enumeraciones cerradas (`destreza` 6, `enfoque` 6) ni la restricción de orden alfabético de destreza. Fix: §13 reescrita con las 6 enumeraciones explícitas y restricciones condicionales completas. **F3 (Bajo)**: ambigüedad de `numero` — schema lo declaraba `<int>` sin obligatoriedad; algunas actividades ("Para aprender", autoevaluación) no tienen número visible. Decisión: opcional. Fix: schema §3 actualizado + nueva §3.1 explicativa; validador añade check de tipo si presente. Próximo: componente 4 — reglas-operativas.md.
- **2026-05-07 05:30** — Fix de drift documental post-v10.60 en `prompt.md` (v10.61). Revisión componente-a-componente: componente 1 (CLAUDE.md de fase) aprobado por el revisor sin findings materiales; componente 2 (prompt.md) con drift detectado — checklist manual y paso 5 no reflejaban `destreza`/`enfoque` obligatorios tras v10.60. Cambios quirúrgicos: paso 5 ampliado con los 3 ejes ortogonales y punteros a schema §5b/§5c; checklist 9 → 11 items con "Destrezas válidas" e "Enfoque válido" insertados. prompt.md 109 → 111 líneas (norte 80-120 cumplido). Próximo: componente 3, schema-inventario.md.
- **2026-05-07 05:00** — **Contrato `destreza`/`enfoque`: separación de ejes habilidad ↔ dominio** (v10.60, **commit intermedio rompedor de contrato**). Tras dos rondas con el revisor sobre cómo cerrar `destreza` (subespecificada en versiones previas), decisión final: separar en dos campos ortogonales. (1) `destreza` reescrita como **eje habilidad MCER puro**: lista de strings, enum cerrado de 6 valores (`comprension_auditiva`, `comprension_lectora`, `expresion_escrita`, `expresion_oral`, `interaccion_oral`, `mediacion`), orden alfabético obligatorio (cierra la deriva de serialización con orden libre que el revisor diagnosticó: 27 strings legacy colapsaban a 17 combos por orden/espacios), sin duplicados, mínimo 1 elemento. (2) **Nuevo campo `enfoque`** como **eje dominio de contenido**: string único, enum cerrado de 6 (`gramatica`, `vocabulario`, `comunicacion`, `fonetica`, `cultura`, `transversal`), obligatorio. El valor `transversal` reemplaza la propuesta inicial `destrezas` (que reutilizaba el nombre de una sección editorial y pegaba el eje al layout de página). (3) **Regla anti-sobreasignación de `expresion_escrita`**: mecánicas de manipulación (completar, relacionar, ordenar, marcar) NO añaden por sí mismas `expresion_escrita`; solo se añade cuando el alumno produce texto lingüístico propio. (4) Decisiones semánticas: `comprension_auditiva` canónico (no alias); `produccion_*` → `expresion_*` (MCER moderno); `gramatica`/`vocabulario` salen del eje destreza y entran en `enfoque`. (5) Validador: `DESTREZAS_VALIDAS` (6) + `ENFOQUES_VALIDOS` (6), validación de presencia/forma/enum/orden/no-duplicados. (6) Referencia colgada `§2b` → corregida a §2.3. **Estado intermedio aceptado:** branch en rojo, validador rechaza U0/U1/U3 (~250 errores: legacy string + ausencia de enfoque). La reclasificación per-unidad de las 97 actividades se hace en otro chat antes de A4.5. Gates en orden: reclasificación per-unidad → A4.5 → A4.5.5 → merge. Próximo: revisión componente-a-componente del refactor (en curso) + reclasificación per-unidad.
- **2026-05-07 04:00** — **Taxonomía de `tipo` rediseñada 17 → 19 por acción imperativa del enunciado** (v10.59), durante la primera pasada de A4.5. La pasada empírica sobre U1 con oráculo destapó 7 divergencias de `tipo` (más análogas en U3): la causa raíz no era mala clasificación sino ambigüedad de la taxonomía — `comprension_lectora`/`comprension_auditiva` mezclaban `tipo` (acción/formato) con `destreza` (habilidad). Decisión: separación de dimensiones. `tipo` = acción específica del enunciado; `destreza` = campo separado (próximo trabajo). Cambios: schema §5 reescrito con 19 valores agrupados en 7 familias; validador `TIPOS_VALIDOS`/`TIPOS_QUE_REQUIEREN_ITEMS` actualizados + 3 claves añadidas a `CONTENIDOS_VISIBLES` (`ejemplos_modelo`, `programas_tv`, `horarios_digitales` — divergencia schema↔validador cerrada como adelanto parcial de A4.5.5); `reglas-operativas.md` §2 reescrita íntegra (§2.1 regla operativa, §2.2 tabla canónica, §2.3 nota tipo vs destreza); 16 actividades reclasificadas (U1: 9; U3: 7; U0: 0). Validación: U0/U1/U3 → 0 errores. Mapeo de reasignaciones: U1-p12-act1 `comprension_lectora`→`lee_y_escucha`; U1-p13-act7 `produccion_escrita_guiada`→`completa_huecos`; U1-p13-act9 `comprension_lectora`→`responder_preguntas_cerradas`; U1-p16-act1 `comprension_auditiva`→`ver_video`; U1-p17-act7 `comprension_auditiva`→`completa_huecos`; U1-p18-act1 `comprension_lectora`→`completa_huecos`; U1-p19-act3 `comprension_auditiva`→`completa_huecos`; U1-p19-act4 `comprension_auditiva`→`seleccion_multiple`; U1-p21-act6 `expresion_escrita_libre`→`responder_preguntas_abiertas`; U3-p35-act06/08 `comprension_lectora`→`lee_y_escucha`; U3-p35-act09 `comprension_lectora`→`responder_preguntas_cerradas`; U3-p40-act01 `comprension_lectora`→`produccion_escrita_guiada`; U3-p40-act02 `comprension_lectora`→`responder_preguntas_cerradas`; U3-p41-act04 `comprension_auditiva`→`relaciona`; U3-p42-act01 `comprension_lectora`→`lee_y_escucha`. Próximo: trabajar la dimensión `destreza` como campo separado.
- **2026-05-07 03:15** — Sincronización de fila-resumen A4.4 con la bitácora (v10.58, hallazgo Bajo del revisor): la tabla de sub-pasos seguía diciendo "✅ 02:30 (57 líneas)" mientras CHANGELOG y bitácora ya reflejaban el cierre limpio en v10.57 con 59 líneas. Fila reescrita a "cerrado limpio 03:00 en v10.57 (59 líneas)". Adicional: el revisor confirma que ejecutó el validador desde la raíz del repo con la convención root-relative ahora explícita y devuelve JSON válido y 0 avisos para U1 — primera verificación funcional externa al ejecutor (informal, no sustituye A4.5). Próximo: A4.5.
- **2026-05-07 03:00** — **Cierre limpio de A4.4 tras dictamen del revisor** (v10.57): hallazgo Medio sobre ambigüedad de comandos. `CLAUDE.md` de fase se auto-carga al trabajar dentro de `fases/1-extraccion-inventario/`, pero los comandos `python3 scripts/validar_inventario.py X` y `python3 diagrama.py` son root-relative — fallan con "No such file or directory" si se ejecutan literalmente desde la carpeta de fase. La convención del repo es root-relative en todos los docs (CLAUDE.md raíz también lo es), pero no estaba declarada explícitamente. Resolución: nota explícita "Convención de comandos: root-relative" añadida al inicio de "Cómo validar" (CLAUDE de fase) y de "Cierre y validación" (prompt.md). Antes de A4.5 (primera prueba funcional oficial) la ambigüedad queda eliminada. CLAUDE 57 → 59, prompt 107 → 109 (ambos siguen dentro de su norte). Próximo: A4.5.
- **2026-05-07 02:30** — **A4.4 cerrado** (v10.56): `CLAUDE.md` de fase reescrito en modo contrato corto según REFACTOR-PROPUESTA §3.1. Estructura final 57 líneas, 7 secciones (Qué produce, Input/output, Cómo se invoca, Cómo validar, Reglas críticas, Para qué consultar qué archivo, Documentos relacionados). Eliminados 4 bloques redundantes del CLAUDE pre-refactor: (1) sección "Reglas operativas críticas (resumen)" de 22 líneas que reescribía contenido ya migrado a `reglas-operativas.md` y `schema-inventario.md` violando single source of truth; (2) "Coste estimado" (no operativo, ya eliminado del prompt en A4.3); (3) "Mejora continua" (vive ahora en `convenciones-y-casos.md` §5); (4) "Contexto futuro CrewAI" (especulación, no contrato operativo). Pre-refactor: 111 líneas, 9 secciones. Post-A4.4: 57 líneas, 7 secciones. Reducción del 49%. Próximo: A4.5 (prueba funcional empírica de reextracción — primera prueba funcional oficial del plan).
- **2026-05-07 02:00** — **A4.3 cerrado** (v10.55): `prompt.md` reescrito desde cero según REFACTOR-PROPUESTA §5 paso 3. Estructura mínima en 9 secciones (Objetivo, Input, Output, Definición de éxito, Regla de oro, Artefactos de soporte, Pasos de la extracción, Cierre y validación + Salida absorbida). Eliminados los 4 placeholders intermedios redundantes (Reglas decisionales, Convenciones de transcripción y ejemplos canónicos, Convenciones específicas, Reglas para cuadros, Casos resueltos y mejora continua) — la cabecera global "Artefactos de soporte" cubre todas las referencias a los archivos hermanos. Sección "Definición de éxito" añadida nueva (no estaba en pre-refactor) con 4 condiciones explícitas alineadas con el plan. "Cierre y validación" absorbe la antigua "Validación post-extracción" + Salida en una sola sección con sub-encabezados. Conteo: 107 líneas (dentro del norte 80-120). Próximo: A4.4 (reescribir CLAUDE.md de fase, hoy 111 líneas, objetivo 40-60).
- **2026-05-07 01:30** — **A4.2c cerrado y A4.2 cerrado al completo** (v10.54). Migrado a `convenciones-y-casos.md` todo el contenido residual del prompt: convenciones de transcripción (sílaba tónica subrayada hasta U3, patrón "primer ítem resuelto como ejemplo", textos de lectura, diálogos con marcadores `[1]`/`[2]`, sopas de letras), ejemplos canónicos de `items_libro` por tipo de actividad (cloze, selección múltiple, cuestionario), ejemplos INCORRECTOS, ejemplo JSON canónico de U0, casebook de U3, política de mejora continua. **Sección "Coste estimado" eliminada** del prompt (no aporta valor operativo). **Aplicada proactivamente la lección de v10.53** (verificar zona reemplazada como enlace limpio): detectadas y corregidas 3 referencias huérfanas en los pasos de la extracción que apuntaban a secciones eliminadas; redirigidas a los archivos externos (`reglas-operativas.md` §1/§3/§4/§7, `schema-inventario.md` §3, `convenciones-y-casos.md` §1.1/§1.2). 8 anclas semánticas verificadas: 7/8 perfectas (presentes en convenciones, ausentes de prompt); la única en prompt ("primer ítem resuelto") es referencia legítima dentro de los pasos. Métricas: prompt 108 (de 547 originales, –80%), schema 308, reglas-operativas 208, convenciones-y-casos 165. **A4.2 cerrado al 100%.** Próximo: A4.3 (reescribir prompt.md core desde cero — el prompt actual ya cumple norte de tamaño 80-120 líneas, A4.3 será revisión y consolidación de la estructura).
- **2026-05-07 00:30** — **Cierre real de A4.2b** (v10.53) tras dictamen del revisor: el v10.52 declaró ✅ prematuramente. Hallazgo bloqueante real: la cabecera transicional de `prompt.md` (líneas 44-52, "Esquema y schema del JSON") seguía describiendo el estado pre-A4.2b ("se migrarán en A4.2b", "Hasta entonces", "vive provisionalmente en este prompt"), mientras el resto del archivo y `reglas-operativas.md` ya describían el estado post-A4.2b. Reabría exactamente la contradicción de source of truth que v10.50/v10.51 cerraron. El criterio del paso 2 del plan (zona reemplazada del prompt = enlace limpio sin semántica residual) no se había aplicado a esa cabecera. Resolución: cabecera reescrita como puente corto y verdadero (3 bullets: schema → schema-inventario.md, reglas decisionales → reglas-operativas.md, convenciones → convenciones-y-casos.md "en construcción"). Verificación reforzada: grep de afirmaciones pre-A4.2b en prompt → 0. Las 3 referencias residuales a "A4.2c" son legítimas (esperan ese sub-paso). Cabecera REVIEW sincronizada (v10.52 había quedado en 22:30 cuando el commit fue a las 23:58). A4.2b queda ahora **cerrado limpio**. Próximo: A4.2c.
- **2026-05-06 22:30** — **A4.2b cerrado** (v10.52). Migrado a `reglas-operativas.md` todo el contenido decisional: precedencias actividad/cuadro/nota/autoevaluación, criterios de `tipo` (con la Distinción crítica `completa_huecos` vs `produccion_escrita_guiada` ahora canónica aquí), criterios de `tipo_cuadro`, "Para aprender" / "Observa", reglas de población de cada campo (`vocabulario_consolidado`, `secciones`, `seccion`, `respuestas`, `audio/imagen/video`, `campo_semantico`, `items_libro`), bloque `autoevaluacion` y unidades atípicas. **Hallazgo importante durante la ejecución:** A4.2a fue más extensiva en pérdidas de lo identificado en v10.51 — además de la "Distinción crítica" se habían perdido también las reglas decisionales de `vocabulario_consolidado` (criterios principal/recurrente/comprension), `respuestas` (formato selección múltiple, V/F), `campo_semantico` (cuándo aplica + nota "liberal por ahora") y `secciones` top-level (regla "actividades_ids en orden", regla "secciones inexistentes vacías"). A4.2b las restaura todas tomando como fuente `cc1f18b` (estado pre-refactor). Verificación de anclas: 7 frases canónicas presentes en `reglas-operativas.md` y ausentes de `prompt.md`. Métricas: prompt 260, schema 308, reglas-operativas 208, convenciones-y-casos 10. Próximo: A4.2c.
- **2026-05-06 21:30** — **Cleanup pre-A4.2b parte 2** (v10.51): el revisor señaló que v10.50 reformuló pero no cerró el hallazgo del source of truth de la taxonomía. Verdad única alineada entre `prompt.md` y `reglas-operativas.md`, pero **falsa**: el prompt afirmaba "el contenido vive aquí" cuando en realidad el bloque decisional había sido absorbido por error de scope en A4.2a (placeholder grande de v10.47). Pérdida real, no solo de redacción. Resolución: bloque "Distinción crítica `completa_huecos` vs `produccion_escrita_guiada`" recuperado de `cc1f18b` y restaurado en `prompt.md` como nueva sección "Reglas decisionales provisionales (a migrar en A4.2b)". Ambos archivos describen ahora con honestidad qué hay y qué no: distinción crítica explícita en prompt; resto de criterios para los 17 tipos = implícitos del dominio editorial + oráculo de facto en U0/U1/U3, a canonizar en A4.2b. El bajo (tabla de secciones) ya estaba bien resuelto en v10.50. Próximo: A4.2b ya con source of truth consistente.
- **2026-05-06 21:00** — **Cleanup pre-A4.2b tras dictamen del revisor** (v10.50). Dos correcciones antes de arrancar A4.2b: (a) eliminada doble verdad sobre source of truth de la taxonomía — el placeholder de `prompt.md` decía "viven en reglas-operativas (se migran en A4.2b)" cuando reglas-operativas decía "viven aún en prompt"; ambos archivos ahora alineados: source of truth provisional = `prompt.md` hasta que A4.2b cierre. (b) Tabla de mapeo de headers de sección en `reglas-operativas.md` corregida contra los inventarios reales: U1-p21 y U3-p43 (páginas con bloque `autoevaluacion` top-level) usan `seccion: evaluacion` en el JSON, no `reflexion`; mi tabla decía "Reflexión / Autoevaluación / cierre → reflexion" lo cual era una regla inventada sin avalar. Tabla reescrita con columna "Avalado en" citando los casos del oráculo, y nota explícita sobre `reflexion`: el valor existe en el enum pero ningún inventario actual lo usa, decisión diferida hasta primer caso real o cierre de A4.2b. Próximo: A4.2b.
- **2026-05-06 20:30** — **Cleanup de A4.2a tras dictamen del revisor** (v10.49). El cierre de A4.2a se había sobredeclarado: 3 fugas decisionales se habían colado en `schema-inventario.md` violando la frontera "split por capa": (1) "se omite en unidades atípicas" (cuándo, no forma); (2) "Las páginas que continúan una sección usan la misma clave normalizada" (cómo determinar, no enumeración); (3) workflow "se consulta al autor" en taxonomía (no contrato de datos puro). Los 3 fragmentos retirados de schema y absorbidos en `reglas-operativas.md` como adelanto parcial de A4.2b. Schema ahora contiene solo formas + enumeraciones + restricciones validables + líneas-puente a reglas-operativas. Verificación: grep del contenido decisional → reglas=1, schema=0 en cada caso. Métrica corregida: schema-inventario.md tiene **308 líneas** (no "300" como dijo v10.47 por aproximación), `prompt.md` 290 (correcto), `reglas-operativas.md` ahora 75 líneas (antes 8). Próximo: A4.2b (continuar la migración de reglas-operativas con el resto del contenido decisional del prompt).
- **2026-05-06 19:45** — Regla de tipología de verificaciones añadida (v10.48): A4.2-A4.4 = checks locales de integridad documental (no pruebas funcionales). A4.5 = primera prueba funcional oficial (reextracción empírica). A4.5.5 = cross-check schema↔validador antes del cierre. Smoke test opcional tras A4.4 permitido pero no es gate formal. Insertado en `REFACTOR-PROPUESTA.md` §5 como nota destacada al inicio del plan; eco breve en este REVIEW tras la tabla de sub-pasos. Próximo: A4.2b.
- **2026-05-06 19:30** — **A4.2a cerrado** (v10.47): contenido estructural migrado del `prompt.md` a `schema-inventario.md`. El nuevo schema-inventario.md (300 líneas, 13 secciones numeradas) contiene: estructura top-level (10+1 claves), schema por página/actividad/cuadro, schema del bloque autoevaluación, taxonomía cerrada de 17 tipos, enumeraciones de `tipo_cuadro` (5) y `seccion` (7), estructura de `vocabulario_consolidado` y demás campos, restricciones condicionales validables (ej. `imagen.descripcion` obligatoria si `presente=true`), y la nota de source-of-truth con `validar_inventario.py`. En `prompt.md` las 9 secciones movidas se reemplazaron por un único placeholder con enlace al schema. Verificación de anclas semánticas: 4 frases canónicas verificadas (`Taxonomía cerrada de tipos de actividad`, `tipo_cuadro describe la categoría pedagógica`, `Mis resultados en esta unidad son`, `MUY BUENOS / BUENOS / NO MUY BUENOS`) — todas presentes en schema, ausentes de prompt. `prompt.md` pasó de 547 a 290 líneas; el resto de migraciones bajará el tamaño hasta el norte ~80-120 líneas tras A4.3. Próximo: A4.2b (migrar a `reglas-operativas.md`).
- **2026-05-06 18:45** — **A4.1 cerrado** (v10.46): tres archivos auxiliares creados en `fases/1-extraccion-inventario/` con headers de identidad solo (sin contenido editorial todavía): `schema-inventario.md` (responsabilidad: contrato de datos puro, contrato paralelo con `validar_inventario.py`), `reglas-operativas.md` (responsabilidad: decisión + clasificación + población + unidades atípicas, single source of truth de precedencias), `convenciones-y-casos.md` (responsabilidad: transcripción + casebook). Cada header incluye qué SÍ contiene, qué NO contiene y referencia al mapeo de la sección 4 de `REFACTOR-PROPUESTA.md` para A4.2. `prompt.md` sigue intacto en 547 líneas — la migración de contenido es A4.2. Próximo: A4.2 (mover contenido fila por fila + verificación de anclas semánticas).
- **2026-05-06 17:00** — Limpieza de dos residuos cosméticos del commit v10.44 (v10.45). (a) Tabla de estado del CHANGELOG decía `e3ed91d` como HEAD del worktree, pero el propio commit v10.44 lo elevó a `a9f710e`; actualizado a `a9f710e` y aclarado que es el SHA "al cerrar v10.44" — el HEAD vivo cambia con cada commit del refactor (referencia a `git worktree list` para el estado actual). (b) `REFACTOR-WORKTREE.md` cerraba con "commit posterior bumpeará la versión a v10.44" cuando ese commit ya había ocurrido; reescrito como referencia retrospectiva al commit `a9f710e`/v10.44.
- **2026-05-06 16:30** — **Migración a worktree dedicado** (v10.44, refinamiento de A4.0). Tras dictamen del revisor con lente Anthropic-first: la rama `refactor/prompt-fase-1` se mueve a un worktree propio en `/Users/armandocruz/Desktop/guia-didactica-refactor/`. El directorio original (`/Users/armandocruz/Desktop/guia-didactica-profesor-IA/`) vuelve a `main`. **Verificación clave:** los untracked `unidades/U2/` y `viejo/_template/` NO existen físicamente en el worktree del refactor — git worktree crea checkout fresco que no copia untracked del checkout originante. Esto elimina ruido durante A4.1-A4.6. Documentado paso a paso en `fases/1-extraccion-inventario/REFACTOR-WORKTREE.md` (incluye comandos de verificación, cierre y aborto). El estado de A4.0 queda como ✅ cerrado con el sub-detalle del worktree. Próximo: A4.1 (crear los 3 archivos auxiliares vacíos: `schema-inventario.md`, `reglas-operativas.md`, `convenciones-y-casos.md`).
- **2026-05-06 16:00** — **A4.0 cerrado** (v10.43): tag `pre-refactor-prompt-fase1` creado sobre HEAD del main pre-refactor (`cc1f18b`); rama `refactor/prompt-fase-1` creada y checkout activo. `main` queda intacto en `cc1f18b` durante todo el refactor. Si rollback: `git checkout main` (sin reset destructivo). Próximo: A4.1 (crear los 3 archivos auxiliares vacíos: `schema-inventario.md`, `reglas-operativas.md`, `convenciones-y-casos.md`).
- **2026-05-06 15:30** — Limpieza grep tras dictamen del revisor (v10.42, hallazgo bajo no bloqueante): anotación inline *"(renumeradas a 28-34 en v10.41)"* añadida a las dos entradas históricas que seguían siendo grep-ables con el rango viejo "27-33" sin contexto en la misma línea (bitácora del 14:30 + CHANGELOG v10.40 sección 1). El texto histórico se mantiene íntegro (no se reescribe lo que sí hizo v10.40); solo se contextualiza inline. Próximo: A4.0 (tag + rama).
- **2026-05-06 15:00** — Fix de coherencia documental tras dictamen del revisor (v10.41): (a) PROCESO-MAESTRO Parte 4 — eliminada numeración duplicada de "Decisión 27"; el bloque "Arquitectura datos+instrucciones" se ha movido de Parte 5 a Parte 4 (donde corresponde por estar cerrada) preservando su número 27 por antigüedad; las decisiones del refactor de fase 1 (v10.40) se renumeran de 27-33 a 28-34. Eliminado el subheader "Decisiones cerradas adicionales (post-creación inicial)" de Parte 5 (contradecía el título "Decisiones pendientes" de la propia parte). (b) Cabecera "Última actualización" de REVIEW sincronizada con la bitácora.
- **2026-05-06 14:30** — Refactor de fase 1 documentado como plan ejecutable (v10.40). Antes de empezar la ejecución, plan trazado en los dos artefactos canónicos: PROCESO-MAESTRO Parte 4 ampliada con 7 decisiones cerradas (27-33 *— renumeradas a 28-34 en v10.41 por colisión con la "Decisión 27" preexistente; ver entrada del 15:00*: arquitectura, frontera de capas, source of truth, skill fuera, contrato schema↔validador, delegación operativa a REFACTOR-PROPUESTA.md); REVIEW bloque A con paso A4 nuevo y 8 sub-pasos enumerados (A4.0-A4.6 + A4.5.5 cross-check obligatorio). Estado global bloque A actualizado a "🔄 A4 en curso". Cero código tocado: solo documentación del plan ejecutable. Próximo: A4.0 (tag + rama).
- **2026-05-06 14:00** — REFACTOR-PROPUESTA: 2 rastros residuales tras v10.38 (v10.39). (a) Tabla de riesgos seguía hablando de "checklist" cuando el paso 2 ya usa marcador externo — mitigación reescrita en consonancia. (b) Verificación de anclas en paso 2 decía "desaparecer del prompt core" pero el prompt core no existe hasta el paso 3 — reformulado para respetar la secuencia: durante paso 2 se verifica que el ancla esté en destino y NO en la zona reemplazada por placeholder; la comprobación "ancla solo en un sitio" se cierra al terminar el paso 3.
- **2026-05-06 13:30** — REFACTOR-PROPUESTA: 4 correcciones de coherencia interna tras nuevo dictamen del revisor (v10.38). Fix Medio: mapeo de unidades atípicas (línea 246) repartido entre los 3 destinos según capa, en lugar de monolíticamente a `reglas-operativas.md` — coherente con el principio "split por capa" que el propio documento declara. Fix Bajos: (a) subpaso de "checklist" reformulado para no asumir un formato que la tabla no tiene; (b) anclas semánticas sustituidas por frases que sí existen en `prompt.md` actual ("Taxonomía cerrada de tipos de actividad", "tipo_cuadro describe la categoría pedagógica", "primer ítem resuelto como ejemplo"); (c) cifra "9 ejecuciones" corregida a "10 (U0-U9)" en las dos referencias del documento.
- **2026-05-06 13:00** — REFACTOR-PROPUESTA: fix completo de la tabla "Estado medido" (v10.37). El fix de v10.36 había sido parcial: solo corrigió la fila de `prompt.md` pero dejó la fila de `CLAUDE.md` fase con datos viejos (`7|11` cuando el real es `9|10`). Ahora íntegramente reproducible. Verificación cruzada del resto de cifras del documento (17 tipos, 5 tipo_cuadro, 7 secciones, 3 opciones, líneas de los dos archivos) — todas correctas. Lección registrada: cuando una tabla es "evidencia dura", verificar todas sus filas, no solo la que motiva la revisión.
- **2026-05-06 12:30** — REFACTOR-PROPUESTA: dos correcciones tras dictamen del revisor (v10.36). Conteo "34 secciones" sustituido por dato real (27 `##` / 37 totales) con nota de reproducibilidad. Sección 8 (scope) reescrita para eliminar la contradicción entre "no se toca el validador" y "alinear validador antes del merge": ahora dice explícitamente que la alineación es prerequisito ineludible del merge, en commit aparte, fuera del scope nominal pero no opcional. Documenta que los defectos estaban en el commit `d15a0dd` antes de detectarse.
- **2026-05-06 12:00** — Sincronización del plan con el trabajo real + propuesta de refactor de fase 1 (v10.35): insertado **B1.5** (diseño de `nc1-reciclaje.json` con gate propio, resuelve la doble verdad entre nota del bloque B y B2); insertado **B5** (despliegue público del dashboard como ✅ cerrado, marcado explícitamente como carril paralelo fuera del orden B1-B4); tabla de Contenido editorial reestructurada con estado real verificado contra filesystem (U0/U1/U3 trackeados y validan; **U2 solo working tree, no trackeado, no valida**: 2 errores de `autoevaluacion.emoticonos` ausente; U4-U9 carpetas vacías); cabecera "Última actualización" corregida; conteo de líneas obsoleto de `CLAUDE.md` eliminado; "A3 (verificar bug B3)" como próxima modificación de `diagrama.py` retirada (A3/B3 ya cerrados). Adicional: creado `fases/1-extraccion-inventario/REFACTOR-PROPUESTA.md` tras 4 rondas de revisión (propuesta aprobable, sin ejecutar todavía — ver CHANGELOG v10.35).
- **2026-05-06 11:00** — Build slim para Railway (v10.34): quitadas `crewai`, `langfuse`, `deepeval` de requirements.txt y `node`+`promptfoo` del Dockerfile. Solo queda lo que el dashboard necesita en runtime. **Documentado retroactivamente en B5 como cerrado**; el dashboard sirve v10.34 en `https://entornoeditorial.up.railway.app`.
- **2026-05-06 09:30** — Tildes/eñes corregidas en los 3 diagramas Mermaid del dashboard (v10.33). Niveles 1, 2 y 3. ERD y rutas de filesystem sin tocar. Entrada CHANGELOG retroactiva: el commit `5024914` se pusheó antes de bumpear versión, lo que viola la regla "cada commit bumpea versión" — esta entrada lo regulariza.
- **2026-05-06 00:30** — Correcciones tras 2.º dictamen del revisor (v10.32): validador endurecido — añadidas validaciones de presencia de `emoticonos`, tipos de sub-campos (int/str/bool/list-of-str), y valores fijos NC1 (instrucción literal, opciones canónicas, emoticonos=true) cuando `curso == "nc1"`. PROCESO-MAESTRO encabezado: "10 claves" → "10 claves obligatorias + 1 opcional". CHANGELOG v10.32 nota del validador actualizada.
- **2026-05-06 00:15** — Correcciones tras dictamen del revisor (v10.31/v10.32): U0 cuadro "Saludos" reclasificado de `lexical` a `cultural` (según definición del prompt: fórmulas sociales = cultural). CHANGELOG v10.31 desglose corregido (eran 10 cuadros = 4+1+1+2+2, antes sumaba 11). Limpiadas 2 referencias antiguas restantes: prompt.md paso 4 ("Reglas para cuadros gramaticales" → "Reglas para cuadros") y comentario en web/index.html ("Renderizar cuadros gramaticales" → con los 5 tipos).
- **2026-05-05 23:45** — Bloque de autoevaluación como campo top-level (v10.32): U1 migrado del `_nota` al top-level `autoevaluacion`, U3 capturado por primera vez (p43), U0 sin cambios (atípica). Validador, prompt, CLAUDE.md fase 1, ERD, dashboard y PROCESO-MAESTRO sincronizados.
- **2026-05-05 23:30** — Schema cuadros migrado (v10.31): `cuadros_gramaticales` → `cuadros` + `tipo_cuadro` en U0, U1, U3. Validador actualizado. Prompt, CLAUDE.md fase 1, diagrama.py, web/index.html y PROCESO-MAESTRO sincronizados. Validación: U0 1 aviso intencional, U1/U3 limpios.
- **2026-05-05 23:00** — Terminología ELE aplicada (v10.25): 3 tipos renombrados en los 3 JSONs + validador + prompt + CLAUDE.md de fase 1. CHANGELOG v10.25 añadido.
- **2026-05-05 22:00** — A1 validado por el autor (47 actividades de U3 correctas). A3 cerrado: B1 pospuesto (CrewAI bloqueado), B2 aceptado (en viejo sin trackear), B3 resuelto en v10.15, B4 no requiere acción (cosmético, se resuelve con nuevo schema). Bloque B parcializado: tarjetas necesita fase 2 primero, píldoras en su momento, reciclaje próximo a discutir. Próximo paso: diseñar `nc1-reciclaje.json` y su visualización en dashboard.
- **2026-05-05 21:30** — Rebajada afirmación "arquitectura limpia" en CHANGELOG v10.17 (revisor): se limpió solo el diagrama, no el código (referencias legacy a `viejo/repertorios/` siguen en `diagrama.py:550-557` para el flujo de agentes bloqueado).
- **2026-05-05 21:00** — Dashboard saneado: sidebar con INVENTARIOS/PROYECTO/AGENTES en mayúsculas (AGENTES bloqueado), arquitectura mermaid sin caja `viejo/`, zoom +/- en diagramas, eliminadas 3 referencias residuales a `padStart(2,'0')`. Cero referencias hardcoded a U01-U09 en la UI.
- **2026-05-05 20:00** — A2 cerrado (U0 extraído como prueba). Prompt de fase 1 ampliado con 3 secciones nuevas (unidades atípicas, sílaba tónica subrayada hasta U3, patrón "primer ítem resuelto como ejemplo"). Convención de naming extendida a U0 en docs.
- **2026-05-05 18:30** — CLAUDE.md raíz reducido a 85 líneas (Anthropic best practices). Movida historia/estado/planes a PROCESO-MAESTRO/README/REVIEW según corresponda.
- **2026-05-05 18:00** — Confirmada arquitectura datos centralizados + instrucciones modulares. Añadido al inicio del documento como principio arquitectónico no negociable. Sin cambios estructurales en los pasos.
- **2026-05-05 17:00** — Reescrito con gates explícitos. Cada paso ahora declara: archivos involucrados + actualizaciones meta requeridas + gate de cierre con condiciones numeradas + validación del revisor + dependencias hacia adelante. Añadidas reglas globales del proceso (qué meta-archivo se actualiza cuándo) y sección "cómo el revisor analiza este documento".
- **2026-05-05 16:30** — Creación inicial.
